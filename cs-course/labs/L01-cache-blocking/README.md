# L01 缓存分块矩阵乘法

比较三种等价的方阵乘法：

1. `ijk`：朴素循环，按列访问 `B`；
2. `ikj`：交换循环次序，让内层连续访问；
3. `blocked`：把工作集切成能留在缓存中的小块。

```bash
make test
make run
./build/matmul --size 1024 --block 32
```

`make test` 用小矩阵核对三版结果；`make run` 输出 CSV，可改变矩阵大小和块大小观察
缓存层级。扩展任务：扫描 `8,16,32,64,128` 五种块大小，并解释最佳点为什么随 CPU
变化。

