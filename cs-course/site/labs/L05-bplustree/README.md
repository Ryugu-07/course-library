# L05 B+ 树

实现内节点导航、叶节点存值、插入分裂和叶链范围扫描。`order` 表示内节点最多拥有
多少个孩子；所有数据只出现在叶节点。

```bash
python3 -m unittest -v
python3 bplustree.py
```

测试会随机插入键、检查树结构不变量，并把范围扫描结果与 Python 排序结果对照。
扩展任务：实现删除与再平衡，再把节点序列化成固定大小页。

