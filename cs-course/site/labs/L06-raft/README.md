# L06 Raft 选主与日志复制

这是单进程、离散时钟的 Raft 教学模拟器。网络消息经过显式队列，可以分区、恢复或按
概率丢包；节点实现 term、投票、选主、心跳、日志一致性检查和多数派提交。

```bash
python3 -m unittest -v
python3 raft.py
```

实现为便于观察而让 leader 每次发送完整的缺失后缀，没有加入快照和持久化。扩展任务：
记录每条消息，画出一次“旧 leader 被隔离、新 leader 当选、网络恢复”的时序图。

