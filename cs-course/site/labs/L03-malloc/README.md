# L03 显式空闲链表分配器

`mm.c` 在一块固定大小的模拟堆上实现：

- 16 字节对齐；
- 头部/脚部边界标记；
- 显式双向空闲链表；
- first-fit、块分裂和四种相邻块合并。

```bash
make test
```

测试覆盖对齐、数据保持、分裂、向前/向后合并以及随机压力序列。扩展任务：把
first-fit 改成 segregated free lists，记录吞吐量和峰值利用率的变化。

