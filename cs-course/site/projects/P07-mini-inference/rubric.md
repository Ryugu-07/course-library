# P07 细化评分表

总分 100。CPU 基础路径和 GPU 扩展分开记录；没有 GPU 不影响基础功能/故障/报告评分。

| 维度 | 权重 | 满分表现 | 部分完成参考 |
|---|---:|---|---|
| 功能正确性 | 40 | 单请求 golden、KV cache、连续 batch、paged KV、量化路径按契约工作 | 只有单请求或 cache 与输出不一致 |
| 故障与边界 | 25 | 取消、预算/OOM、page 回收、隔离、错误输入、量化极值有测试 | 只测固定短请求，存在泄漏/串 token |
| 性能/资源 | 15 | TTFT/TPOT/吞吐、batch 利用率、KV bytes/碎片、量化误差可复现 | 只报 tokens/s 或只在单请求测量 |
| 结构与文档 | 10 | schema/engine/scheduler/cache/backend 分层，事件可观测，CPU 默认可跑 | GPU 依赖硬编码或资源生命周期不清 |
| 报告与反思 | 10 | 解释内存—精度—速度—公平性取舍、失败案例和平台限制 | 只有截图或宣传数字，缺少方法 |
