#include "benchmark.h"

int student_stencil(const float *input, float *output,
                    size_t width, size_t height, size_t steps) {
    /* TODO(P06): replace this stub with one measured optimization at a time. */
    (void)input;
    (void)output;
    (void)width;
    (void)height;
    (void)steps;
    return -1;
}
