(function () {
  "use strict";

  var LAB_ID = "auto-digital-twin";
  var SVG_NS = "http://www.w3.org/2000/svg";
  var STYLE_ID = LAB_ID + "-styles";
  var DEFAULTS = { samples: 16, driftStart: 8, driftRate: 0.25, syncEvery: 4, syncGain: 0.2, threshold: 0.8, persistence: 2, noiseAmp: 0.15, gainMismatch: 0.025 };
  var NOISE = [0.2, -0.5, 0.35, -0.15, 0.1, -0.25, 0.3, -0.1, 0.15, -0.4, 0.25, -0.2, 0.1, -0.3, 0.2, -0.1, 0.25, -0.15, 0.1, -0.2, 0.3, -0.1, 0.2, -0.25];
  var CSS = [
    '[data-learning-lab="auto-digital-twin"]{--dt-blue:var(--cl-blue,#315f9d);--dt-orange:var(--cl-gold,#9b6a12);--dt-green:var(--cl-green,#39734d);--dt-red:var(--cl-red,#b64335);display:block;max-width:100%;color:var(--fg,currentColor);line-height:1.55;overflow-wrap:anywhere}',
    '[data-learning-lab="auto-digital-twin"] *{box-sizing:border-box}[data-learning-lab="auto-digital-twin"] [hidden]{display:none!important}[data-learning-lab="auto-digital-twin"] h3{margin:0;font-size:1.16rem;letter-spacing:0}[data-learning-lab="auto-digital-twin"] p{margin:8px 0}',
    '[data-learning-lab="auto-digital-twin"] .dt-note,[data-learning-lab="auto-digital-twin"] .dt-feedback{font-size:13px;color:var(--fg-soft,currentColor);line-height:1.7}[data-learning-lab="auto-digital-twin"] fieldset{min-width:0;margin:10px 0;padding:9px 10px;border:1px solid var(--border,#cbd5e1);border-radius:6px}[data-learning-lab="auto-digital-twin"] legend{max-width:100%;padding:0 4px;font-size:13px;font-weight:750;line-height:1.5}',
    '[data-learning-lab="auto-digital-twin"] .dt-choices{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:7px}[data-learning-lab="auto-digital-twin"] button,[data-learning-lab="auto-digital-twin"] input{font:inherit;letter-spacing:0}[data-learning-lab="auto-digital-twin"] button{min-width:0;min-height:44px;padding:8px 11px;border:1px solid var(--border,#cbd5e1);border-radius:6px;background:var(--bg,transparent);color:var(--fg,currentColor);line-height:1.35;cursor:pointer;overflow-wrap:anywhere}[data-learning-lab="auto-digital-twin"] button:hover{border-color:var(--accent,#315f9d)}[data-learning-lab="auto-digital-twin"] button[aria-pressed="true"],[data-learning-lab="auto-digital-twin"] .dt-primary{border-color:var(--accent,#315f9d);background:var(--accent,#315f9d);color:var(--bg,#fff);font-weight:750}[data-learning-lab="auto-digital-twin"] button:focus-visible,[data-learning-lab="auto-digital-twin"] input:focus-visible{outline:3px solid var(--cl-focus,#1769aa);outline-offset:2px}',
    '[data-learning-lab="auto-digital-twin"] .dt-actions{display:flex;flex-wrap:wrap;gap:8px;margin:11px 0}[data-learning-lab="auto-digital-twin"] .dt-actions>*{flex:1 1 170px}[data-learning-lab="auto-digital-twin"] .dt-feedback{min-height:2em;font-weight:700}[data-learning-lab="auto-digital-twin"] .dt-correct{color:var(--dt-green)}[data-learning-lab="auto-digital-twin"] .dt-wrong{color:var(--dt-red)}',
    '[data-learning-lab="auto-digital-twin"] .dt-layout{display:grid;grid-template-columns:minmax(220px,.72fr) minmax(0,1.28fr);gap:16px;align-items:start}[data-learning-lab="auto-digital-twin"] .dt-controls{display:grid;gap:10px;padding:12px;border:1px solid var(--border,#cbd5e1);border-radius:7px}[data-learning-lab="auto-digital-twin"] .dt-control{display:grid;gap:5px;min-width:0}[data-learning-lab="auto-digital-twin"] .dt-control label{font-size:13px;font-weight:700;color:var(--fg-soft,currentColor)}[data-learning-lab="auto-digital-twin"] output{color:var(--accent,#315f9d);font-variant-numeric:tabular-nums}',
    '[data-learning-lab="auto-digital-twin"] input[type="range"]{width:100%;min-height:44px;margin:0;accent-color:var(--accent,#315f9d)}[data-learning-lab="auto-digital-twin"] .dt-stage-frame{padding:8px;border:1px solid var(--border,#cbd5e1);border-radius:7px;overflow:hidden}[data-learning-lab="auto-digital-twin"] svg{display:block;width:100%;height:auto;max-width:100%}[data-learning-lab="auto-digital-twin"] svg text{fill:currentColor;font-family:inherit;letter-spacing:0}',
    '[data-learning-lab="auto-digital-twin"] .dt-grid{stroke:var(--border,#cbd5e1);stroke-width:1;stroke-opacity:.7}[data-learning-lab="auto-digital-twin"] .dt-axis{stroke:currentColor;stroke-width:1.1;stroke-opacity:.75}[data-learning-lab="auto-digital-twin"] .dt-measured{stroke:var(--dt-blue);stroke-width:2.7;fill:none}[data-learning-lab="auto-digital-twin"] .dt-model{stroke:var(--dt-orange);stroke-width:2.3;stroke-dasharray:6 4;fill:none}[data-learning-lab="auto-digital-twin"] .dt-residual{stroke:var(--dt-green);stroke-width:2.4;fill:none}[data-learning-lab="auto-digital-twin"] .dt-threshold{stroke:var(--dt-red);stroke-width:1.5;stroke-dasharray:5 4}[data-learning-lab="auto-digital-twin"] .dt-drift{stroke:var(--dt-orange);stroke-width:1.4;stroke-dasharray:3 3}[data-learning-lab="auto-digital-twin"] .dt-label{font-size:11px}[data-learning-lab="auto-digital-twin"] .dt-small{font-size:10px;fill:var(--fg-soft,currentColor)!important}',
    '[data-learning-lab="auto-digital-twin"] .dt-metrics{display:grid;grid-template-columns:repeat(auto-fit,minmax(128px,1fr));gap:8px;margin:12px 0}[data-learning-lab="auto-digital-twin"] .dt-metric{min-width:0;padding:9px;border-top:2px solid var(--border,#cbd5e1)}[data-learning-lab="auto-digital-twin"] .dt-metric:nth-child(3n+1){border-color:var(--dt-blue)}[data-learning-lab="auto-digital-twin"] .dt-metric:nth-child(3n+2){border-color:var(--dt-orange)}[data-learning-lab="auto-digital-twin"] .dt-metric:nth-child(3n){border-color:var(--dt-green)}[data-learning-lab="auto-digital-twin"] .dt-metric span{display:block;font-size:11.5px;color:var(--fg-soft,currentColor)}[data-learning-lab="auto-digital-twin"] .dt-metric strong{display:block;margin-top:3px;font-size:15px;font-variant-numeric:tabular-nums;overflow-wrap:anywhere}',
    '[data-learning-lab="auto-digital-twin"] .dt-table{max-width:100%;overflow-x:auto}[data-learning-lab="auto-digital-twin"] table{width:100%;min-width:1100px;border-collapse:collapse;font-size:11.5px;font-variant-numeric:tabular-nums}[data-learning-lab="auto-digital-twin"] th,[data-learning-lab="auto-digital-twin"] td{padding:7px 6px;border-bottom:1px solid var(--border,#cbd5e1);text-align:left;vertical-align:top}[data-learning-lab="auto-digital-twin"] th{font-size:11px;color:var(--fg-soft,currentColor)}[data-learning-lab="auto-digital-twin"] .dt-evidence{margin-top:11px;padding:10px 12px;border-left:3px solid var(--dt-green);font-size:13px;line-height:1.7}',
    '@media(max-width:900px){[data-learning-lab="auto-digital-twin"] .dt-layout{grid-template-columns:minmax(0,1fr)}}@media(max-width:620px){[data-learning-lab="auto-digital-twin"] .dt-choices{grid-template-columns:minmax(0,1fr)}}@media(max-width:420px){[data-learning-lab="auto-digital-twin"] .dt-stage-frame{padding:4px}[data-learning-lab="auto-digital-twin"] table{font-size:10.8px}}@media(prefers-reduced-motion:reduce){[data-learning-lab="auto-digital-twin"] *{animation:none!important;transition:none!important}}'
  ].join("");
  function assert(condition, message) { if (!condition) throw new Error(message); }
  function finite(value, label) { var number = Number(value); if (!Number.isFinite(number)) throw new RangeError(label + " must be finite"); return number; }
  function nearly(left, right, tolerance) { var scale = Math.max(1, Math.abs(left), Math.abs(right)); return Math.abs(left - right) <= (tolerance || 1e-8) * scale; }
  function format(value, digits) { if (!Number.isFinite(value)) return "—"; var places = digits === undefined ? 3 : digits; if (Math.abs(value) > 0 && (Math.abs(value) < 0.001 || Math.abs(value) >= 10000)) return value.toExponential(Math.min(places, 4)); return value.toFixed(places).replace(/0+$/, "").replace(/\.$/, ""); }
  function normalize(input) { var source = input || {}; var config = { samples: Math.round(finite(source.samples === undefined ? DEFAULTS.samples : source.samples, "samples")), driftStart: Math.round(finite(source.driftStart === undefined ? DEFAULTS.driftStart : source.driftStart, "driftStart")), driftRate: finite(source.driftRate === undefined ? DEFAULTS.driftRate : source.driftRate, "driftRate"), syncEvery: Math.round(finite(source.syncEvery === undefined ? DEFAULTS.syncEvery : source.syncEvery, "syncEvery")), syncGain: finite(source.syncGain === undefined ? DEFAULTS.syncGain : source.syncGain, "syncGain"), threshold: finite(source.threshold === undefined ? DEFAULTS.threshold : source.threshold, "threshold"), persistence: Math.round(finite(source.persistence === undefined ? DEFAULTS.persistence : source.persistence, "persistence")), noiseAmp: finite(source.noiseAmp === undefined ? DEFAULTS.noiseAmp : source.noiseAmp, "noiseAmp"), gainMismatch: finite(source.gainMismatch === undefined ? DEFAULTS.gainMismatch : source.gainMismatch, "gainMismatch") }; if (config.samples < 12 || config.samples > 24) throw new RangeError("samples must be in [12, 24]"); if (config.driftStart < 3 || config.driftStart >= config.samples) throw new RangeError("driftStart must be before final sample"); if (config.driftRate < 0 || config.driftRate > 0.6) throw new RangeError("driftRate must be in [0, 0.6] C/sample"); if (config.syncEvery < 1 || config.syncEvery > 8) throw new RangeError("syncEvery must be in [1, 8]"); if (config.syncGain < 0 || config.syncGain > 0.8) throw new RangeError("syncGain must be in [0, 0.8]"); if (config.threshold < 0.1 || config.threshold > 2) throw new RangeError("threshold must be in [0.1, 2] C"); if (config.persistence < 1 || config.persistence > 5) throw new RangeError("persistence must be in [1, 5]"); if (config.noiseAmp < 0 || config.noiseAmp > 0.4) throw new RangeError("noise must be in [0, 0.4] C"); if (config.gainMismatch < -0.1 || config.gainMismatch > 0.1) throw new RangeError("gain mismatch must be in [-0.1, 0.1]"); return config; }
  function loadAt(index) { return 0.4 + 0.2 * (index % 4); }
  function run(input) { var config = normalize(input); var bias = 0; var streak = 0; var rows = []; var falseAlarms = 0; var firstDetection = -1; var missedAfterDrift = 0; var maxResidual = 0; for (var index = 0; index < config.samples; index += 1) { var load = loadAt(index); var prediction = 20 + 8 * load + bias; var drift = index >= config.driftStart ? config.driftRate * (index - config.driftStart + 1) : 0; var noise = config.noiseAmp * NOISE[index % NOISE.length]; var measurement = 20 + 8 * (1 + config.gainMismatch) * load + drift + noise; var residual = measurement - prediction; var above = Math.abs(residual) > config.threshold; streak = above ? streak + 1 : 0; var alarm = streak >= config.persistence; var sync = index > 0 && index % config.syncEvery === 0; var biasBefore = bias; if (sync) bias += config.syncGain * residual; if (alarm && index < config.driftStart) falseAlarms += 1; if (alarm && index >= config.driftStart && firstDetection < 0) firstDetection = index; if (!alarm && index >= config.driftStart) missedAfterDrift += 1; maxResidual = Math.max(maxResidual, Math.abs(residual)); rows.push({ index: index, load: load, measurement: measurement, prediction: prediction, biasBefore: biasBefore, biasAfter: bias, drift: drift, noise: noise, residual: residual, above: above, streak: streak, sync: sync, alarm: alarm, action: alarm ? "inspect/derate" : "monitor" }); } return { config: config, rows: rows, falseAlarms: falseAlarms, detectionDelay: firstDetection < 0 ? NaN : firstDetection - config.driftStart, missedAfterDrift: missedAfterDrift, maxResidual: maxResidual, firstDetection: firstDetection }; }
  function element(doc, tag, attrs, children) { var node = doc.createElement(tag); Object.keys(attrs || {}).forEach(function (key) { var value = attrs[key]; if (value === undefined || value === null || value === false) return; if (key === "className") node.setAttribute("class", String(value)); else if (key === "text") node.textContent = String(value); else if (value === true) node.setAttribute(key, ""); else node.setAttribute(key, String(value)); }); (Array.isArray(children) ? children : children === undefined ? [] : [children]).forEach(function (child) { if (child !== undefined && child !== null && child !== false) node.appendChild(child.nodeType ? child : doc.createTextNode(String(child))); }); return node; }
  function svgElement(doc, tag, attrs, children) { var node = doc.createElementNS(SVG_NS, tag); Object.keys(attrs || {}).forEach(function (key) { node.setAttribute(key, String(attrs[key])); }); (Array.isArray(children) ? children : children === undefined ? [] : [children]).forEach(function (child) { if (child !== undefined && child !== null && child !== false) node.appendChild(child.nodeType ? child : doc.createTextNode(String(child))); }); return node; }
  function clear(node) { while (node.firstChild) node.removeChild(node.firstChild); }
  function installStyles(doc) { if (doc.getElementById && doc.getElementById(STYLE_ID)) return; var style = doc.createElement("style"); style.id = STYLE_ID; style.textContent = CSS; (doc.head || doc.documentElement).appendChild(style); }
  function announce(api, root, message) { if (api && typeof api.announce === "function") api.announce(root, message); }
  function metric(doc, label, value) { return element(doc, "div", { className: "dt-metric" }, [element(doc, "span", { text: label }), element(doc, "strong", { text: value })]); }
  function linePath(rows, key, mapX, mapY) { return rows.map(function (row, index) { return (index ? "L" : "M") + mapX(index).toFixed(2) + " " + mapY(row[key]).toFixed(2); }).join(" "); }
  function draw(doc, svg, result) { clear(svg); var width = 720; var height = 420; var left = 54; var right = 18; var top = 28; var middle = 190; var bottom = 342; var x = function (index) { return left + index / Math.max(result.config.samples - 1, 1) * (width - left - right); }; var values = result.rows.reduce(function (list, row) { list.push(row.measurement, row.prediction); return list; }, []); var min = Math.min.apply(null, values) - 1; var max = Math.max.apply(null, values) + 1; var yTemp = function (value) { return top + (middle - top) * (max - value) / (max - min); }; var resMax = Math.max(result.config.threshold * 1.4, result.maxResidual * 1.15, 0.5); var yRes = function (value) { return middle + 26 + (bottom - (middle + 26)) * (resMax - value) / (2 * resMax); }; svg.appendChild(svgElement(doc, "title", {}, "数字孪生测量、模型、残差和报警阈值")); svg.appendChild(svgElement(doc, "desc", {}, "上图比较现场测量与带同步偏置的模型，下图显示残差、正负阈值和漂移开始位置。")); for (var grid = 0; grid <= 3; grid += 1) { var gy = top + grid / 3 * (middle - top); svg.appendChild(svgElement(doc, "line", { x1: left, y1: gy, x2: width - right, y2: gy, class: "dt-grid" })); var ry = middle + 26 + grid / 2 * (bottom - (middle + 26)); svg.appendChild(svgElement(doc, "line", { x1: left, y1: ry, x2: width - right, y2: ry, class: "dt-grid" })); } svg.appendChild(svgElement(doc, "line", { x1: left, y1: middle + 26, x2: width - right, y2: middle + 26, class: "dt-axis" })); svg.appendChild(svgElement(doc, "path", { d: linePath(result.rows, "measurement", x, yTemp), class: "dt-measured" })); svg.appendChild(svgElement(doc, "path", { d: linePath(result.rows, "prediction", x, yTemp), class: "dt-model" })); svg.appendChild(svgElement(doc, "line", { x1: x(result.config.driftStart), y1: top, x2: x(result.config.driftStart), y2: bottom, class: "dt-drift" })); svg.appendChild(svgElement(doc, "path", { d: linePath(result.rows, "residual", x, yRes), class: "dt-residual" })); svg.appendChild(svgElement(doc, "line", { x1: left, y1: yRes(result.config.threshold), x2: width - right, y2: yRes(result.config.threshold), class: "dt-threshold" })); svg.appendChild(svgElement(doc, "line", { x1: left, y1: yRes(-result.config.threshold), x2: width - right, y2: yRes(-result.config.threshold), class: "dt-threshold" })); svg.appendChild(svgElement(doc, "text", { x: left + 4, y: top + 13, class: "dt-label" }, "温度 y (°C)：测量蓝 / 孪生预测橙")); svg.appendChild(svgElement(doc, "text", { x: left + 4, y: middle + 18, class: "dt-label" }, "残差 r=y−ŷ (°C)：绿；红虚线为阈值")); svg.appendChild(svgElement(doc, "text", { x: x(result.config.driftStart) + 4, y: top + 28, class: "dt-small" }, "漂移起点")); svg.appendChild(svgElement(doc, "text", { x: width - right, y: height - 20, "text-anchor": "end", class: "dt-small" }, "采样 k；同步与报警是两种不同动作")); }
  function renderTable(doc, host, result) { var body = element(doc, "tbody"); result.rows.forEach(function (row) { body.appendChild(element(doc, "tr", {}, [element(doc, "th", { text: String(row.index) }), element(doc, "td", { text: format(row.load, 2) }), element(doc, "td", { text: format(row.measurement, 3) + " °C" }), element(doc, "td", { text: format(row.prediction, 3) + " °C" }), element(doc, "td", { text: format(row.residual, 3) + " °C" }), element(doc, "td", { text: row.sync ? "sync" : "—" }), element(doc, "td", { text: row.alarm ? "alarm" : "—" }), element(doc, "td", { text: row.action })])); }); clear(host); host.appendChild(element(doc, "table", {}, [element(doc, "caption", { text: "数字孪生同步/决策 evidence ledger" }), element(doc, "thead", {}, [element(doc, "tr", {}, ["k", "load", "y", "ŷ", "r (°C)", "sync", "alarm", "decision"].map(function (label) { return element(doc, "th", { text: label }); }))]), body])); }
  function questionSpecs() { return [
    { key: "residual", prompt: "数字孪生 residual 的方向定义？", expected: "difference", choices: [{ value: "difference", label: "r=测量−预测" }, { value: "score", label: "只给相似度分数" }, { value: "absolute", label: "永远取无符号" }] },
    { key: "threshold", prompt: "降低报警阈值通常带来什么？", expected: "tradeoff", choices: [{ value: "tradeoff", label: "灵敏度与误报权衡" }, { value: "free", label: "灵敏度免费提高" }, { value: "none", label: "完全没有影响" }] },
    { key: "sync", prompt: "高同步增益可能怎样影响慢漂移检测？", expected: "absorb", choices: [{ value: "absorb", label: "吸收漂移，可能延迟报警" }, { value: "instant", label: "必然立即报警" }, { value: "safe", label: "直接证明安全" }] }
  ]; }
  function mount(root, api) {
    if (!root || !root.ownerDocument) return; var doc = root.ownerDocument; installStyles(doc); var state = { config: normalize(DEFAULTS), predictions: {}, revealed: false, feedback: "" }; var refs = { questions: [], controls: {} }; var shell = element(doc, "div"); shell.appendChild(element(doc, "h3", { text: "数字孪生实验：同步、残差、误报与漂移检测" })); shell.appendChild(element(doc, "p", { className: "dt-note", text: "温度使用 °C，负载是无量纲实验输入；噪声和负载序列是确定性的。同步只更新模型偏置，报警/inspect/derate 是独立的决策动作。" }));
    var predictionHost = element(doc, "div"); questionSpecs().forEach(function (spec) { var fieldset = element(doc, "fieldset"); fieldset.appendChild(element(doc, "legend", { text: spec.prompt })); var grid = element(doc, "div", { className: "dt-choices" }); var question = { spec: spec, buttons: [] }; spec.choices.forEach(function (choice) { var button = element(doc, "button", { type: "button", text: choice.label, "aria-pressed": "false" }); button.addEventListener("click", function () { state.predictions[spec.key] = choice.value; state.feedback = ""; render(); }); question.buttons.push({ choice: choice, node: button }); grid.appendChild(button); }); fieldset.appendChild(grid); predictionHost.appendChild(fieldset); refs.questions.push(question); });
    var actions = element(doc, "div", { className: "dt-actions" }); var reveal = element(doc, "button", { type: "button", className: "dt-primary", text: "提交预测并揭晓" }); var reset = element(doc, "button", { type: "button", text: "重置" }); actions.appendChild(reveal); actions.appendChild(reset); var feedback = element(doc, "p", { className: "dt-feedback", "aria-live": "polite" }); var resultShell = element(doc, "div", { hidden: true }); var controls = element(doc, "div", { className: "dt-controls" });
    function range(key, label, min, max, step, digits, unit) { var input = element(doc, "input", { type: "range", min: min, max: max, step: step, value: state.config[key], "aria-label": label }); var output = element(doc, "output", { text: format(state.config[key], digits) + unit }); refs.controls[key] = { input: input, output: output, digits: digits, unit: unit }; input.addEventListener("input", function () { var next = {}; Object.keys(state.config).forEach(function (name) { next[name] = state.config[name]; }); next[key] = Number(input.value); if (key === "samples" && next.driftStart >= next.samples) next.driftStart = next.samples - 1; if (key === "samples") refs.controls.driftStart.input.max = next.samples - 1; state.config = normalize(next); state.feedback = ""; render(); }); return element(doc, "div", { className: "dt-control" }, [element(doc, "label", {}, [label + " = ", output]), input]); }
    controls.appendChild(range("samples", "采样数", 12, 24, 1, 0, " 点")); controls.appendChild(range("driftStart", "漂移起点", 3, state.config.samples - 1, 1, 0, " k")); controls.appendChild(range("driftRate", "漂移速率", 0, 0.6, 0.05, 2, " °C/sample")); controls.appendChild(range("syncEvery", "同步间隔", 1, 8, 1, 0, " sample")); controls.appendChild(range("syncGain", "同步增益 γ", 0, 0.8, 0.05, 2, "")); controls.appendChild(range("threshold", "报警阈值 h", 0.1, 2, 0.05, 2, " °C")); controls.appendChild(range("persistence", "连续确认", 1, 5, 1, 0, " sample")); controls.appendChild(range("noiseAmp", "噪声幅值", 0, 0.4, 0.05, 2, " °C")); controls.appendChild(range("gainMismatch", "真实增益失配", -0.1, 0.1, 0.005, 3, ""));
    var svg = svgElement(doc, "svg", { viewBox: "0 0 720 420", role: "img", "aria-label": "数字孪生测量与残差图" }); var metrics = element(doc, "div", { className: "dt-metrics" }); var table = element(doc, "div", { className: "dt-table" }); var evidence = element(doc, "div", { className: "dt-evidence" }); resultShell.appendChild(element(doc, "div", { className: "dt-layout" }, [controls, element(doc, "div", { className: "dt-stage" }, [element(doc, "div", { className: "dt-stage-frame" }, [svg]), metrics, table, evidence])])); shell.appendChild(predictionHost); shell.appendChild(actions); shell.appendChild(feedback); shell.appendChild(resultShell); clear(root); root.appendChild(shell);
    reveal.addEventListener("click", function () { var specs = questionSpecs(); if (!specs.every(function (spec) { return state.predictions[spec.key] !== undefined; })) { state.feedback = "请先完成三项预测；温度/残差图和同步决策 ledger 会在揭晓后显示。"; render(); return; } var correct = specs.filter(function (spec) { return state.predictions[spec.key] === spec.expected; }).length; state.revealed = true; state.feedback = "已揭晓：" + correct + "/" + specs.length + " 命中。调阈值或同步增益观察误报/检测延迟权衡。"; render(); announce(api, root, state.feedback); }); reset.addEventListener("click", function () { state = { config: normalize(DEFAULTS), predictions: {}, revealed: false, feedback: "" }; render(); announce(api, root, "数字孪生预测和同步/决策 ledger 已重置。"); });
    function render() { var result = run(state.config); Object.keys(refs.controls).forEach(function (key) { var ref = refs.controls[key]; ref.input.value = result.config[key]; ref.output.textContent = format(result.config[key], ref.digits) + ref.unit; }); refs.questions.forEach(function (question) { question.buttons.forEach(function (item) { var selected = state.predictions[question.spec.key] === item.choice.value; item.node.setAttribute("aria-pressed", selected ? "true" : "false"); item.node.className = state.revealed && item.choice.value === question.spec.expected ? "dt-correct" : state.revealed && selected ? "dt-wrong" : ""; item.node.textContent = state.revealed && item.choice.value === question.spec.expected ? "✓ " + item.choice.label : item.choice.label; }); }); feedback.textContent = state.feedback; feedback.className = "dt-feedback" + (state.feedback.indexOf("请先") === 0 ? " dt-wrong" : ""); resultShell.hidden = !state.revealed; if (!state.revealed) return; draw(doc, svg, result); clear(metrics); metrics.appendChild(metric(doc, "漂移前误报", String(result.falseAlarms) + " 次")); metrics.appendChild(metric(doc, "检测延迟", Number.isFinite(result.detectionDelay) ? String(result.detectionDelay) + " samples" : "未检测")); metrics.appendChild(metric(doc, "漂移后未报警", String(result.missedAfterDrift) + " 点")); metrics.appendChild(metric(doc, "最大残差", format(result.maxResidual, 3) + " °C")); metrics.appendChild(metric(doc, "同步动作数", String(result.rows.filter(function (row) { return row.sync; }).length))); metrics.appendChild(metric(doc, "决策动作", result.rows.some(function (row) { return row.alarm; }) ? "inspect/derate" : "monitor")); renderTable(doc, table, result); clear(evidence); evidence.appendChild(element(doc, "p", { text: "动作返回：sync 只更新模型偏置 b←b+γr；decision 在连续 m 个样本超过 |r|>h 后才变为 inspect/derate。当前 false alarm 统计漂移起点前的错误触发，detection delay 用 samples 计；阈值与同步增益的变化不会自动给出健康概率。" })); }
    render();
  }
  function selfTest() { var checks = 0; function check(condition, message) { checks += 1; assert(condition, message); } var base = run(DEFAULTS); var repeat = run(DEFAULTS); check(JSON.stringify(base) === JSON.stringify(repeat), "deterministic twin ledger"); check(nearly(loadAt(0), 0.4, 1e-12), "deterministic load sequence"); check(nearly(base.rows[0].residual, base.rows[0].measurement - base.rows[0].prediction, 1e-12), "residual definition"); check(base.rows.some(function (row) { return row.sync; }), "synchronization action appears"); check(base.rows.some(function (row) { return row.alarm; }), "default drift eventually alarms"); check(run({ samples: 16, driftStart: 8, driftRate: 0, syncEvery: 4, syncGain: 0.2, threshold: 0.8, persistence: 2, noiseAmp: 0, gainMismatch: 0 }).falseAlarms === 0, "no-drift high-threshold boundary"); check(run({ samples: 16, driftStart: 8, driftRate: 0.25, syncEvery: 4, syncGain: 0, threshold: 0.8, persistence: 1, noiseAmp: 0, gainMismatch: 0 }).detectionDelay <= run({ samples: 16, driftStart: 8, driftRate: 0.25, syncEvery: 4, syncGain: 0.2, threshold: 0.8, persistence: 1, noiseAmp: 0, gainMismatch: 0 }).detectionDelay, "sync gain can delay or preserve detection comparison"); var rejected = false; try { normalize({ persistence: 0 }); } catch (error) { rejected = true; } check(rejected, "invalid persistence rejected"); return { checks: checks }; }
  var exported = { DEFAULTS: DEFAULTS, normalize: normalize, loadAt: loadAt, run: run, mount: mount, selfTest: selfTest };
  if (typeof module === "object" && module.exports) module.exports = exported;
  if (typeof window !== "undefined" && window.CourseLearning && typeof window.CourseLearning.register === "function") window.CourseLearning.register(LAB_ID, exported.mount);
  if (typeof module === "object" && module.exports && typeof require === "function" && require.main === module) { try { var report = exported.selfTest(); console.log(LAB_ID + " self-test: PASS (" + report.checks + " checks)"); } catch (error) { console.error(LAB_ID + " self-test: FAIL\n" + error.stack); process.exitCode = 1; } }
})();
