(function () {
  "use strict";

  var LAB_ID = "auto-plc-scan";
  var SVG_NS = "http://www.w3.org/2000/svg";
  var STYLE_ID = LAB_ID + "-styles";
  var DEFAULTS = { scanMs: 20, inputBusMs: 1, logicMs: 5, outputBusMs: 1, jitterMs: 2, pulseStartMs: 8, pulseWidthMs: 5, scans: 8 };
  var CSS = [
    '[data-learning-lab="auto-plc-scan"]{--ps-blue:var(--cl-blue,#315f9d);--ps-orange:var(--cl-gold,#9b6a12);--ps-green:var(--cl-green,#39734d);--ps-red:var(--cl-red,#b64335);display:block;max-width:100%;color:var(--fg,currentColor);line-height:1.55;overflow-wrap:anywhere}',
    '[data-learning-lab="auto-plc-scan"] *{box-sizing:border-box}[data-learning-lab="auto-plc-scan"] [hidden]{display:none!important}[data-learning-lab="auto-plc-scan"] h3{margin:0;font-size:1.16rem;letter-spacing:0}[data-learning-lab="auto-plc-scan"] p{margin:8px 0}',
    '[data-learning-lab="auto-plc-scan"] .ps-note,[data-learning-lab="auto-plc-scan"] .ps-feedback{font-size:13px;color:var(--fg-soft,currentColor);line-height:1.7}[data-learning-lab="auto-plc-scan"] fieldset{min-width:0;margin:10px 0;padding:9px 10px;border:1px solid var(--border,#cbd5e1);border-radius:6px}[data-learning-lab="auto-plc-scan"] legend{max-width:100%;padding:0 4px;font-size:13px;font-weight:750;line-height:1.5}',
    '[data-learning-lab="auto-plc-scan"] .ps-choices{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:7px}[data-learning-lab="auto-plc-scan"] button,[data-learning-lab="auto-plc-scan"] input{font:inherit;letter-spacing:0}[data-learning-lab="auto-plc-scan"] button{min-width:0;min-height:44px;padding:8px 11px;border:1px solid var(--border,#cbd5e1);border-radius:6px;background:var(--bg,transparent);color:var(--fg,currentColor);line-height:1.35;cursor:pointer;overflow-wrap:anywhere}[data-learning-lab="auto-plc-scan"] button:hover{border-color:var(--accent,#315f9d)}[data-learning-lab="auto-plc-scan"] button[aria-pressed="true"],[data-learning-lab="auto-plc-scan"] .ps-primary{border-color:var(--accent,#315f9d);background:var(--accent,#315f9d);color:var(--bg,#fff);font-weight:750}[data-learning-lab="auto-plc-scan"] button:focus-visible,[data-learning-lab="auto-plc-scan"] input:focus-visible{outline:3px solid var(--cl-focus,#1769aa);outline-offset:2px}',
    '[data-learning-lab="auto-plc-scan"] .ps-actions{display:flex;flex-wrap:wrap;gap:8px;margin:11px 0}[data-learning-lab="auto-plc-scan"] .ps-actions>*{flex:1 1 170px}[data-learning-lab="auto-plc-scan"] .ps-feedback{min-height:2em;font-weight:700}[data-learning-lab="auto-plc-scan"] .ps-correct{color:var(--ps-green)}[data-learning-lab="auto-plc-scan"] .ps-wrong{color:var(--ps-red)}',
    '[data-learning-lab="auto-plc-scan"] .ps-layout{display:grid;grid-template-columns:minmax(220px,.72fr) minmax(0,1.28fr);gap:16px;align-items:start}[data-learning-lab="auto-plc-scan"] .ps-controls{display:grid;gap:10px;padding:12px;border:1px solid var(--border,#cbd5e1);border-radius:7px}[data-learning-lab="auto-plc-scan"] .ps-control{display:grid;gap:5px;min-width:0}[data-learning-lab="auto-plc-scan"] .ps-control label{font-size:13px;font-weight:700;color:var(--fg-soft,currentColor)}[data-learning-lab="auto-plc-scan"] output{color:var(--accent,#315f9d);font-variant-numeric:tabular-nums}',
    '[data-learning-lab="auto-plc-scan"] input[type="range"]{width:100%;min-height:44px;margin:0;accent-color:var(--accent,#315f9d)}[data-learning-lab="auto-plc-scan"] .ps-stage-frame{padding:8px;border:1px solid var(--border,#cbd5e1);border-radius:7px;overflow:hidden}[data-learning-lab="auto-plc-scan"] svg{display:block;width:100%;height:auto;max-width:100%}[data-learning-lab="auto-plc-scan"] svg text{fill:currentColor;font-family:inherit;letter-spacing:0}',
    '[data-learning-lab="auto-plc-scan"] .ps-grid{stroke:var(--border,#cbd5e1);stroke-width:1;stroke-opacity:.7}[data-learning-lab="auto-plc-scan"] .ps-axis{stroke:currentColor;stroke-width:1.1;stroke-opacity:.75}[data-learning-lab="auto-plc-scan"] .ps-pulse{fill:var(--ps-orange);fill-opacity:.35;stroke:var(--ps-orange);stroke-width:1.2}[data-learning-lab="auto-plc-scan"] .ps-sample{stroke:var(--ps-blue);stroke-width:1.8}[data-learning-lab="auto-plc-scan"] .ps-output{stroke:var(--ps-green);stroke-width:1.8}[data-learning-lab="auto-plc-scan"] .ps-miss{stroke:var(--ps-red);stroke-width:2;stroke-dasharray:4 4}[data-learning-lab="auto-plc-scan"] .ps-label{font-size:11px}[data-learning-lab="auto-plc-scan"] .ps-small{font-size:10px;fill:var(--fg-soft,currentColor)!important}',
    '[data-learning-lab="auto-plc-scan"] .ps-metrics{display:grid;grid-template-columns:repeat(auto-fit,minmax(128px,1fr));gap:8px;margin:12px 0}[data-learning-lab="auto-plc-scan"] .ps-metric{min-width:0;padding:9px;border-top:2px solid var(--border,#cbd5e1)}[data-learning-lab="auto-plc-scan"] .ps-metric:nth-child(3n+1){border-color:var(--ps-blue)}[data-learning-lab="auto-plc-scan"] .ps-metric:nth-child(3n+2){border-color:var(--ps-orange)}[data-learning-lab="auto-plc-scan"] .ps-metric:nth-child(3n){border-color:var(--ps-green)}[data-learning-lab="auto-plc-scan"] .ps-metric span{display:block;font-size:11.5px;color:var(--fg-soft,currentColor)}[data-learning-lab="auto-plc-scan"] .ps-metric strong{display:block;margin-top:3px;font-size:15px;font-variant-numeric:tabular-nums;overflow-wrap:anywhere}',
    '[data-learning-lab="auto-plc-scan"] .ps-table{max-width:100%;overflow-x:auto}[data-learning-lab="auto-plc-scan"] table{width:100%;min-width:900px;border-collapse:collapse;font-size:11.5px;font-variant-numeric:tabular-nums}[data-learning-lab="auto-plc-scan"] th,[data-learning-lab="auto-plc-scan"] td{padding:7px 6px;border-bottom:1px solid var(--border,#cbd5e1);text-align:left;vertical-align:top}[data-learning-lab="auto-plc-scan"] th{font-size:11px;color:var(--fg-soft,currentColor)}[data-learning-lab="auto-plc-scan"] .ps-evidence{margin-top:11px;padding:10px 12px;border-left:3px solid var(--ps-green);font-size:13px;line-height:1.7}',
    '@media(max-width:900px){[data-learning-lab="auto-plc-scan"] .ps-layout{grid-template-columns:minmax(0,1fr)}}@media(max-width:620px){[data-learning-lab="auto-plc-scan"] .ps-choices{grid-template-columns:minmax(0,1fr)}}@media(max-width:420px){[data-learning-lab="auto-plc-scan"] .ps-stage-frame{padding:4px}[data-learning-lab="auto-plc-scan"] table{font-size:10.8px}}@media(prefers-reduced-motion:reduce){[data-learning-lab="auto-plc-scan"] *{animation:none!important;transition:none!important}}'
  ].join("");
  function assert(condition, message) { if (!condition) throw new Error(message); }
  function finite(value, label) { var number = Number(value); if (!Number.isFinite(number)) throw new RangeError(label + " must be finite"); return number; }
  function nearly(left, right, tolerance) { var scale = Math.max(1, Math.abs(left), Math.abs(right)); return Math.abs(left - right) <= (tolerance || 1e-8) * scale; }
  function format(value, digits) { if (!Number.isFinite(value)) return "—"; var places = digits === undefined ? 3 : digits; if (Math.abs(value) > 0 && (Math.abs(value) < 0.001 || Math.abs(value) >= 10000)) return value.toExponential(Math.min(places, 4)); return value.toFixed(places).replace(/0+$/, "").replace(/\.$/, ""); }
  function normalize(input) {
    var source = input || {}; var config = { scanMs: finite(source.scanMs === undefined ? DEFAULTS.scanMs : source.scanMs, "scanMs"), inputBusMs: finite(source.inputBusMs === undefined ? DEFAULTS.inputBusMs : source.inputBusMs, "inputBusMs"), logicMs: finite(source.logicMs === undefined ? DEFAULTS.logicMs : source.logicMs, "logicMs"), outputBusMs: finite(source.outputBusMs === undefined ? DEFAULTS.outputBusMs : source.outputBusMs, "outputBusMs"), jitterMs: finite(source.jitterMs === undefined ? DEFAULTS.jitterMs : source.jitterMs, "jitterMs"), pulseStartMs: finite(source.pulseStartMs === undefined ? DEFAULTS.pulseStartMs : source.pulseStartMs, "pulseStartMs"), pulseWidthMs: finite(source.pulseWidthMs === undefined ? DEFAULTS.pulseWidthMs : source.pulseWidthMs, "pulseWidthMs"), scans: Math.round(finite(source.scans === undefined ? DEFAULTS.scans : source.scans, "scans")) };
    if (config.scanMs < 6 || config.scanMs > 60) throw new RangeError("scanMs must be in [6, 60] ms"); if (config.inputBusMs < 0.1 || config.inputBusMs > 8) throw new RangeError("input bus must be in [0.1, 8] ms"); if (config.logicMs < 0.5 || config.logicMs > 30) throw new RangeError("logic must be in [0.5, 30] ms"); if (config.outputBusMs < 0.1 || config.outputBusMs > 8) throw new RangeError("output bus must be in [0.1, 8] ms"); if (config.jitterMs < 0 || config.jitterMs > 8) throw new RangeError("jitter must be in [0, 8] ms"); if (config.pulseStartMs < 0 || config.pulseStartMs > 160) throw new RangeError("pulse start out of range"); if (config.pulseWidthMs < 0.5 || config.pulseWidthMs > 60) throw new RangeError("pulse width out of range"); if (config.scans < 4 || config.scans > 12) throw new RangeError("scans must be in [4, 12]");
    return config;
  }
  function run(input) {
    var config = normalize(input); var rows = []; var time = 0; var pathMs = config.inputBusMs + config.logicMs + config.outputBusMs; var pulseEnd = config.pulseStartMs + config.pulseWidthMs; var sampledCount = 0; var firstResponseLatency = NaN;
    for (var index = 0; index < config.scans; index += 1) {
      var period = Math.max(pathMs, config.scanMs + (index % 2 === 0 ? -config.jitterMs : config.jitterMs)); var inputTime = time + config.inputBusMs; var logicTime = inputTime + config.logicMs; var outputTime = logicTime + config.outputBusMs; var sampled = inputTime >= config.pulseStartMs && inputTime < pulseEnd; var outputValue = sampled ? 1 : 0;
      if (sampled) { sampledCount += 1; if (!Number.isFinite(firstResponseLatency)) firstResponseLatency = outputTime - config.pulseStartMs; }
      rows.push({ index: index, start: time, period: period, inputTime: inputTime, logicTime: logicTime, outputTime: outputTime, sampled: sampled, outputValue: outputValue }); time += period;
    }
    var intervals = rows.slice(0, -1).map(function (row) { return row.period; });
    var averagePeriod = intervals.length ? intervals.reduce(function (sum, value) { return sum + value; }, 0) / intervals.length : NaN;
    var minimumInterval = intervals.length ? Math.min.apply(null, intervals) : pathMs;
    var maximumInterval = intervals.length ? Math.max.apply(null, intervals) : pathMs;
    return { config: config, rows: rows, pulseEnd: pulseEnd, pathMs: pathMs, sampledCount: sampledCount, missed: sampledCount === 0, averagePeriod: averagePeriod, minimumInterval: minimumInterval, maximumInterval: maximumInterval, observedFirstResponse: firstResponseLatency, theoreticalWorstResponse: maximumInterval + pathMs };
  }
  function element(doc, tag, attrs, children) { var node = doc.createElement(tag); Object.keys(attrs || {}).forEach(function (key) { var value = attrs[key]; if (value === undefined || value === null || value === false) return; if (key === "className") node.setAttribute("class", String(value)); else if (key === "text") node.textContent = String(value); else if (value === true) node.setAttribute(key, ""); else node.setAttribute(key, String(value)); }); (Array.isArray(children) ? children : children === undefined ? [] : [children]).forEach(function (child) { if (child !== undefined && child !== null && child !== false) node.appendChild(child.nodeType ? child : doc.createTextNode(String(child))); }); return node; }
  function svgElement(doc, tag, attrs, children) { var node = doc.createElementNS(SVG_NS, tag); Object.keys(attrs || {}).forEach(function (key) { node.setAttribute(key, String(attrs[key])); }); (Array.isArray(children) ? children : children === undefined ? [] : [children]).forEach(function (child) { if (child !== undefined && child !== null && child !== false) node.appendChild(child.nodeType ? child : doc.createTextNode(String(child))); }); return node; }
  function clear(node) { while (node.firstChild) node.removeChild(node.firstChild); }
  function installStyles(doc) { if (doc.getElementById && doc.getElementById(STYLE_ID)) return; var style = doc.createElement("style"); style.id = STYLE_ID; style.textContent = CSS; (doc.head || doc.documentElement).appendChild(style); }
  function announce(api, root, message) { if (api && typeof api.announce === "function") api.announce(root, message); }
  function metric(doc, label, value) { return element(doc, "div", { className: "ps-metric" }, [element(doc, "span", { text: label }), element(doc, "strong", { text: value })]); }
  function draw(doc, svg, result) {
    clear(svg); var width = 720; var height = 400; var left = 52; var right = 18; var top = 34; var timeMax = result.rows[result.rows.length - 1].outputTime + 18; var x = function (time) { return left + time / timeMax * (width - left - right); }; var yPulse = 94; var yImage = 176; var yOutput = 258;
    svg.appendChild(svgElement(doc, "title", {}, "PLC 输入映像、逻辑和输出刷新时间线")); svg.appendChild(svgElement(doc, "desc", {}, "橙色为现场脉冲，蓝线为输入映像采样，绿线为输出刷新，红虚线标出未捕获脉冲。"));
    [yPulse, yImage, yOutput].forEach(function (y) { svg.appendChild(svgElement(doc, "line", { x1: left, y1: y, x2: width - right, y2: y, class: "ps-axis" })); });
    for (var tick = 0; tick <= 5; tick += 1) { var tx = left + tick / 5 * (width - left - right); svg.appendChild(svgElement(doc, "line", { x1: tx, y1: 28, x2: tx, y2: 306, class: "ps-grid" })); svg.appendChild(svgElement(doc, "text", { x: tx, y: 324, "text-anchor": "middle", class: "ps-small" }, format(timeMax * tick / 5, 0) + " ms")); }
    var pulseX = x(result.config.pulseStartMs); var pulseWidth = Math.max(1, x(result.pulseEnd) - pulseX); svg.appendChild(svgElement(doc, "rect", { x: pulseX, y: yPulse - 24, width: pulseWidth, height: 48, class: "ps-pulse" }));
    result.rows.forEach(function (row) { var sampleX = x(row.inputTime); var outputX = x(row.outputTime); svg.appendChild(svgElement(doc, "line", { x1: sampleX, y1: yImage - 27, x2: sampleX, y2: yImage + 27, class: row.sampled ? "ps-sample" : "ps-miss" })); svg.appendChild(svgElement(doc, "line", { x1: outputX, y1: yOutput - 27, x2: outputX, y2: yOutput + 27, class: "ps-output" })); });
    svg.appendChild(svgElement(doc, "text", { x: left + 4, y: yPulse - 32, class: "ps-label" }, "现场输入脉冲")); svg.appendChild(svgElement(doc, "text", { x: left + 4, y: yImage - 32, class: "ps-label" }, "输入 image 采样")); svg.appendChild(svgElement(doc, "text", { x: left + 4, y: yOutput - 32, class: "ps-label" }, "输出 image 刷新")); svg.appendChild(svgElement(doc, "text", { x: width - right, y: 372, "text-anchor": "end", class: "ps-small" }, result.missed ? "当前视窗：普通周期输入 miss" : "当前视窗：至少一次采样命中"));
  }
  function renderTable(doc, host, result) { var body = element(doc, "tbody"); result.rows.forEach(function (row) { body.appendChild(element(doc, "tr", {}, [element(doc, "th", { text: String(row.index) }), element(doc, "td", { text: format(row.start, 2) + " ms" }), element(doc, "td", { text: format(row.inputTime, 2) + " ms" }), element(doc, "td", { text: format(row.logicTime, 2) + " ms" }), element(doc, "td", { text: format(row.outputTime, 2) + " ms" }), element(doc, "td", { text: row.sampled ? "1" : "0" }), element(doc, "td", { text: row.outputValue ? "置位" : "未置位" })])); }); clear(host); host.appendChild(element(doc, "table", {}, [element(doc, "caption", { text: "PLC 扫描时间 ledger（ms）" }), element(doc, "thead", {}, [element(doc, "tr", {}, ["scan", "start", "input image", "logic end", "output update", "sample", "output"].map(function (label) { return element(doc, "th", { text: label }); }))]), body])); }
  function questionSpecs() { return [
    { key: "image", prompt: "扫描中途输入变化时，逻辑读取什么？", expected: "snapshot", choices: [{ value: "snapshot", label: "本次扫描的输入快照" }, { value: "live", label: "随时变化的现场值" }, { value: "output", label: "上次输出值" }] },
    { key: "pulse", prompt: "小于扫描周期的脉冲是否一定捕获？", expected: "miss", choices: [{ value: "miss", label: "不一定，可能 miss" }, { value: "always", label: "一定捕获" }, { value: "average", label: "只看平均延迟" }] },
    { key: "latency", prompt: "总线延迟/抖动应怎样进入联锁证据？", expected: "worst", choices: [{ value: "worst", label: "平均值与最坏值都记账" }, { value: "average", label: "只记录平均值" }, { value: "ignore", label: "可以忽略" }] }
  ]; }
  function mount(root, api) {
    if (!root || !root.ownerDocument) return; var doc = root.ownerDocument; installStyles(doc); var state = { config: normalize(DEFAULTS), predictions: {}, revealed: false, feedback: "" }; var refs = { questions: [], controls: {} }; var shell = element(doc, "div"); shell.appendChild(element(doc, "h3", { text: "PLC 扫描实验：输入映像、逻辑完成、输出刷新与事件 miss" })); shell.appendChild(element(doc, "p", { className: "ps-note", text: "时间单位均为 ms。该 ledger 模拟固定扫描和确定性交替抖动；异步 I/O、高速锁存和中断输入属于不同架构。" }));
    var predictionHost = element(doc, "div"); questionSpecs().forEach(function (spec) { var fieldset = element(doc, "fieldset"); fieldset.appendChild(element(doc, "legend", { text: spec.prompt })); var grid = element(doc, "div", { className: "ps-choices" }); var question = { spec: spec, buttons: [] }; spec.choices.forEach(function (choice) { var button = element(doc, "button", { type: "button", text: choice.label, "aria-pressed": "false" }); button.addEventListener("click", function () { state.predictions[spec.key] = choice.value; state.feedback = ""; render(); }); question.buttons.push({ choice: choice, node: button }); grid.appendChild(button); }); fieldset.appendChild(grid); predictionHost.appendChild(fieldset); refs.questions.push(question); });
    var actions = element(doc, "div", { className: "ps-actions" }); var reveal = element(doc, "button", { type: "button", className: "ps-primary", text: "提交预测并揭晓" }); var reset = element(doc, "button", { type: "button", text: "重置" }); actions.appendChild(reveal); actions.appendChild(reset); var feedback = element(doc, "p", { className: "ps-feedback", "aria-live": "polite" }); var resultShell = element(doc, "div", { hidden: true }); var controls = element(doc, "div", { className: "ps-controls" });
    function range(key, label, min, max, step, digits, unit) { var input = element(doc, "input", { type: "range", min: min, max: max, step: step, value: state.config[key], "aria-label": label }); var output = element(doc, "output", { text: format(state.config[key], digits) + unit }); refs.controls[key] = { input: input, output: output, digits: digits, unit: unit }; input.addEventListener("input", function () { var next = {}; Object.keys(state.config).forEach(function (name) { next[name] = state.config[name]; }); next[key] = Number(input.value); state.config = normalize(next); state.feedback = ""; render(); }); return element(doc, "div", { className: "ps-control" }, [element(doc, "label", {}, [label + " = ", output]), input]); }
    controls.appendChild(range("scanMs", "名义扫描周期", 6, 60, 1, 0, " ms")); controls.appendChild(range("inputBusMs", "输入总线", 0.1, 8, 0.1, 1, " ms")); controls.appendChild(range("logicMs", "逻辑执行", 0.5, 30, 0.5, 1, " ms")); controls.appendChild(range("outputBusMs", "输出总线", 0.1, 8, 0.1, 1, " ms")); controls.appendChild(range("jitterMs", "周期抖动幅值", 0, 8, 0.5, 1, " ms")); controls.appendChild(range("pulseStartMs", "脉冲起点", 0, 160, 1, 0, " ms")); controls.appendChild(range("pulseWidthMs", "脉冲宽度", 0.5, 60, 0.5, 1, " ms")); controls.appendChild(range("scans", "扫描行数", 4, 12, 1, 0, " 行"));
    var svg = svgElement(doc, "svg", { viewBox: "0 0 720 400", role: "img", "aria-label": "PLC 扫描时间线" }); var metrics = element(doc, "div", { className: "ps-metrics" }); var table = element(doc, "div", { className: "ps-table" }); var evidence = element(doc, "div", { className: "ps-evidence" }); resultShell.appendChild(element(doc, "div", { className: "ps-layout" }, [controls, element(doc, "div", { className: "ps-stage" }, [element(doc, "div", { className: "ps-stage-frame" }, [svg]), metrics, table, evidence])])); shell.appendChild(predictionHost); shell.appendChild(actions); shell.appendChild(feedback); shell.appendChild(resultShell); clear(root); root.appendChild(shell);
    reveal.addEventListener("click", function () { var specs = questionSpecs(); if (!specs.every(function (spec) { return state.predictions[spec.key] !== undefined; })) { state.feedback = "请先完成三项预测；时间线和扫描 ledger 会在揭晓后显示。"; render(); return; } var correct = specs.filter(function (spec) { return state.predictions[spec.key] === spec.expected; }).length; state.revealed = true; state.feedback = "已揭晓：" + correct + "/" + specs.length + " 命中。改变脉冲相位可直接观察 miss。"; render(); announce(api, root, state.feedback); }); reset.addEventListener("click", function () { state = { config: normalize(DEFAULTS), predictions: {}, revealed: false, feedback: "" }; render(); announce(api, root, "PLC 扫描预测和时间 ledger 已重置。"); });
    function render() { var result = run(state.config); Object.keys(refs.controls).forEach(function (key) { var ref = refs.controls[key]; ref.input.value = result.config[key]; ref.output.textContent = format(result.config[key], ref.digits) + ref.unit; }); refs.questions.forEach(function (question) { question.buttons.forEach(function (item) { var selected = state.predictions[question.spec.key] === item.choice.value; item.node.setAttribute("aria-pressed", selected ? "true" : "false"); item.node.className = state.revealed && item.choice.value === question.spec.expected ? "ps-correct" : state.revealed && selected ? "ps-wrong" : ""; item.node.textContent = state.revealed && item.choice.value === question.spec.expected ? "✓ " + item.choice.label : item.choice.label; }); }); feedback.textContent = state.feedback; feedback.className = "ps-feedback" + (state.feedback.indexOf("请先") === 0 ? " ps-wrong" : ""); resultShell.hidden = !state.revealed; if (!state.revealed) return; draw(doc, svg, result); clear(metrics); metrics.appendChild(metric(doc, "捕获次数", String(result.sampledCount) + " 次")); metrics.appendChild(metric(doc, "脉冲状态", result.missed ? "miss" : "captured")); metrics.appendChild(metric(doc, "相邻周期范围", format(result.minimumInterval, 2) + "–" + format(result.maximumInterval, 2) + " ms")); metrics.appendChild(metric(doc, "真实相邻平均周期", format(result.averagePeriod, 2) + " ms")); metrics.appendChild(metric(doc, "首次输出响应", Number.isFinite(result.observedFirstResponse) ? format(result.observedFirstResponse, 2) + " ms" : "无命中")); metrics.appendChild(metric(doc, "理论最坏响应界", format(result.theoreticalWorstResponse, 2) + " ms")); renderTable(doc, table, result); clear(evidence); evidence.appendChild(element(doc, "p", { text: "时序返回：输入 image 在 t_in 冻结，逻辑只读快照，输出在 t_out 才更新。当前脉冲区间为 [" + format(result.config.pulseStartMs, 1) + ", " + format(result.pulseEnd, 1) + ") ms；普通周期输入的 miss 不能靠平均延迟补救。相邻扫描间隔用于周期统计；首次输出响应只记录事件第一次被采样后的输出时刻，理论最坏界则保守地用最大相邻间隔加输入/逻辑/输出路径计算。" })); }
    render();
  }
  function selfTest() { var checks = 0; function check(condition, message) { checks += 1; assert(condition, message); } var base = run(DEFAULTS); var repeat = run(DEFAULTS); check(JSON.stringify(base) === JSON.stringify(repeat), "deterministic scan ledger"); check(nearly(base.rows[0].inputTime, 1, 1e-12), "input bus timestamp"); check(nearly(base.rows[0].outputTime, 7, 1e-12), "logic and output timestamp"); check(base.missed, "default short pulse can miss"); check(run({ scanMs: 20, inputBusMs: 1, logicMs: 5, outputBusMs: 1, jitterMs: 0, pulseStartMs: 0, pulseWidthMs: 30, scans: 8 }).sampledCount > 0, "wide pulse captured"); var noJitter = run({ scanMs: 20, inputBusMs: 1, logicMs: 5, outputBusMs: 1, jitterMs: 0, pulseStartMs: 0, pulseWidthMs: 60, scans: 8 }); var jitter = run({ scanMs: 20, inputBusMs: 1, logicMs: 5, outputBusMs: 1, jitterMs: 2, pulseStartMs: 0, pulseWidthMs: 60, scans: 8 }); check(!nearly(jitter.rows[1].inputTime, noJitter.rows[1].inputTime, 1e-12), "jitter changes sample timestamp"); check(nearly(noJitter.averagePeriod, 20, 1e-12), "average uses adjacent scan intervals"); check(nearly(noJitter.observedFirstResponse, 7, 1e-12), "sustained pulse reports first response rather than later repeated samples"); check(jitter.theoreticalWorstResponse >= jitter.observedFirstResponse, "theoretical response bound covers first observed response"); var rejected = false; try { normalize({ pulseWidthMs: 0 }); } catch (error) { rejected = true; } check(rejected, "invalid pulse width rejected"); return { checks: checks }; }
  var exported = { DEFAULTS: DEFAULTS, normalize: normalize, run: run, mount: mount, selfTest: selfTest };
  if (typeof module === "object" && module.exports) module.exports = exported;
  if (typeof window !== "undefined" && window.CourseLearning && typeof window.CourseLearning.register === "function") window.CourseLearning.register(LAB_ID, exported.mount);
  if (typeof module === "object" && module.exports && typeof require === "function" && require.main === module) { try { var report = exported.selfTest(); console.log(LAB_ID + " self-test: PASS (" + report.checks + " checks)"); } catch (error) { console.error(LAB_ID + " self-test: FAIL\n" + error.stack); process.exitCode = 1; } }
})();
