(function (root, factory) {
  "use strict";

  var exported = factory(root);
  if (typeof module === "object" && module.exports) module.exports = exported;
  if (root && root.CourseLearning && typeof root.CourseLearning.register === "function") {
    root.CourseLearning.register("photo-fourier-aperture", exported.mount);
  }
  if (typeof module === "object" && module.exports && typeof require === "function" && require.main === module) {
    try {
      var report = exported.selfTest();
      console.log("photo-fourier-aperture self-test: PASS (" + report.checks + " checks)");
    } catch (error) {
      console.error("photo-fourier-aperture self-test: FAIL\n" + error.stack);
      process.exitCode = 1;
    }
  }
})(typeof window !== "undefined" ? window : typeof globalThis !== "undefined" ? globalThis : this, function (host) {
  "use strict";

  var STYLE_ID = "photo-fourier-aperture-styles";
  var SVG_NS = "http://www.w3.org/2000/svg";
  var PI = Math.PI;
  var EPS = 1e-9;
  var FREQUENCIES = [0.4, 1.2, 2.4, 3.6];

  var STYLE_TEXT = [
    ".pfa-lab{--pfa-blue:var(--cl-blue,#315f9d);--pfa-green:var(--cl-green,#39734d);--pfa-gold:var(--cl-gold,#9b6a12);--pfa-red:var(--cl-red,#b64335);max-width:100%;min-width:0;color:var(--fg);line-height:1.55;overflow-wrap:anywhere}",
    ".pfa-lab *,.pfa-lab *::before,.pfa-lab *::after{box-sizing:border-box}.pfa-lab [hidden]{display:none!important}.pfa-lab h3,.pfa-lab h4{margin:0;letter-spacing:0}.pfa-lab h3{font-size:1.15rem}.pfa-lab p{margin:.65em 0}.pfa-lab button,.pfa-lab input,.pfa-lab select{font:inherit;letter-spacing:0}.pfa-lab button,.pfa-lab select{min-width:0;min-height:44px;padding:8px 11px;border:1px solid var(--border);border-radius:6px;background:var(--bg);color:var(--fg);cursor:pointer;line-height:1.35;overflow-wrap:anywhere}.pfa-lab button:hover{border-color:var(--pfa-blue)}.pfa-lab button:focus-visible,.pfa-lab input:focus-visible,.pfa-lab select:focus-visible{outline:3px solid var(--cl-focus,#1769aa);outline-offset:2px}.pfa-lab button[aria-pressed=true],.pfa-lab .pfa-primary{border-color:var(--pfa-blue);background:var(--pfa-blue);color:var(--bg);font-weight:750}.pfa-lab .pfa-note,.pfa-lab .pfa-feedback{color:var(--fg-soft);font-size:13px;line-height:1.65}.pfa-lab .pfa-prediction{margin:14px 0;padding:12px 14px;border-left:3px solid var(--pfa-gold);background:var(--block-bg,var(--bg))}.pfa-lab .pfa-question{margin:0 0 12px;padding:0;border:0}.pfa-lab .pfa-question:last-of-type{margin-bottom:0}.pfa-lab .pfa-question legend{max-width:100%;margin-bottom:7px;font-size:13px;font-weight:750;line-height:1.5}.pfa-lab .pfa-choices{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:7px}.pfa-lab .pfa-choices button{font-size:12px}.pfa-lab .pfa-actions{display:flex;flex-wrap:wrap;gap:8px;margin-top:12px}.pfa-lab .pfa-actions>*{flex:1 1 170px}.pfa-lab .pfa-feedback{min-height:2em;margin:8px 0 0;font-weight:700}.pfa-lab .pfa-pass{color:var(--pfa-green)}.pfa-lab .pfa-warn{color:var(--pfa-red)}.pfa-lab .pfa-revealed{margin-top:18px;padding-top:16px;border-top:1px solid var(--border)}.pfa-lab .pfa-controls{display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:10px;margin:12px 0;align-items:end}.pfa-lab .pfa-control{display:grid;gap:5px;min-width:0}.pfa-lab .pfa-control label{color:var(--fg-soft);font-size:12.5px;font-weight:700}.pfa-lab .pfa-control output{color:var(--pfa-blue);font-variant-numeric:tabular-nums}.pfa-lab input[type=range]{display:block;width:100%;min-height:44px;margin:0;accent-color:var(--pfa-blue)}.pfa-lab .pfa-scale{display:flex;justify-content:space-between;gap:8px;color:var(--fg-soft);font-size:11px}.pfa-lab .pfa-stage{min-width:0;padding:8px;border:1px solid var(--border);border-radius:7px;background:var(--bg);overflow:hidden}.pfa-lab .pfa-stage-title{display:flex;flex-wrap:wrap;justify-content:space-between;gap:8px;margin-bottom:8px;color:var(--fg-soft);font-size:13px}.pfa-lab svg{display:block;width:100%;height:auto;max-width:100%;color:var(--fg)}.pfa-lab svg text{fill:currentColor;font-family:inherit;letter-spacing:0}.pfa-lab .pfa-grid{stroke:currentColor;stroke-width:1;stroke-opacity:.14}.pfa-lab .pfa-axis{stroke:currentColor;stroke-width:1.1;stroke-opacity:.62}.pfa-lab .pfa-passbar{fill:var(--pfa-blue);fill-opacity:.75}.pfa-lab .pfa-blockbar{fill:var(--pfa-red);fill-opacity:.7}.pfa-lab .pfa-pupil{fill:var(--pfa-blue);fill-opacity:.18;stroke:var(--pfa-blue);stroke-width:2}.pfa-lab .pfa-psf{fill:none;stroke:var(--pfa-gold);stroke-width:2.5}.pfa-lab .pfa-points{fill:none;stroke:var(--pfa-green);stroke-width:2.5}.pfa-lab .pfa-marker{stroke:var(--pfa-red);stroke-width:2;stroke-dasharray:5 4}.pfa-lab .pfa-label{font-size:11px;fill:var(--fg-soft)}.pfa-lab .pfa-metrics{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:8px;margin-top:12px}.pfa-lab .pfa-metric{min-width:0;padding:9px;border-top:2px solid var(--border);background:var(--bg)}.pfa-lab .pfa-metric:nth-child(4n+1){border-color:var(--pfa-blue)}.pfa-lab .pfa-metric:nth-child(4n+2){border-color:var(--pfa-gold)}.pfa-lab .pfa-metric:nth-child(4n+3){border-color:var(--pfa-red)}.pfa-lab .pfa-metric:nth-child(4n){border-color:var(--pfa-green)}.pfa-lab .pfa-metric span{display:block;color:var(--fg-soft);font-size:11px}.pfa-lab .pfa-metric strong{display:block;margin-top:3px;font-size:14px;font-variant-numeric:tabular-nums;overflow-wrap:anywhere}.pfa-lab .pfa-formula{margin:12px 0 0;padding:9px 11px;border-left:3px solid var(--pfa-blue);background:var(--bg);font-family:SFMono-Regular,Menlo,Consolas,monospace;font-size:12px;line-height:1.65;overflow-x:auto}.pfa-lab .pfa-reset{margin-top:10px;color:var(--fg-soft)}@media(max-width:900px){.pfa-lab .pfa-controls{grid-template-columns:repeat(3,minmax(0,1fr))}.pfa-lab .pfa-metrics{grid-template-columns:repeat(2,minmax(0,1fr))}}@media(max-width:620px){.pfa-lab .pfa-choices{grid-template-columns:minmax(0,1fr)}.pfa-lab .pfa-controls{grid-template-columns:repeat(2,minmax(0,1fr))}}@media(max-width:400px){.pfa-lab .pfa-controls,.pfa-lab .pfa-metrics{grid-template-columns:minmax(0,1fr)}.pfa-lab .pfa-actions>*{flex-basis:100%}}@media(prefers-reduced-motion:reduce){.pfa-lab *{animation:none!important;transition:none!important;scroll-behavior:auto!important}}"
  ].join("\n");

  function finite(value, fallback) { return typeof value === "number" && Number.isFinite(value) ? value : fallback; }
  function clamp(value, minimum, maximum) { return Math.max(minimum, Math.min(maximum, value)); }
  function normalize(value, fallback, minimum, maximum) { return clamp(finite(Number(value), fallback), minimum, maximum); }
  function near(left, right, tolerance) { return Math.abs(left - right) <= (tolerance || EPS) * Math.max(1, Math.abs(left), Math.abs(right)); }
  function sinc(value) { if (Math.abs(value) < 1e-6) return 1 - value * value / 6; return Math.sin(value) / value; }
  function coherentCutoff(lambda, na) { return na / lambda; }
  function incoherentCutoff(lambda, na) { return 2 * na / lambda; }
  function cutoff(lambda, na) { return incoherentCutoff(lambda, na); }
  function resolution(lambda, na) { return 0.61 * lambda / na; }
  function transmission(frequency, source) {
    if (source.filter === "stop" && frequency < source.stop) return 0;
    if (source.filter === "low" && frequency > source.stop) return 0;
    if (frequency > cutoff(source.lambda, source.na)) return 0;
    var apertureEnvelope = source.pattern === "slit" ? Math.pow(sinc(PI * source.aperture * frequency), 2) : Math.max(0, 1 - Math.pow(frequency / Math.max(cutoff(source.lambda, source.na), 1e-9), 2));
    return clamp(apertureEnvelope, 0, 1);
  }

  function evaluate(input) {
    var source = input || {};
    var pattern = source.pattern === "circle" ? "circle" : "slit";
    var lambda = normalize(source.lambda, 0.5, 0.4, 0.7);
    var na = normalize(source.na, 0.5, 0.2, 0.9);
    var aperture = normalize(source.aperture, 1, 0.5, 2);
    var stop = normalize(source.stop, 1, 0, 3);
    var separation = normalize(source.separation, 0.7, 0.2, 1.4);
    var filter = source.filter === "stop" || source.filter === "low" ? source.filter : "none";
    var params = { pattern: pattern, lambda: lambda, na: na, aperture: aperture, stop: stop, separation: separation, filter: filter };
    var values = FREQUENCIES.map(function (frequency) { return { frequency: frequency, value: transmission(frequency, params), passed: transmission(frequency, params) > EPS }; });
    var res = resolution(lambda, na);
    return { pattern: pattern, lambda: lambda, na: na, aperture: aperture, stop: stop, separation: separation, filter: filter, cutoff: cutoff(lambda, na), coherentCutoff: coherentCutoff(lambda, na), incoherentCutoff: incoherentCutoff(lambda, na), resolution: res, values: values, resolved: separation >= res, lowPass: filter === "low", highPass: filter === "stop" };
  }

  function setAttributes(node, attrs) { Object.keys(attrs || {}).forEach(function (key) { var value = attrs[key]; if (value === undefined || value === null || value === false) return; if (key === "className") node.setAttribute("class", value); else if (key === "text") node.textContent = String(value); else if (key.slice(0, 2) === "on" && typeof value === "function") node.addEventListener(key.slice(2).toLowerCase(), value); else if (value === true) node.setAttribute(key, ""); else node.setAttribute(key, String(value)); }); return node; }
  function appendChildren(node, children) { (Array.isArray(children) ? children : [children]).forEach(function (child) { if (child !== undefined && child !== null && child !== false) node.appendChild(child && child.nodeType ? child : node.ownerDocument.createTextNode(String(child))); }); return node; }
  function element(doc, tag, attrs, children) { return appendChildren(setAttributes(doc.createElement(tag), attrs), children); }
  function svgElement(doc, tag, attrs, children) { return appendChildren(setAttributes(doc.createElementNS(SVG_NS, tag), attrs), children); }
  function clear(node) { while (node.firstChild) node.removeChild(node.firstChild); }
  function format(value, digits) { if (!Number.isFinite(value)) return "—"; var places = digits === undefined ? 3 : digits; var text = Math.abs(value) < 0.001 && value !== 0 ? value.toExponential(Math.min(places, 4)) : value.toFixed(places); return text.indexOf(".") === -1 ? text : text.replace(/0+$/, "").replace(/\.$/, ""); }
  function installStyles(doc) { if (!doc || !doc.getElementById || doc.getElementById(STYLE_ID)) return; var style = doc.createElement("style"); style.id = STYLE_ID; style.textContent = STYLE_TEXT; (doc.head || doc.documentElement).appendChild(style); }
  function announce(api, root, message) { if (api && typeof api.announce === "function") api.announce(root, message); }
  function metric(doc, label) { var value = element(doc, "strong", { text: "—" }); return { node: element(doc, "div", { className: "pfa-metric" }, [element(doc, "span", { text: label }), value]), value: value }; }

  function mount(root, api) {
    if (!root || !root.ownerDocument) return;
    var doc = root.ownerDocument; installStyles(doc); root.classList.add("pfa-lab"); clear(root);
    var state = { pattern: "slit", lambda: 0.5, na: 0.5, aperture: 1, stop: 1, separation: 0.7, filter: "none", predictions: [null, null, null] };
    var questions = [
      { prompt: "矩形孔径宽度减半，第一零点频率？", options: ["减半", "加倍", "不变"], answer: 1 },
      { prompt: "后焦面中心挡块主要保留？", options: ["低频底座", "高频/边缘", "所有频率"], answer: 1 },
      { prompt: "提高 NA 对 d_min 的作用？", options: ["变大", "变小", "不改变"], answer: 1 }
    ];
    root.appendChild(element(doc, "h3", { text: "预测闸门：先猜哪一条频率会消失" })); root.appendChild(element(doc, "p", { className: "pfa-note", text: "先判断孔径缩放、中心挡块和 NA；揭示后可在频谱面与像面之间来回核对。" })); var prediction = element(doc, "div", { className: "pfa-prediction" }); var choiceButtons = [];
    questions.forEach(function (question, questionIndex) { var fieldset = element(doc, "fieldset", { className: "pfa-question" }); fieldset.appendChild(element(doc, "legend", { text: (questionIndex + 1) + ". " + question.prompt })); var choices = element(doc, "div", { className: "pfa-choices" }); question.options.forEach(function (label, optionIndex) { var button = element(doc, "button", { type: "button", text: label, "aria-pressed": "false", onclick: function () { state.predictions[questionIndex] = optionIndex; choiceButtons[questionIndex].forEach(function (item) { item.setAttribute("aria-pressed", "false"); }); button.setAttribute("aria-pressed", "true"); } }); choices.appendChild(button); if (!choiceButtons[questionIndex]) choiceButtons[questionIndex] = []; choiceButtons[questionIndex].push(button); }); fieldset.appendChild(choices); prediction.appendChild(fieldset); }); var feedback = element(doc, "p", { className: "pfa-feedback", "aria-live": "polite" }); var actions = element(doc, "div", { className: "pfa-actions" }); var reveal = element(doc, "button", { type: "button", className: "pfa-primary", text: "揭示频谱" }); var clearPredictions = element(doc, "button", { type: "button", text: "清空预测" }); actions.appendChild(reveal); actions.appendChild(clearPredictions); prediction.appendChild(actions); prediction.appendChild(feedback); root.appendChild(prediction);
    var revealed = element(doc, "div", { className: "pfa-revealed", hidden: true }); revealed.appendChild(element(doc, "h4", { text: "孔径、频谱通带与点扩散" })); var controls = element(doc, "div", { className: "pfa-controls" });
    var patternSelect = element(doc, "select", { "aria-label": "选择孔径模型" }); patternSelect.appendChild(element(doc, "option", { value: "slit", text: "矩形单缝" })); patternSelect.appendChild(element(doc, "option", { value: "circle", text: "圆孔近似" })); patternSelect.value = state.pattern; patternSelect.addEventListener("change", function () { state.pattern = patternSelect.value; render(); }); controls.appendChild(element(doc, "div", { className: "pfa-control" }, [element(doc, "label", { text: "孔径模型" }), patternSelect]));
    var filterSelect = element(doc, "select", { "aria-label": "选择频谱滤波器" }); [{ value: "none", text: "无额外滤波" }, { value: "stop", text: "中心挡块（高通）" }, { value: "low", text: "外圈挡板（低通）" }].forEach(function (item) { filterSelect.appendChild(element(doc, "option", { value: item.value, text: item.text })); }); filterSelect.value = state.filter; filterSelect.addEventListener("change", function () { state.filter = filterSelect.value; render(); }); controls.appendChild(element(doc, "div", { className: "pfa-control" }, [element(doc, "label", { text: "频谱滤波" }), filterSelect]));
    function addRange(label, key, min, max, step, digits, suffix) { var output = element(doc, "output", { text: format(state[key], digits) + suffix }); var input = element(doc, "input", { type: "range", min: min, max: max, step: step, value: state[key], "aria-label": label }); input.addEventListener("input", function () { state[key] = Number(input.value); output.textContent = format(state[key], digits) + suffix; render(); }); controls.appendChild(element(doc, "div", { className: "pfa-control" }, [element(doc, "label", { text: label }), output, input, element(doc, "div", { className: "pfa-scale" }, [element(doc, "span", { text: String(min) + suffix }), element(doc, "span", { text: String(max) + suffix })])])); }
    addRange("波长 λ", "lambda", 0.4, 0.7, 0.01, 2, " μm"); addRange("NA", "na", 0.2, 0.9, 0.01, 2, ""); addRange("缝宽 a", "aperture", 0.5, 2, 0.05, 2, " μm"); addRange("挡块频率", "stop", 0, 3, 0.05, 2, " cyc/μm"); addRange("双点间距", "separation", 0.2, 1.4, 0.02, 2, " μm"); revealed.appendChild(controls);
    var stage = element(doc, "div", { className: "pfa-stage" }); var stageTitle = element(doc, "div", { className: "pfa-stage-title" }, [element(doc, "span", { text: "上：非相干强度/MTF proxy 的离散频率通过率；下：PSF 与双点" }), element(doc, "span", { className: "pfa-stage-status", text: "" })]); var chart = svgElement(doc, "svg", { viewBox: "0 0 650 360", role: "img", "aria-label": "傅里叶孔径的频谱通过率和点扩散函数图" }); stage.appendChild(stageTitle); stage.appendChild(chart); revealed.appendChild(stage);
    var metrics = element(doc, "div", { className: "pfa-metrics" }); var metricNodes = [metric(doc, "非相干截止频率"), metric(doc, "d_min / μm"), metric(doc, "非零通过频率数"), metric(doc, "双点判定")]; metricNodes.forEach(function (item) { metrics.appendChild(item.node); }); revealed.appendChild(metrics); var formula = element(doc, "div", { className: "pfa-formula", text: "f_c,inc = 2NA/λ；f_c,coh = NA/λ；d_min = 0.61λ/NA" }); revealed.appendChild(formula); var reset = element(doc, "button", { type: "button", className: "pfa-reset", text: "重置实验" }); revealed.appendChild(reset); root.appendChild(revealed);

    function render() {
      var result = evaluate(state); metricNodes[0].value.textContent = format(result.cutoff, 2); metricNodes[1].value.textContent = format(result.resolution, 3); metricNodes[2].value.textContent = String(result.values.filter(function (item) { return item.passed; }).length) + "/" + FREQUENCIES.length; metricNodes[3].value.textContent = result.resolved ? "可分辨" : "低于 d_min"; stageTitle.querySelector(".pfa-stage-status").textContent = result.highPass ? "中心低频被挡" : result.lowPass ? "外圈高频被挡" : "孔径通带直接决定频率"; formula.textContent = "f_c,inc=2NA/λ=" + format(result.incoherentCutoff, 2) + "；f_c,coh=NA/λ=" + format(result.coherentCutoff, 2) + "；d_min=0.61λ/NA=" + format(result.resolution, 3) + " μm"; clear(chart);
      var left = 48, right = 15, width = 650, top = 28, mid = 145, lowerTop = 205, bottom = 315;
      function xFrequency(value) { return left + (width - left - right) * value / 4; } function yBar(value) { return mid - (mid - top) * value; } function xSpace(value) { return left + (width - left - right) * (value + 1.6) / 3.2; } function yIntensity(value) { return bottom - (bottom - lowerTop) * clamp(value, 0, 1); }
      [0, 0.5, 1].forEach(function (value) { chart.appendChild(svgElement(doc, "line", { class: "pfa-grid", x1: left, x2: width - right, y1: yBar(value), y2: yBar(value) })); chart.appendChild(svgElement(doc, "text", { class: "pfa-label", x: 5, y: yBar(value) + 4, text: format(value, 1) })); }); chart.appendChild(svgElement(doc, "line", { class: "pfa-axis", x1: left, x2: width - right, y1: mid, y2: mid })); result.values.forEach(function (item) { var x = xFrequency(item.frequency); var barWidth = 34; chart.appendChild(svgElement(doc, "rect", { class: item.passed ? "pfa-passbar" : "pfa-blockbar", x: x - barWidth / 2, y: yBar(item.value), width: barWidth, height: mid - yBar(item.value), rx: 2 })); chart.appendChild(svgElement(doc, "text", { class: "pfa-label", x: x, y: mid + 17, "text-anchor": "middle", text: String(item.frequency) })); }); var cutX = xFrequency(result.cutoff); if (result.cutoff <= 4) chart.appendChild(svgElement(doc, "line", { class: "pfa-marker", x1: cutX, x2: cutX, y1: top, y2: mid })); chart.appendChild(svgElement(doc, "text", { class: "pfa-label", x: left, y: top - 8, text: "频谱面：cycles/μm" }));
      [0, 0.5, 1].forEach(function (value) { chart.appendChild(svgElement(doc, "line", { class: "pfa-grid", x1: left, x2: width - right, y1: yIntensity(value), y2: yIntensity(value) })); }); chart.appendChild(svgElement(doc, "line", { class: "pfa-axis", x1: left, x2: width - right, y1: bottom, y2: bottom })); var psf = []; var points = []; for (var index = 0; index <= 160; index += 1) { var position = -1.6 + 3.2 * index / 160; var envelope = Math.exp(-Math.pow(position / Math.max(result.resolution * 1.6, 0.05), 2)); psf.push(xSpace(position) + "," + yIntensity(envelope)); var pointLeft = Math.exp(-Math.pow((position + result.separation / 2) / Math.max(result.resolution * 0.8, 0.05), 2)); var pointRight = Math.exp(-Math.pow((position - result.separation / 2) / Math.max(result.resolution * 0.8, 0.05), 2)); points.push(xSpace(position) + "," + yIntensity(Math.max(pointLeft, pointRight))); } chart.appendChild(svgElement(doc, "polyline", { class: "pfa-psf", points: psf.join(" ") })); chart.appendChild(svgElement(doc, "polyline", { class: "pfa-points", points: points.join(" ") })); var centerX = xSpace(0); chart.appendChild(svgElement(doc, "line", { class: "pfa-marker", x1: centerX, x2: centerX, y1: lowerTop, y2: bottom })); chart.appendChild(svgElement(doc, "text", { class: "pfa-label", x: left, y: lowerTop - 8, text: "像面横截面：蓝=双点，金=单点 PSF" })); chart.appendChild(svgElement(doc, "text", { class: "pfa-label", x: width - 70, y: bottom + 17, text: "x / μm" }));
    }
    reveal.addEventListener("click", function () { if (state.predictions.some(function (value) { return value === null; })) { feedback.className = "pfa-feedback pfa-warn"; feedback.textContent = "请先完成三个预测，再打开频谱账本。"; return; } var score = state.predictions.reduce(function (sum, value, index) { return sum + (value === questions[index].answer ? 1 : 0); }, 0); feedback.className = "pfa-feedback " + (score === questions.length ? "pfa-pass" : "pfa-warn"); feedback.textContent = "预测 " + score + "/" + questions.length + "。现在观察通带、PSF 与双点判据。"; revealed.hidden = false; render(); announce(api, root, feedback.textContent); }); clearPredictions.addEventListener("click", function () { state.predictions = [null, null, null]; choiceButtons.forEach(function (buttons) { buttons.forEach(function (button) { button.setAttribute("aria-pressed", "false"); }); }); feedback.className = "pfa-feedback"; feedback.textContent = "预测已清空。"; }); reset.addEventListener("click", function () { state.pattern = "slit"; state.lambda = 0.5; state.na = 0.5; state.aperture = 1; state.stop = 1; state.separation = 0.7; state.filter = "none"; revealed.hidden = true; feedback.className = "pfa-feedback"; feedback.textContent = "实验已重新上锁，请再预测。"; announce(api, root, "傅里叶孔径实验已重置。"); });
  }

  function selfTest() { var checks = 0; function check(condition, message) { checks += 1; if (!condition) throw new Error(message); } check(near(sinc(0), 1), "sinc limit"); check(near(cutoff(0.5, 0.5), 2), "incoherent cutoff formula"); check(near(coherentCutoff(0.5, 0.5), 1), "coherent cutoff formula"); check(near(resolution(0.5, 0.5), 0.61), "Rayleigh resolution"); var narrow = evaluate({ pattern: "slit", aperture: 0.5, lambda: 0.5, na: 0.5, filter: "none" }); var wide = evaluate({ pattern: "slit", aperture: 1, lambda: 0.5, na: 0.5, filter: "none" }); check(narrow.values[0].value >= 0 && narrow.values.every(function (item) { return item.value >= 0 && item.value <= 1; }), "bounded transmissions"); check(narrow.values[1].value > wide.values[1].value, "narrow aperture broadens frequency envelope"); var defaultLedger = evaluate({ pattern: "slit", aperture: 1, lambda: 0.5, na: 0.5, filter: "none" }); check(defaultLedger.values[1].passed && !defaultLedger.values[2].passed, "hard cutoff support versus attenuation"); check(evaluate({ filter: "stop", stop: 1, na: 0.9 }).values[0].value === 0, "center stop rejects low frequency"); check(evaluate({ separation: 1, lambda: 0.5, na: 0.5 }).resolved, "separation above resolution"); var again = evaluate({ pattern: "circle", aperture: 1.2, lambda: 0.55, na: 0.6, filter: "low", stop: 1.4 }); check(JSON.stringify(again) === JSON.stringify(evaluate({ pattern: "circle", aperture: 1.2, lambda: 0.55, na: 0.6, filter: "low", stop: 1.4 })), "deterministic Fourier model"); return { checks: checks }; }

  return { sinc: sinc, cutoff: cutoff, coherentCutoff: coherentCutoff, incoherentCutoff: incoherentCutoff, resolution: resolution, transmission: transmission, evaluate: evaluate, mount: mount, selfTest: selfTest };
});
