(function (root, factory) {
  "use strict";

  var exported = factory(root);
  if (typeof module === "object" && module.exports) module.exports = exported;
  if (root && root.CourseLearning && typeof root.CourseLearning.register === "function") {
    root.CourseLearning.register("photo-gaussian-beam", exported.mount);
  }
  if (typeof module === "object" && module.exports && typeof require === "function" && require.main === module) {
    try {
      var report = exported.selfTest();
      console.log("photo-gaussian-beam self-test: PASS (" + report.checks + " checks)");
    } catch (error) {
      console.error("photo-gaussian-beam self-test: FAIL\n" + error.stack);
      process.exitCode = 1;
    }
  }
})(typeof window !== "undefined" ? window : typeof globalThis !== "undefined" ? globalThis : this, function (host) {
  "use strict";

  var STYLE_ID = "photo-gaussian-beam-styles";
  var SVG_NS = "http://www.w3.org/2000/svg";
  var PI = Math.PI;
  var EPS = 1e-9;

  var STYLE_TEXT = [
    ".pgb-lab{--pgb-blue:var(--cl-blue,#315f9d);--pgb-green:var(--cl-green,#39734d);--pgb-gold:var(--cl-gold,#9b6a12);--pgb-red:var(--cl-red,#b64335);max-width:100%;min-width:0;color:var(--fg);line-height:1.55;overflow-wrap:anywhere}",
    ".pgb-lab *,.pgb-lab *::before,.pgb-lab *::after{box-sizing:border-box}.pgb-lab [hidden]{display:none!important}.pgb-lab h3,.pgb-lab h4{margin:0;letter-spacing:0}.pgb-lab h3{font-size:1.15rem}.pgb-lab p{margin:.65em 0}.pgb-lab button,.pgb-lab input{font:inherit;letter-spacing:0}.pgb-lab button{min-width:0;min-height:44px;padding:8px 11px;border:1px solid var(--border);border-radius:6px;background:var(--bg);color:var(--fg);cursor:pointer;line-height:1.35;overflow-wrap:anywhere}.pgb-lab button:hover{border-color:var(--pgb-blue)}.pgb-lab button:focus-visible,.pgb-lab input:focus-visible{outline:3px solid var(--cl-focus,#1769aa);outline-offset:2px}.pgb-lab button[aria-pressed=true],.pgb-lab .pgb-primary{border-color:var(--pgb-blue);background:var(--pgb-blue);color:var(--bg);font-weight:750}.pgb-lab .pgb-note,.pgb-lab .pgb-feedback{color:var(--fg-soft);font-size:13px;line-height:1.65}.pgb-lab .pgb-prediction{margin:14px 0;padding:12px 14px;border-left:3px solid var(--pgb-gold);background:var(--block-bg,var(--bg))}.pgb-lab .pgb-question{margin:0 0 12px;padding:0;border:0}.pgb-lab .pgb-question:last-of-type{margin-bottom:0}.pgb-lab .pgb-question legend{max-width:100%;margin-bottom:7px;font-size:13px;font-weight:750;line-height:1.5}.pgb-lab .pgb-choices{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:7px}.pgb-lab .pgb-choices button{font-size:12px}.pgb-lab .pgb-actions{display:flex;flex-wrap:wrap;gap:8px;margin-top:12px}.pgb-lab .pgb-actions>*{flex:1 1 170px}.pgb-lab .pgb-feedback{min-height:2em;margin:8px 0 0;font-weight:700}.pgb-lab .pgb-pass{color:var(--pgb-green)}.pgb-lab .pgb-warn{color:var(--pgb-red)}.pgb-lab .pgb-revealed{margin-top:18px;padding-top:16px;border-top:1px solid var(--border)}.pgb-lab .pgb-controls{display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:10px;margin:12px 0;align-items:end}.pgb-lab .pgb-control{display:grid;gap:5px;min-width:0}.pgb-lab .pgb-control label{color:var(--fg-soft);font-size:12.5px;font-weight:700}.pgb-lab .pgb-control output{color:var(--pgb-blue);font-variant-numeric:tabular-nums}.pgb-lab input[type=range]{display:block;width:100%;min-height:44px;margin:0;accent-color:var(--pgb-blue)}.pgb-lab .pgb-scale{display:flex;justify-content:space-between;gap:8px;color:var(--fg-soft);font-size:11px}.pgb-lab .pgb-stage{min-width:0;padding:8px;border:1px solid var(--border);border-radius:7px;background:var(--bg);overflow:hidden}.pgb-lab .pgb-stage-title{display:flex;flex-wrap:wrap;justify-content:space-between;gap:8px;margin-bottom:8px;color:var(--fg-soft);font-size:13px}.pgb-lab svg{display:block;width:100%;height:auto;max-width:100%;color:var(--fg)}.pgb-lab svg text{fill:currentColor;font-family:inherit;letter-spacing:0}.pgb-lab .pgb-grid{stroke:currentColor;stroke-width:1;stroke-opacity:.14}.pgb-lab .pgb-axis{stroke:currentColor;stroke-width:1.1;stroke-opacity:.62}.pgb-lab .pgb-beam{fill:var(--pgb-blue);fill-opacity:.18;stroke:var(--pgb-blue);stroke-width:2.5}.pgb-lab .pgb-waist{stroke:var(--pgb-red);stroke-width:2;stroke-dasharray:5 4}.pgb-lab .pgb-marker{stroke:var(--pgb-gold);stroke-width:2;stroke-dasharray:4 4}.pgb-lab .pgb-label{font-size:11px;fill:var(--fg-soft)}.pgb-lab .pgb-metrics{display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:8px;margin-top:12px}.pgb-lab .pgb-metric{min-width:0;padding:9px;border-top:2px solid var(--border);background:var(--bg)}.pgb-lab .pgb-metric:nth-child(5n+1){border-color:var(--pgb-blue)}.pgb-lab .pgb-metric:nth-child(5n+2){border-color:var(--pgb-gold)}.pgb-lab .pgb-metric:nth-child(5n+3){border-color:var(--pgb-green)}.pgb-lab .pgb-metric:nth-child(5n+4){border-color:var(--pgb-red)}.pgb-lab .pgb-metric span{display:block;color:var(--fg-soft);font-size:11px}.pgb-lab .pgb-metric strong{display:block;margin-top:3px;font-size:14px;font-variant-numeric:tabular-nums;overflow-wrap:anywhere}.pgb-lab .pgb-formula{margin:12px 0 0;padding:9px 11px;border-left:3px solid var(--pgb-blue);background:var(--bg);font-family:SFMono-Regular,Menlo,Consolas,monospace;font-size:12px;line-height:1.65;overflow-x:auto}.pgb-lab .pgb-reset{margin-top:10px;color:var(--fg-soft)}@media(max-width:950px){.pgb-lab .pgb-controls{grid-template-columns:repeat(3,minmax(0,1fr))}.pgb-lab .pgb-metrics{grid-template-columns:repeat(3,minmax(0,1fr))}}@media(max-width:620px){.pgb-lab .pgb-choices{grid-template-columns:minmax(0,1fr)}.pgb-lab .pgb-controls,.pgb-lab .pgb-metrics{grid-template-columns:repeat(2,minmax(0,1fr))}}@media(max-width:400px){.pgb-lab .pgb-controls,.pgb-lab .pgb-metrics{grid-template-columns:minmax(0,1fr)}.pgb-lab .pgb-actions>*{flex-basis:100%}}@media(prefers-reduced-motion:reduce){.pgb-lab *{animation:none!important;transition:none!important;scroll-behavior:auto!important}}"
  ].join("\n");

  function finite(value, fallback) { return typeof value === "number" && Number.isFinite(value) ? value : fallback; }
  function clamp(value, minimum, maximum) { return Math.max(minimum, Math.min(maximum, value)); }
  function normalize(value, fallback, minimum, maximum) { return clamp(finite(Number(value), fallback), minimum, maximum); }
  function near(left, right, tolerance) { return Math.abs(left - right) <= (tolerance || EPS) * Math.max(1, Math.abs(left), Math.abs(right)); }

  function evaluate(input) {
    var source = input || {};
    var w0mm = normalize(source.w0, 0.5, 0.1, 1);
    var lambdaUm = normalize(source.lambda, 1, 0.4, 1.5);
    var m2 = normalize(source.m2, 1, 1, 3);
    var zMultiple = normalize(source.zMultiple, 0, -3, 3);
    var power = normalize(source.power, 1, 0.1, 5);
    var w0 = w0mm * 1e-3;
    var lambda = lambdaUm * 1e-6;
    var zRIdeal = PI * w0 * w0 / lambda;
    var zR = zRIdeal / m2;
    var theta = m2 * lambda / (PI * w0);
    var z = zMultiple * zR;
    var radius = w0 * Math.sqrt(1 + Math.pow(z / zR, 2));
    var bpp = w0 * theta;
    var peak = 2 * power / (PI * radius * radius);
    return { w0mm: w0mm, lambdaUm: lambdaUm, m2: m2, zMultiple: zMultiple, power: power, w0: w0, lambda: lambda, zRIdeal: zRIdeal, zR: zR, z: z, theta: theta, radius: radius, bpp: bpp, peak: peak };
  }

  function setAttributes(node, attrs) { Object.keys(attrs || {}).forEach(function (key) { var value = attrs[key]; if (value === undefined || value === null || value === false) return; if (key === "className") node.setAttribute("class", value); else if (key === "text") node.textContent = String(value); else if (key.slice(0, 2) === "on" && typeof value === "function") node.addEventListener(key.slice(2).toLowerCase(), value); else if (value === true) node.setAttribute(key, ""); else node.setAttribute(key, String(value)); }); return node; }
  function appendChildren(node, children) { (Array.isArray(children) ? children : [children]).forEach(function (child) { if (child !== undefined && child !== null && child !== false) node.appendChild(child && child.nodeType ? child : node.ownerDocument.createTextNode(String(child))); }); return node; }
  function element(doc, tag, attrs, children) { return appendChildren(setAttributes(doc.createElement(tag), attrs), children); }
  function svgElement(doc, tag, attrs, children) { return appendChildren(setAttributes(doc.createElementNS(SVG_NS, tag), attrs), children); }
  function clear(node) { while (node.firstChild) node.removeChild(node.firstChild); }
  function format(value, digits) { if (!Number.isFinite(value)) return "—"; var places = digits === undefined ? 3 : digits; var text = Math.abs(value) < 0.001 && value !== 0 ? value.toExponential(Math.min(places, 4)) : value.toFixed(places); return text.indexOf(".") === -1 ? text : text.replace(/0+$/, "").replace(/\.$/, ""); }
  function installStyles(doc) { if (!doc || !doc.getElementById || doc.getElementById(STYLE_ID)) return; var style = doc.createElement("style"); style.id = STYLE_ID; style.textContent = STYLE_TEXT; (doc.head || doc.documentElement).appendChild(style); }
  function announce(api, root, message) { if (api && typeof api.announce === "function") api.announce(root, message); }
  function metric(doc, label) { var value = element(doc, "strong", { text: "—" }); return { node: element(doc, "div", { className: "pgb-metric" }, [element(doc, "span", { text: label }), value]), value: value }; }

  function mount(root, api) {
    if (!root || !root.ownerDocument) return;
    var doc = root.ownerDocument; installStyles(doc); root.classList.add("pgb-lab"); clear(root);
    var state = { w0: 0.5, lambda: 1, m2: 1, zMultiple: 0, power: 1, predictions: [null, null, null] };
    var questions = [
      { prompt: "w₀ 减半，z_R 如何变？", options: ["减半", "减为 1/4", "不变"], answer: 1 },
      { prompt: "w₀ 减半，发散角如何变？", options: ["减半", "加倍", "不变"], answer: 1 },
      { prompt: "无源透镜能把 BPP 降低到 λ/π 以下吗？", options: ["能", "不能", "只要功率足够"], answer: 1 }
    ];
    root.appendChild(element(doc, "h3", { text: "预测闸门：先追踪束腰与发散的水床" })); root.appendChild(element(doc, "p", { className: "pgb-note", text: "先判断平方缩放、反比缩放和 BPP 守恒；揭示后沿传播轴拖动观察平面。" })); var prediction = element(doc, "div", { className: "pgb-prediction" }); var choiceButtons = [];
    questions.forEach(function (question, questionIndex) { var fieldset = element(doc, "fieldset", { className: "pgb-question" }); fieldset.appendChild(element(doc, "legend", { text: (questionIndex + 1) + ". " + question.prompt })); var choices = element(doc, "div", { className: "pgb-choices" }); question.options.forEach(function (label, optionIndex) { var button = element(doc, "button", { type: "button", text: label, "aria-pressed": "false", onclick: function () { state.predictions[questionIndex] = optionIndex; choiceButtons[questionIndex].forEach(function (item) { item.setAttribute("aria-pressed", "false"); }); button.setAttribute("aria-pressed", "true"); } }); choices.appendChild(button); if (!choiceButtons[questionIndex]) choiceButtons[questionIndex] = []; choiceButtons[questionIndex].push(button); }); fieldset.appendChild(choices); prediction.appendChild(fieldset); }); var feedback = element(doc, "p", { className: "pgb-feedback", "aria-live": "polite" }); var actions = element(doc, "div", { className: "pgb-actions" }); var reveal = element(doc, "button", { type: "button", className: "pgb-primary", text: "揭示传播" }); var clearPredictions = element(doc, "button", { type: "button", text: "清空预测" }); actions.appendChild(reveal); actions.appendChild(clearPredictions); prediction.appendChild(actions); prediction.appendChild(feedback); root.appendChild(prediction);
    var revealed = element(doc, "div", { className: "pgb-revealed", hidden: true }); revealed.appendChild(element(doc, "h4", { text: "高斯半径曲线与 BPP" })); var controls = element(doc, "div", { className: "pgb-controls" });
    function addRange(label, key, min, max, step, digits, suffix) { var output = element(doc, "output", { text: format(state[key], digits) + suffix }); var input = element(doc, "input", { type: "range", min: min, max: max, step: step, value: state[key], "aria-label": label }); input.addEventListener("input", function () { state[key] = Number(input.value); output.textContent = format(state[key], digits) + suffix; render(); }); controls.appendChild(element(doc, "div", { className: "pgb-control" }, [element(doc, "label", { text: label }), output, input, element(doc, "div", { className: "pgb-scale" }, [element(doc, "span", { text: String(min) + suffix }), element(doc, "span", { text: String(max) + suffix })])])); }
    addRange("束腰半径 w₀", "w0", 0.1, 1, 0.05, 2, " mm"); addRange("波长 λ", "lambda", 0.4, 1.5, 0.05, 2, " μm"); addRange("光束质量 M²", "m2", 1, 3, 0.1, 1, ""); addRange("观察位置 z/zR", "zMultiple", -3, 3, 0.1, 1, ""); addRange("总功率 P", "power", 0.1, 5, 0.1, 1, " W"); revealed.appendChild(controls);
    var stage = element(doc, "div", { className: "pgb-stage" }); var stageTitle = element(doc, "div", { className: "pgb-stage-title" }, [element(doc, "span", { text: "传播轴上的 1/e² 半径" }), element(doc, "span", { className: "pgb-stage-status", text: "" })]); var chart = svgElement(doc, "svg", { viewBox: "0 0 650 310", role: "img", "aria-label": "高斯光束半径随传播位置变化的曲线" }); stage.appendChild(stageTitle); stage.appendChild(chart); revealed.appendChild(stage);
    var metrics = element(doc, "div", { className: "pgb-metrics" }); var metricNodes = [metric(doc, "z_R /m"), metric(doc, "发散半角 /mrad"), metric(doc, "当前半径 /mm"), metric(doc, "BPP /m·rad"), metric(doc, "束腰峰值 /W·m⁻²")]; metricNodes.forEach(function (item) { metrics.appendChild(item.node); }); revealed.appendChild(metrics); var formula = element(doc, "div", { className: "pgb-formula", text: "w(z)=w₀√(1+(z/zR)²)；BPP=w₀θ=M²λ/π" }); revealed.appendChild(formula); var reset = element(doc, "button", { type: "button", className: "pgb-reset", text: "重置实验" }); revealed.appendChild(reset); root.appendChild(revealed);

    function render() { var result = evaluate(state); metricNodes[0].value.textContent = format(result.zR, 3); metricNodes[1].value.textContent = format(result.theta * 1000, 3); metricNodes[2].value.textContent = format(result.radius * 1000, 3); metricNodes[3].value.textContent = format(result.bpp, 4); metricNodes[4].value.textContent = format(result.peak, 0); stageTitle.querySelector(".pgb-stage-status").textContent = "M²=" + format(result.m2, 1) + "；当前 z=" + format(result.z, 3) + " m"; formula.textContent = "zR=πw₀²/(M²λ)=" + format(result.zR, 3) + " m；θ=M²λ/(πw₀)=" + format(result.theta * 1000, 3) + " mrad；w₀θ=" + format(result.bpp, 4) + " m·rad"; clear(chart);
      var left = 48, right = 16, top = 28, bottom = 150, width = 650, maxRadius = result.w0 * Math.sqrt(10) * 1000; function x(value) { return left + (width - left - right) * (value + 3) / 6; } function y(value) { return bottom - (bottom - top) * value / maxRadius; } [0, maxRadius / 2, maxRadius].forEach(function (value) { chart.appendChild(svgElement(doc, "line", { class: "pgb-grid", x1: left, x2: width - right, y1: y(value), y2: y(value) })); chart.appendChild(svgElement(doc, "text", { class: "pgb-label", x: 6, y: y(value) + 4, text: format(value, 2) })); }); chart.appendChild(svgElement(doc, "line", { class: "pgb-axis", x1: left, x2: width - right, y1: y(0), y2: y(0) })); chart.appendChild(svgElement(doc, "line", { class: "pgb-axis", x1: x(-3), x2: x(3), y1: bottom, y2: bottom })); var upper = [], lower = []; for (var index = 0; index <= 160; index += 1) { var position = -3 + 6 * index / 160; var radius = result.w0 * Math.sqrt(1 + Math.pow(position, 2)) * 1000; upper.push(x(position) + "," + y(radius)); lower.push(x(position) + "," + y(-radius)); } chart.appendChild(svgElement(doc, "polygon", { class: "pgb-beam", points: upper.concat(lower.reverse()).join(" ") })); chart.appendChild(svgElement(doc, "line", { class: "pgb-waist", x1: x(0), x2: x(0), y1: top, y2: bottom })); chart.appendChild(svgElement(doc, "line", { class: "pgb-marker", x1: x(result.zMultiple), x2: x(result.zMultiple), y1: top, y2: bottom })); chart.appendChild(svgElement(doc, "text", { class: "pgb-label", x: left, y: top - 8, text: "半径 /mm" })); chart.appendChild(svgElement(doc, "text", { class: "pgb-label", x: width - 86, y: bottom + 19, text: "z/zR" })); chart.appendChild(svgElement(doc, "text", { class: "pgb-label", x: x(0) + 4, y: top + 14, text: "束腰" })); }
    reveal.addEventListener("click", function () { if (state.predictions.some(function (value) { return value === null; })) { feedback.className = "pgb-feedback pgb-warn"; feedback.textContent = "请先完成三个预测，再打开高斯束账本。"; return; } var score = state.predictions.reduce(function (sum, value, index) { return sum + (value === questions[index].answer ? 1 : 0); }, 0); feedback.className = "pgb-feedback " + (score === questions.length ? "pgb-pass" : "pgb-warn"); feedback.textContent = "预测 " + score + "/" + questions.length + "。现在检查 z_R、θ 和 BPP 是否同时满足约束。"; revealed.hidden = false; render(); announce(api, root, feedback.textContent); }); clearPredictions.addEventListener("click", function () { state.predictions = [null, null, null]; choiceButtons.forEach(function (buttons) { buttons.forEach(function (button) { button.setAttribute("aria-pressed", "false"); }); }); feedback.className = "pgb-feedback"; feedback.textContent = "预测已清空。"; }); reset.addEventListener("click", function () { state.w0 = 0.5; state.lambda = 1; state.m2 = 1; state.zMultiple = 0; state.power = 1; revealed.hidden = true; feedback.className = "pgb-feedback"; feedback.textContent = "实验已重新上锁，请再预测。"; announce(api, root, "高斯光束实验已重置。"); });
  }

  function selfTest() { var checks = 0; function check(condition, message) { checks += 1; if (!condition) throw new Error(message); } var base = evaluate({ w0: 0.5, lambda: 1, m2: 1, zMultiple: 1 }); check(near(base.radius / base.w0, Math.sqrt(2), 1e-9), "Rayleigh radius"); check(near(base.w0 * base.theta, base.lambda / PI, 1e-12), "ideal BPP invariant"); var half = evaluate({ w0: 0.25, lambda: 1, m2: 1, zMultiple: 0 }); check(near(half.zRIdeal / evaluate({ w0: 0.5, lambda: 1, m2: 1 }).zRIdeal, 0.25, 1e-9), "Rayleigh length square scaling"); check(near(half.theta / evaluate({ w0: 0.5, lambda: 1, m2: 1 }).theta, 2, 1e-9), "divergence inverse scaling"); var poor = evaluate({ w0: 0.5, lambda: 1, m2: 2, zMultiple: 0 }); check(near(poor.bpp, 2 * base.bpp, 1e-9) && poor.theta > base.theta, "M2 penalty"); check(JSON.stringify(base) === JSON.stringify(evaluate({ w0: 0.5, lambda: 1, m2: 1, zMultiple: 1 })), "deterministic Gaussian model"); return { checks: checks }; }

  return { evaluate: evaluate, mount: mount, selfTest: selfTest };
});
