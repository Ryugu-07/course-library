(function (root, factory) {
  "use strict";

  var exported = factory(root);
  if (typeof module === "object" && module.exports) module.exports = exported;
  if (root && root.CourseLearning && typeof root.CourseLearning.register === "function") {
    root.CourseLearning.register("photo-polarization", exported.mount);
  }
  if (typeof module === "object" && module.exports && typeof require === "function" && require.main === module) {
    try {
      var report = exported.selfTest();
      console.log("photo-polarization self-test: PASS (" + report.checks + " checks)");
    } catch (error) {
      console.error("photo-polarization self-test: FAIL\n" + error.stack);
      process.exitCode = 1;
    }
  }
})(typeof window !== "undefined" ? window : typeof globalThis !== "undefined" ? globalThis : this, function (host) {
  "use strict";

  var STYLE_ID = "photo-polarization-styles";
  var SVG_NS = "http://www.w3.org/2000/svg";
  var PI = Math.PI;
  var EPS = 1e-9;

  var STYLE_TEXT = [
    ".ppo-lab{--ppo-blue:var(--cl-blue,#315f9d);--ppo-green:var(--cl-green,#39734d);--ppo-gold:var(--cl-gold,#9b6a12);--ppo-red:var(--cl-red,#b64335);max-width:100%;min-width:0;color:var(--fg);line-height:1.55;overflow-wrap:anywhere}",
    ".ppo-lab *,.ppo-lab *::before,.ppo-lab *::after{box-sizing:border-box}.ppo-lab [hidden]{display:none!important}.ppo-lab h3,.ppo-lab h4{margin:0;letter-spacing:0}.ppo-lab h3{font-size:1.15rem}.ppo-lab p{margin:.65em 0}.ppo-lab button,.ppo-lab input,.ppo-lab select{font:inherit;letter-spacing:0}.ppo-lab button,.ppo-lab select{min-width:0;min-height:44px;padding:8px 11px;border:1px solid var(--border);border-radius:6px;background:var(--bg);color:var(--fg);cursor:pointer;line-height:1.35;overflow-wrap:anywhere}.ppo-lab button:hover{border-color:var(--ppo-blue)}.ppo-lab button:focus-visible,.ppo-lab input:focus-visible,.ppo-lab select:focus-visible{outline:3px solid var(--cl-focus,#1769aa);outline-offset:2px}.ppo-lab button[aria-pressed=true],.ppo-lab .ppo-primary{border-color:var(--ppo-blue);background:var(--ppo-blue);color:var(--bg);font-weight:750}.ppo-lab .ppo-note,.ppo-lab .ppo-feedback{color:var(--fg-soft);font-size:13px;line-height:1.65}.ppo-lab .ppo-prediction{margin:14px 0;padding:12px 14px;border-left:3px solid var(--ppo-gold);background:var(--block-bg,var(--bg))}.ppo-lab .ppo-question{margin:0 0 12px;padding:0;border:0}.ppo-lab .ppo-question:last-of-type{margin-bottom:0}.ppo-lab .ppo-question legend{max-width:100%;margin-bottom:7px;font-size:13px;font-weight:750;line-height:1.5}.ppo-lab .ppo-choices{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:7px}.ppo-lab .ppo-choices button{font-size:12px}.ppo-lab .ppo-actions{display:flex;flex-wrap:wrap;gap:8px;margin-top:12px}.ppo-lab .ppo-actions>*{flex:1 1 170px}.ppo-lab .ppo-feedback{min-height:2em;margin:8px 0 0;font-weight:700}.ppo-lab .ppo-pass{color:var(--ppo-green)}.ppo-lab .ppo-warn{color:var(--ppo-red)}.ppo-lab .ppo-revealed{margin-top:18px;padding-top:16px;border-top:1px solid var(--border)}.ppo-lab .ppo-controls{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:10px;margin:12px 0;align-items:end}.ppo-lab .ppo-control{display:grid;gap:5px;min-width:0}.ppo-lab .ppo-control label{color:var(--fg-soft);font-size:12.5px;font-weight:700}.ppo-lab .ppo-control output{color:var(--ppo-blue);font-variant-numeric:tabular-nums}.ppo-lab input[type=range]{display:block;width:100%;min-height:44px;margin:0;accent-color:var(--ppo-blue)}.ppo-lab .ppo-scale{display:flex;justify-content:space-between;gap:8px;color:var(--fg-soft);font-size:11px}.ppo-lab .ppo-stage{min-width:0;padding:8px;border:1px solid var(--border);border-radius:7px;background:var(--bg);overflow:hidden}.ppo-lab .ppo-stage-title{display:flex;flex-wrap:wrap;justify-content:space-between;gap:8px;margin-bottom:8px;color:var(--fg-soft);font-size:13px}.ppo-lab svg{display:block;width:100%;height:auto;max-width:100%;color:var(--fg)}.ppo-lab svg text{fill:currentColor;font-family:inherit;letter-spacing:0}.ppo-lab .ppo-grid{stroke:currentColor;stroke-width:1;stroke-opacity:.14}.ppo-lab .ppo-axis{stroke:currentColor;stroke-width:1.1;stroke-opacity:.62}.ppo-lab .ppo-bar{fill:var(--ppo-blue);fill-opacity:.75}.ppo-lab .ppo-negative{fill:var(--ppo-red)}.ppo-lab .ppo-vector{stroke:var(--ppo-gold);stroke-width:3;fill:none}.ppo-lab .ppo-marker{stroke:var(--ppo-red);stroke-width:2;stroke-dasharray:5 4}.ppo-lab .ppo-label{font-size:11px;fill:var(--fg-soft)}.ppo-lab .ppo-metrics{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:8px;margin-top:12px}.ppo-lab .ppo-metric{min-width:0;padding:9px;border-top:2px solid var(--border);background:var(--bg)}.ppo-lab .ppo-metric:nth-child(4n+1){border-color:var(--ppo-blue)}.ppo-lab .ppo-metric:nth-child(4n+2){border-color:var(--ppo-gold)}.ppo-lab .ppo-metric:nth-child(4n+3){border-color:var(--ppo-green)}.ppo-lab .ppo-metric:nth-child(4n){border-color:var(--ppo-red)}.ppo-lab .ppo-metric span{display:block;color:var(--fg-soft);font-size:11px}.ppo-lab .ppo-metric strong{display:block;margin-top:3px;font-size:14px;font-variant-numeric:tabular-nums;overflow-wrap:anywhere}.ppo-lab .ppo-formula{margin:12px 0 0;padding:9px 11px;border-left:3px solid var(--ppo-blue);background:var(--bg);font-family:SFMono-Regular,Menlo,Consolas,monospace;font-size:12px;line-height:1.65;overflow-x:auto}.ppo-lab .ppo-reset{margin-top:10px;color:var(--fg-soft)}@media(max-width:850px){.ppo-lab .ppo-controls{grid-template-columns:repeat(2,minmax(0,1fr))}}@media(max-width:620px){.ppo-lab .ppo-choices{grid-template-columns:minmax(0,1fr)}.ppo-lab .ppo-controls,.ppo-lab .ppo-metrics{grid-template-columns:repeat(2,minmax(0,1fr))}}@media(max-width:400px){.ppo-lab .ppo-controls,.ppo-lab .ppo-metrics{grid-template-columns:minmax(0,1fr)}.ppo-lab .ppo-actions>*{flex-basis:100%}}@media(prefers-reduced-motion:reduce){.ppo-lab *{animation:none!important;transition:none!important;scroll-behavior:auto!important}}"
  ].join("\n");

  function finite(value, fallback) { return typeof value === "number" && Number.isFinite(value) ? value : fallback; }
  function clamp(value, minimum, maximum) { return Math.max(minimum, Math.min(maximum, value)); }
  function normalize(value, fallback, minimum, maximum) { return clamp(finite(Number(value), fallback), minimum, maximum); }
  function near(left, right, tolerance) { return Math.abs(left - right) <= (tolerance || EPS) * Math.max(1, Math.abs(left), Math.abs(right)); }
  function complex(re, im) { return { re: re, im: im }; }
  function add(a, b) { return complex(a.re + b.re, a.im + b.im); }
  function multiply(a, b) { return complex(a.re * b.re - a.im * b.im, a.re * b.im + a.im * b.re); }
  function conjugate(a) { return complex(a.re, -a.im); }
  function magnitudeSquared(a) { return a.re * a.re + a.im * a.im; }
  function rotation(angle) { var c = Math.cos(angle), s = Math.sin(angle); return [[complex(c, 0), complex(-s, 0)], [complex(s, 0), complex(c, 0)]]; }
  function matrixMultiply(left, right) { return [[add(multiply(left[0][0], right[0][0]), multiply(left[0][1], right[1][0])), add(multiply(left[0][0], right[0][1]), multiply(left[0][1], right[1][1]))], [add(multiply(left[1][0], right[0][0]), multiply(left[1][1], right[1][0])), add(multiply(left[1][0], right[0][1]), multiply(left[1][1], right[1][1]))]]; }
  function applyMatrix(matrix, vector) { return [add(multiply(matrix[0][0], vector[0]), multiply(matrix[0][1], vector[1])), add(multiply(matrix[1][0], vector[0]), multiply(matrix[1][1], vector[1]))]; }
  function waveplate(kind, angle) { var delta = kind === "half" ? PI : kind === "quarter" ? PI / 2 : 0; var local = [[complex(1, 0), complex(0, 0)], [complex(0, 0), complex(Math.cos(delta), Math.sin(delta))]]; return matrixMultiply(rotation(angle), matrixMultiply(local, rotation(-angle))); }
  function stokes(vector) { var ex = vector[0], ey = vector[1]; var cross = multiply(conjugate(ex), ey); var s0 = magnitudeSquared(ex) + magnitudeSquared(ey); return { s0: s0, s1: magnitudeSquared(ex) - magnitudeSquared(ey), s2: 2 * cross.re, s3: 2 * cross.im, dop: s0 === 0 ? 0 : Math.sqrt(Math.max(0, Math.pow(magnitudeSquared(ex) - magnitudeSquared(ey), 2) + 4 * cross.re * cross.re + 4 * cross.im * cross.im)) / s0 }; }
  function inputVector(kind) { if (kind === "45") return [complex(Math.SQRT1_2, 0), complex(Math.SQRT1_2, 0)]; if (kind === "circular") return [complex(Math.SQRT1_2, 0), complex(0, Math.SQRT1_2)]; return [complex(1, 0), complex(0, 0)]; }

  function evaluate(input) {
    var source = input || {};
    var inputKind = source.input === "45" || source.input === "circular" || source.input === "partial" ? source.input : "horizontal";
    var plate = source.plate === "half" || source.plate === "quarter" ? source.plate : "none";
    var angle = normalize(source.angle, 45, -90, 90) * PI / 180;
    var plateAngle = normalize(source.plateAngle, 22.5, -90, 90) * PI / 180;
    var faraday = normalize(source.faraday, 0, 0, 90) * PI / 180;
    var direction = source.direction === "reverse" ? -1 : 1;
    var vector = inputVector(inputKind);
    if (inputKind !== "partial" && plate !== "none") vector = applyMatrix(waveplate(plate, plateAngle), vector);
    if (inputKind !== "partial" && faraday !== 0) vector = applyMatrix(rotation(direction * faraday), vector);
    var states = stokes(vector);
    if (inputKind === "partial") states = { s0: 1, s1: 0, s2: 0, s3: 0, dop: 0 };
    var analyzerIntensity = inputKind === "partial" ? 0.5 : magnitudeSquared(add(multiply(complex(Math.cos(angle), 0), vector[0]), multiply(complex(Math.sin(angle), 0), vector[1])));
    return { input: inputKind, plate: plate, angle: angle, plateAngle: plateAngle, faraday: faraday, direction: direction, vector: vector, stokes: states, intensity: analyzerIntensity, analyzerAngle: angle * 180 / PI, plateAngleDegrees: plateAngle * 180 / PI, rotationDegrees: direction * faraday * 180 / PI };
  }

  function setAttributes(node, attrs) { Object.keys(attrs || {}).forEach(function (key) { var value = attrs[key]; if (value === undefined || value === null || value === false) return; if (key === "className") node.setAttribute("class", value); else if (key === "text") node.textContent = String(value); else if (key.slice(0, 2) === "on" && typeof value === "function") node.addEventListener(key.slice(2).toLowerCase(), value); else if (value === true) node.setAttribute(key, ""); else node.setAttribute(key, String(value)); }); return node; }
  function appendChildren(node, children) { (Array.isArray(children) ? children : [children]).forEach(function (child) { if (child !== undefined && child !== null && child !== false) node.appendChild(child && child.nodeType ? child : node.ownerDocument.createTextNode(String(child))); }); return node; }
  function element(doc, tag, attrs, children) { return appendChildren(setAttributes(doc.createElement(tag), attrs), children); }
  function svgElement(doc, tag, attrs, children) { return appendChildren(setAttributes(doc.createElementNS(SVG_NS, tag), attrs), children); }
  function clear(node) { while (node.firstChild) node.removeChild(node.firstChild); }
  function format(value, digits) { if (!Number.isFinite(value)) return "—"; var places = digits === undefined ? 3 : digits; var text = Math.abs(value) < 0.001 && value !== 0 ? value.toExponential(Math.min(places, 4)) : value.toFixed(places); return text.indexOf(".") === -1 ? text : text.replace(/0+$/, "").replace(/\.$/, ""); }
  function installStyles(doc) { if (!doc || !doc.getElementById || doc.getElementById(STYLE_ID)) return; var style = doc.createElement("style"); style.id = STYLE_ID; style.textContent = STYLE_TEXT; (doc.head || doc.documentElement).appendChild(style); }
  function announce(api, root, message) { if (api && typeof api.announce === "function") api.announce(root, message); }
  function metric(doc, label) { var value = element(doc, "strong", { text: "—" }); return { node: element(doc, "div", { className: "ppo-metric" }, [element(doc, "span", { text: label }), value]), value: value }; }

  function mount(root, api) {
    if (!root || !root.ownerDocument) return;
    var doc = root.ownerDocument; installStyles(doc); root.classList.add("ppo-lab"); clear(root);
    var state = { input: "horizontal", plate: "none", angle: 45, plateAngle: 22.5, faraday: 0, direction: "forward", predictions: [null, null, null] };
    var questions = [
      { prompt: "水平光过 45° 分析器，透过比例？", options: ["0", "0.5", "1"], answer: 1 },
      { prompt: "半波片轴转 20°，线偏振方向转？", options: ["10°", "20°", "40°"], answer: 2 },
      { prompt: "完全非偏振光能否由一个 Jones 矢量完整表示？", options: ["能", "不能", "只需加倍强度"], answer: 1 }
    ];
    root.appendChild(element(doc, "h3", { text: "预测闸门：先猜分析器会读到多少" })); root.appendChild(element(doc, "p", { className: "ppo-note", text: "先判断投影、半波片的双角旋转和 Jones/Stokes 的适用域；揭示后逐级查看状态。" })); var prediction = element(doc, "div", { className: "ppo-prediction" }); var choiceButtons = [];
    questions.forEach(function (question, questionIndex) { var fieldset = element(doc, "fieldset", { className: "ppo-question" }); fieldset.appendChild(element(doc, "legend", { text: (questionIndex + 1) + ". " + question.prompt })); var choices = element(doc, "div", { className: "ppo-choices" }); question.options.forEach(function (label, optionIndex) { var button = element(doc, "button", { type: "button", text: label, "aria-pressed": "false", onclick: function () { state.predictions[questionIndex] = optionIndex; choiceButtons[questionIndex].forEach(function (item) { item.setAttribute("aria-pressed", "false"); }); button.setAttribute("aria-pressed", "true"); } }); choices.appendChild(button); if (!choiceButtons[questionIndex]) choiceButtons[questionIndex] = []; choiceButtons[questionIndex].push(button); }); fieldset.appendChild(choices); prediction.appendChild(fieldset); }); var feedback = element(doc, "p", { className: "ppo-feedback", "aria-live": "polite" }); var actions = element(doc, "div", { className: "ppo-actions" }); var reveal = element(doc, "button", { type: "button", className: "ppo-primary", text: "揭示偏振" }); var clearPredictions = element(doc, "button", { type: "button", text: "清空预测" }); actions.appendChild(reveal); actions.appendChild(clearPredictions); prediction.appendChild(actions); prediction.appendChild(feedback); root.appendChild(prediction);
    var revealed = element(doc, "div", { className: "ppo-revealed", hidden: true }); revealed.appendChild(element(doc, "h4", { text: "Jones 级联与 Stokes 读出" })); var controls = element(doc, "div", { className: "ppo-controls" });
    function addSelect(label, key, options) { var select = element(doc, "select", { "aria-label": label }); options.forEach(function (item) { select.appendChild(element(doc, "option", { value: item.value, text: item.text })); }); select.value = state[key]; select.addEventListener("change", function () { state[key] = select.value; render(); }); controls.appendChild(element(doc, "div", { className: "ppo-control" }, [element(doc, "label", { text: label }), select])); }
    addSelect("输入偏振", "input", [{ value: "horizontal", text: "水平线偏振" }, { value: "45", text: "45° 线偏振" }, { value: "circular", text: "圆偏振" }, { value: "partial", text: "完全非偏振" }]); addSelect("波片", "plate", [{ value: "none", text: "无波片" }, { value: "half", text: "半波片 λ/2" }, { value: "quarter", text: "四分之一波片 λ/4" }]); addSelect("传播方向", "direction", [{ value: "forward", text: "正向" }, { value: "reverse", text: "反向" }]);
    function addRange(label, key, min, max, step, digits, suffix) { var output = element(doc, "output", { text: format(state[key], digits) + suffix }); var input = element(doc, "input", { type: "range", min: min, max: max, step: step, value: state[key], "aria-label": label }); input.addEventListener("input", function () { state[key] = Number(input.value); output.textContent = format(state[key], digits) + suffix; render(); }); controls.appendChild(element(doc, "div", { className: "ppo-control" }, [element(doc, "label", { text: label }), output, input, element(doc, "div", { className: "ppo-scale" }, [element(doc, "span", { text: String(min) + suffix }), element(doc, "span", { text: String(max) + suffix })])])); }
    addRange("分析器角度", "angle", -90, 90, 1, 0, "°"); addRange("波片快轴", "plateAngle", -90, 90, 1, 1, "°"); addRange("Faraday 角", "faraday", 0, 90, 1, 0, "°"); revealed.appendChild(controls);
    var stage = element(doc, "div", { className: "ppo-stage" }); var stageTitle = element(doc, "div", { className: "ppo-stage-title" }, [element(doc, "span", { text: "Stokes 分量与偏振向量" }), element(doc, "span", { className: "ppo-stage-status", text: "" })]); var chart = svgElement(doc, "svg", { viewBox: "0 0 620 300", role: "img", "aria-label": "偏振 Stokes 分量柱状图与归一化偏振向量" }); stage.appendChild(stageTitle); stage.appendChild(chart); revealed.appendChild(stage);
    var metrics = element(doc, "div", { className: "ppo-metrics" }); var metricNodes = [metric(doc, "分析器强度"), metric(doc, "DOP"), metric(doc, "S₁ / S₀"), metric(doc, "S₂ / S₀")]; metricNodes.forEach(function (item) { metrics.appendChild(item.node); }); revealed.appendChild(metrics); var formula = element(doc, "div", { className: "ppo-formula", text: "DOP = √(S₁²+S₂²+S₃²) / S₀；I_analyzer = |aᵀE|²" }); revealed.appendChild(formula); var reset = element(doc, "button", { type: "button", className: "ppo-reset", text: "重置实验" }); revealed.appendChild(reset); root.appendChild(revealed);

    function render() { var result = evaluate(state); var s = result.stokes; metricNodes[0].value.textContent = format(result.intensity, 3); metricNodes[1].value.textContent = format(s.dop, 3); metricNodes[2].value.textContent = format(s.s0 ? s.s1 / s.s0 : 0, 3); metricNodes[3].value.textContent = format(s.s0 ? s.s2 / s.s0 : 0, 3); stageTitle.querySelector(".ppo-stage-status").textContent = result.input === "partial" ? "Stokes 必要：无唯一 Jones 相位" : "Jones 级联；反向旋转=" + format(result.rotationDegrees, 1) + "°"; formula.textContent = "I_analyzer=" + format(result.intensity, 3) + "；DOP=" + format(s.dop, 3) + "；(S₁,S₂,S₃)/S₀=(" + format(s.s0 ? s.s1 / s.s0 : 0, 2) + ", " + format(s.s0 ? s.s2 / s.s0 : 0, 2) + ", " + format(s.s0 ? s.s3 / s.s0 : 0, 2) + ")"; clear(chart);
      var left = 48, mid = 153, bottom = 246, width = 620; function x(index) { return left + 75 * index; } function y(value) { return mid - value * 88; } [1, 0, -1].forEach(function (value) { chart.appendChild(svgElement(doc, "line", { class: "ppo-grid", x1: left, x2: 300, y1: y(value), y2: y(value) })); chart.appendChild(svgElement(doc, "text", { class: "ppo-label", x: 8, y: y(value) + 4, text: String(value) })); }); chart.appendChild(svgElement(doc, "line", { class: "ppo-axis", x1: left, x2: 300, y1: y(0), y2: y(0) })); var normalized = [s.s1, s.s2, s.s3].map(function (value) { return s.s0 ? value / s.s0 : 0; }); ["S₁", "S₂", "S₃"].forEach(function (label, index) { var value = normalized[index]; var barY = Math.min(y(0), y(value)); chart.appendChild(svgElement(doc, "rect", { class: "ppo-bar" + (value < 0 ? " ppo-negative" : ""), x: x(index), y: barY, width: 40, height: Math.abs(y(value) - y(0)), rx: 2 })); chart.appendChild(svgElement(doc, "text", { class: "ppo-label", x: x(index) + 20, y: bottom - 5, "text-anchor": "middle", text: label })); chart.appendChild(svgElement(doc, "text", { class: "ppo-label", x: x(index) + 20, y: barY - 6, "text-anchor": "middle", text: format(value, 2) })); }); chart.appendChild(svgElement(doc, "text", { class: "ppo-label", x: left, y: 18, text: "归一化 Stokes 分量" }));
      var cx = 455, cy = 142, radius = 86, vectorX = cx + normalized[0] * radius, vectorY = cy - normalized[1] * radius; chart.appendChild(svgElement(doc, "circle", { class: "ppo-grid", cx: cx, cy: cy, r: radius, fill: "none" })); chart.appendChild(svgElement(doc, "line", { class: "ppo-axis", x1: cx - radius, x2: cx + radius, y1: cy, y2: cy })); chart.appendChild(svgElement(doc, "line", { class: "ppo-axis", x1: cx, x2: cx, y1: cy - radius, y2: cy + radius })); chart.appendChild(svgElement(doc, "line", { class: "ppo-vector", x1: cx, y1: cy, x2: vectorX, y2: vectorY })); chart.appendChild(svgElement(doc, "circle", { class: "ppo-marker", cx: vectorX, cy: vectorY, r: 5, fill: "var(--ppo-gold)" })); chart.appendChild(svgElement(doc, "text", { class: "ppo-label", x: cx - radius, y: cy + radius + 18, text: "S₁/S₀" })); chart.appendChild(svgElement(doc, "text", { class: "ppo-label", x: cx + radius - 34, y: cy - radius - 8, text: "S₂/S₀" }));
    }
    reveal.addEventListener("click", function () { if (state.predictions.some(function (value) { return value === null; })) { feedback.className = "ppo-feedback ppo-warn"; feedback.textContent = "请先完成三个预测，再打开偏振账本。"; return; } var score = state.predictions.reduce(function (sum, value, index) { return sum + (value === questions[index].answer ? 1 : 0); }, 0); feedback.className = "ppo-feedback " + (score === questions.length ? "ppo-pass" : "ppo-warn"); feedback.textContent = "预测 " + score + "/" + questions.length + "。现在把投影强度、相对相位和 DOP 分开。"; revealed.hidden = false; render(); announce(api, root, feedback.textContent); }); clearPredictions.addEventListener("click", function () { state.predictions = [null, null, null]; choiceButtons.forEach(function (buttons) { buttons.forEach(function (button) { button.setAttribute("aria-pressed", "false"); }); }); feedback.className = "ppo-feedback"; feedback.textContent = "预测已清空。"; }); reset.addEventListener("click", function () { state.input = "horizontal"; state.plate = "none"; state.angle = 45; state.plateAngle = 22.5; state.faraday = 0; state.direction = "forward"; revealed.hidden = true; feedback.className = "ppo-feedback"; feedback.textContent = "实验已重新上锁，请再预测。"; announce(api, root, "偏振实验已重置。"); });
  }

  function selfTest() { var checks = 0; function check(condition, message) { checks += 1; if (!condition) throw new Error(message); } var horizontal = evaluate({ input: "horizontal", plate: "none", angle: 45 }); check(near(horizontal.intensity, 0.5), "Malus projection at 45 degrees"); var half = evaluate({ input: "horizontal", plate: "half", plateAngle: 22.5, angle: 45 }); check(near(half.intensity, 1), "half-wave double-angle rotation"); var circular = evaluate({ input: "circular", plate: "none", angle: 30 }); check(near(circular.stokes.dop, 1), "circular state is fully polarized"); check(near(circular.stokes.s3, 1), "Stokes S3 sign convention"); var partial = evaluate({ input: "partial", angle: 30 }); check(near(partial.stokes.dop, 0) && near(partial.intensity, 0.5), "unpolarized state needs Stokes"); check(near(horizontal.stokes.s0, half.stokes.s0), "lossless waveplate preserves S0"); var forward = evaluate({ input: "horizontal", faraday: 45, direction: "forward" }); var reverse = evaluate({ input: "horizontal", faraday: 45, direction: "reverse" }); check(near(forward.rotationDegrees, 45) && near(reverse.rotationDegrees, -45), "nonreciprocal direction sign"); check(JSON.stringify(evaluate({ input: "45", plate: "quarter", plateAngle: 0, angle: 45 })) === JSON.stringify(evaluate({ input: "45", plate: "quarter", plateAngle: 0, angle: 45 })), "deterministic Jones model"); return { checks: checks }; }

  return { complex: complex, evaluate: evaluate, stokes: stokes, waveplate: waveplate, mount: mount, selfTest: selfTest };
});
