(function () {
  "use strict";

  var LAB_ID = "auto-capstone-ledger";
  var SVG_NS = "http://www.w3.org/2000/svg";
  var STYLE_ID = LAB_ID + "-styles";
  var DEFAULTS = { target: 50, ambient: 25, tauNom: 12, gainNom: 4, tauRatio: 1.2, gainRatio: 0.85, kp: 0.06, ki: 0.015, uMax: 1, disturbance: 0.25, disturbanceStart: 12, disturbanceEnd: 18, safetyLimit: 62, duration: 30, dt: 0.2 };
  var CSS = [
    '[data-learning-lab="auto-capstone-ledger"]{--clg-blue:var(--cl-blue,#315f9d);--clg-orange:var(--cl-gold,#9b6a12);--clg-green:var(--cl-green,#39734d);--clg-red:var(--cl-red,#b64335);display:block;max-width:100%;color:var(--fg,currentColor);line-height:1.55;overflow-wrap:anywhere}',
    '[data-learning-lab="auto-capstone-ledger"] *{box-sizing:border-box}[data-learning-lab="auto-capstone-ledger"] [hidden]{display:none!important}[data-learning-lab="auto-capstone-ledger"] h3{margin:0;font-size:1.16rem;letter-spacing:0}[data-learning-lab="auto-capstone-ledger"] p{margin:8px 0}',
    '[data-learning-lab="auto-capstone-ledger"] .clg-note,[data-learning-lab="auto-capstone-ledger"] .clg-feedback{font-size:13px;color:var(--fg-soft,currentColor);line-height:1.7}[data-learning-lab="auto-capstone-ledger"] fieldset{min-width:0;margin:10px 0;padding:9px 10px;border:1px solid var(--border,#cbd5e1);border-radius:6px}[data-learning-lab="auto-capstone-ledger"] legend{max-width:100%;padding:0 4px;font-size:13px;font-weight:750;line-height:1.5}',
    '[data-learning-lab="auto-capstone-ledger"] .clg-choices{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:7px}[data-learning-lab="auto-capstone-ledger"] button,[data-learning-lab="auto-capstone-ledger"] input{font:inherit;letter-spacing:0}[data-learning-lab="auto-capstone-ledger"] button{min-width:0;min-height:44px;padding:8px 11px;border:1px solid var(--border,#cbd5e1);border-radius:6px;background:var(--bg,transparent);color:var(--fg,currentColor);line-height:1.35;cursor:pointer;overflow-wrap:anywhere}[data-learning-lab="auto-capstone-ledger"] button:hover{border-color:var(--accent,#315f9d)}[data-learning-lab="auto-capstone-ledger"] button[aria-pressed="true"],[data-learning-lab="auto-capstone-ledger"] .clg-primary{border-color:var(--accent,#315f9d);background:var(--accent,#315f9d);color:var(--bg,#fff);font-weight:750}[data-learning-lab="auto-capstone-ledger"] button:focus-visible,[data-learning-lab="auto-capstone-ledger"] input:focus-visible{outline:3px solid var(--cl-focus,#1769aa);outline-offset:2px}',
    '[data-learning-lab="auto-capstone-ledger"] .clg-actions{display:flex;flex-wrap:wrap;gap:8px;margin:11px 0}[data-learning-lab="auto-capstone-ledger"] .clg-actions>*{flex:1 1 170px}[data-learning-lab="auto-capstone-ledger"] .clg-feedback{min-height:2em;font-weight:700}[data-learning-lab="auto-capstone-ledger"] .clg-correct{color:var(--clg-green)}[data-learning-lab="auto-capstone-ledger"] .clg-wrong{color:var(--clg-red)}',
    '[data-learning-lab="auto-capstone-ledger"] .clg-layout{display:grid;grid-template-columns:minmax(220px,.72fr) minmax(0,1.28fr);gap:16px;align-items:start}[data-learning-lab="auto-capstone-ledger"] .clg-controls{display:grid;gap:10px;padding:12px;border:1px solid var(--border,#cbd5e1);border-radius:7px}[data-learning-lab="auto-capstone-ledger"] .clg-control{display:grid;gap:5px;min-width:0}[data-learning-lab="auto-capstone-ledger"] .clg-control label{font-size:13px;font-weight:700;color:var(--fg-soft,currentColor)}[data-learning-lab="auto-capstone-ledger"] output{color:var(--accent,#315f9d);font-variant-numeric:tabular-nums}',
    '[data-learning-lab="auto-capstone-ledger"] input[type="range"]{width:100%;min-height:44px;margin:0;accent-color:var(--accent,#315f9d)}[data-learning-lab="auto-capstone-ledger"] .clg-stage-frame{padding:8px;border:1px solid var(--border,#cbd5e1);border-radius:7px;overflow:hidden}[data-learning-lab="auto-capstone-ledger"] svg{display:block;width:100%;height:auto;max-width:100%}[data-learning-lab="auto-capstone-ledger"] svg text{fill:currentColor;font-family:inherit;letter-spacing:0}',
    '[data-learning-lab="auto-capstone-ledger"] .clg-grid{stroke:var(--border,#cbd5e1);stroke-width:1;stroke-opacity:.7}[data-learning-lab="auto-capstone-ledger"] .clg-axis{stroke:currentColor;stroke-width:1.1;stroke-opacity:.75}[data-learning-lab="auto-capstone-ledger"] .clg-nominal{stroke:var(--clg-blue);stroke-width:2.4;stroke-dasharray:6 4;fill:none}[data-learning-lab="auto-capstone-ledger"] .clg-stress{stroke:var(--clg-orange);stroke-width:2.8;fill:none}[data-learning-lab="auto-capstone-ledger"] .clg-input{stroke:var(--clg-green);stroke-width:2.3;fill:none}[data-learning-lab="auto-capstone-ledger"] .clg-safe{stroke:var(--clg-red);stroke-width:1.5;stroke-dasharray:5 4}[data-learning-lab="auto-capstone-ledger"] .clg-target{stroke:currentColor;stroke-width:1.4;stroke-dasharray:3 3}[data-learning-lab="auto-capstone-ledger"] .clg-label{font-size:11px}[data-learning-lab="auto-capstone-ledger"] .clg-small{font-size:10px;fill:var(--fg-soft,currentColor)!important}',
    '[data-learning-lab="auto-capstone-ledger"] .clg-metrics{display:grid;grid-template-columns:repeat(auto-fit,minmax(128px,1fr));gap:8px;margin:12px 0}[data-learning-lab="auto-capstone-ledger"] .clg-metric{min-width:0;padding:9px;border-top:2px solid var(--border,#cbd5e1)}[data-learning-lab="auto-capstone-ledger"] .clg-metric:nth-child(4n+1){border-color:var(--clg-blue)}[data-learning-lab="auto-capstone-ledger"] .clg-metric:nth-child(4n+2){border-color:var(--clg-orange)}[data-learning-lab="auto-capstone-ledger"] .clg-metric:nth-child(4n+3){border-color:var(--clg-green)}[data-learning-lab="auto-capstone-ledger"] .clg-metric:nth-child(4n){border-color:var(--clg-red)}[data-learning-lab="auto-capstone-ledger"] .clg-metric span{display:block;font-size:11.5px;color:var(--fg-soft,currentColor)}[data-learning-lab="auto-capstone-ledger"] .clg-metric strong{display:block;margin-top:3px;font-size:15px;font-variant-numeric:tabular-nums;overflow-wrap:anywhere}',
    '[data-learning-lab="auto-capstone-ledger"] .clg-table{max-width:100%;overflow-x:auto}[data-learning-lab="auto-capstone-ledger"] table{width:100%;min-width:1120px;border-collapse:collapse;font-size:11.5px;font-variant-numeric:tabular-nums}[data-learning-lab="auto-capstone-ledger"] th,[data-learning-lab="auto-capstone-ledger"] td{padding:7px 6px;border-bottom:1px solid var(--border,#cbd5e1);text-align:left;vertical-align:top}[data-learning-lab="auto-capstone-ledger"] th{font-size:11px;color:var(--fg-soft,currentColor)}[data-learning-lab="auto-capstone-ledger"] .clg-evidence{margin-top:11px;padding:10px 12px;border-left:3px solid var(--clg-green);font-size:13px;line-height:1.7}',
    '@media(max-width:900px){[data-learning-lab="auto-capstone-ledger"] .clg-layout{grid-template-columns:minmax(0,1fr)}}@media(max-width:620px){[data-learning-lab="auto-capstone-ledger"] .clg-choices{grid-template-columns:minmax(0,1fr)}}@media(max-width:420px){[data-learning-lab="auto-capstone-ledger"] .clg-stage-frame{padding:4px}[data-learning-lab="auto-capstone-ledger"] table{font-size:10.8px}}@media(prefers-reduced-motion:reduce){[data-learning-lab="auto-capstone-ledger"] *{animation:none!important;transition:none!important}}'
  ].join("");
  function assert(condition, message) { if (!condition) throw new Error(message); }
  function finite(value, label) { var number = Number(value); if (!Number.isFinite(number)) throw new RangeError(label + " must be finite"); return number; }
  function nearly(left, right, tolerance) { var scale = Math.max(1, Math.abs(left), Math.abs(right)); return Math.abs(left - right) <= (tolerance || 1e-8) * scale; }
  function clamp(value, lower, upper) { return Math.min(upper, Math.max(lower, value)); }
  function format(value, digits) { if (!Number.isFinite(value)) return "—"; var places = digits === undefined ? 3 : digits; if (Math.abs(value) > 0 && (Math.abs(value) < 0.001 || Math.abs(value) >= 10000)) return value.toExponential(Math.min(places, 4)); return value.toFixed(places).replace(/0+$/, "").replace(/\.$/, ""); }
  function normalize(input) { var source = input || {}; var config = { target: finite(source.target === undefined ? DEFAULTS.target : source.target, "target"), ambient: finite(source.ambient === undefined ? DEFAULTS.ambient : source.ambient, "ambient"), tauNom: finite(source.tauNom === undefined ? DEFAULTS.tauNom : source.tauNom, "tauNom"), gainNom: finite(source.gainNom === undefined ? DEFAULTS.gainNom : source.gainNom, "gainNom"), tauRatio: finite(source.tauRatio === undefined ? DEFAULTS.tauRatio : source.tauRatio, "tauRatio"), gainRatio: finite(source.gainRatio === undefined ? DEFAULTS.gainRatio : source.gainRatio, "gainRatio"), kp: finite(source.kp === undefined ? DEFAULTS.kp : source.kp, "kp"), ki: finite(source.ki === undefined ? DEFAULTS.ki : source.ki, "ki"), uMax: finite(source.uMax === undefined ? DEFAULTS.uMax : source.uMax, "uMax"), disturbance: finite(source.disturbance === undefined ? DEFAULTS.disturbance : source.disturbance, "disturbance"), disturbanceStart: finite(source.disturbanceStart === undefined ? DEFAULTS.disturbanceStart : source.disturbanceStart, "disturbanceStart"), disturbanceEnd: finite(source.disturbanceEnd === undefined ? DEFAULTS.disturbanceEnd : source.disturbanceEnd, "disturbanceEnd"), safetyLimit: finite(source.safetyLimit === undefined ? DEFAULTS.safetyLimit : source.safetyLimit, "safetyLimit"), duration: finite(source.duration === undefined ? DEFAULTS.duration : source.duration, "duration"), dt: finite(source.dt === undefined ? DEFAULTS.dt : source.dt, "dt") }; if (config.ambient < -20 || config.ambient > 60 || config.target < config.ambient + 1 || config.target > 100) throw new RangeError("temperature range invalid"); if (config.tauNom < 2 || config.tauNom > 40) throw new RangeError("tauNom must be in [2, 40] s"); if (config.gainNom < 0.5 || config.gainNom > 8) throw new RangeError("gainNom must be in [0.5, 8] C/s"); if (config.tauRatio < 0.5 || config.tauRatio > 2) throw new RangeError("tauRatio must be in [0.5, 2]"); if (config.gainRatio < 0.5 || config.gainRatio > 1.5) throw new RangeError("gainRatio must be in [0.5, 1.5]"); if (config.kp < 0.005 || config.kp > 0.2 || config.ki < 0 || config.ki > 0.08) throw new RangeError("PI gains out of range"); if (config.uMax < 0.2 || config.uMax > 1.5) throw new RangeError("uMax must be in [0.2, 1.5]"); if (config.disturbance < 0 || config.disturbance > 2) throw new RangeError("disturbance must be in [0, 2] C/s"); if (config.disturbanceStart < 0 || config.disturbanceStart >= config.disturbanceEnd || config.disturbanceEnd > config.duration) throw new RangeError("disturbance interval invalid"); if (config.safetyLimit <= config.target || config.safetyLimit > 120) throw new RangeError("safetyLimit must exceed target"); if (config.duration < 10 || config.duration > 60) throw new RangeError("duration must be in [10, 60] s"); if (config.dt < 0.05 || config.dt > 0.5) throw new RangeError("dt must be in [0.05, 0.5] s"); return config; }
  function disturbanceAt(time, config) { return time >= config.disturbanceStart && time < config.disturbanceEnd ? config.disturbance : 0; }
  function settlingTime(rows, target, band) { for (var index = 0; index < rows.length; index += 1) { var good = true; for (var tail = index; tail < rows.length; tail += 1) if (Math.abs(rows[tail].temperature - target) > band) { good = false; break; } if (good) return rows[index].time; } return NaN; }
  function simulate(config, stress) { var tau = config.tauNom * (stress ? config.tauRatio : 1); var gain = config.gainNom * (stress ? config.gainRatio : 1); var steps = Math.round(config.duration / config.dt); var temperature = config.ambient; var integral = 0; var rows = []; for (var index = 0; index <= steps; index += 1) { var time = index * config.dt; var error = config.target - temperature; var raw = config.kp * error + integral; var action = clamp(raw, 0, config.uMax); var saturated = Math.abs(action - raw) > 1e-10; var disturbance = disturbanceAt(time, config); rows.push({ index: index, time: time, temperature: temperature, error: error, rawAction: raw, action: action, saturated: saturated, disturbance: disturbance }); if (index < steps) { if (!saturated || (action >= config.uMax && error < 0) || (action <= 0 && error > 0)) integral += config.ki * error * config.dt; var derivative = -(temperature - config.ambient) / tau + gain * action + disturbance; temperature += config.dt * derivative; } } var maxTemperature = Math.max.apply(null, rows.map(function (row) { return row.temperature; })); var maxOvershoot = Math.max(0, maxTemperature - config.target); var saturatedCount = rows.filter(function (row) { return row.saturated; }).length; return { stress: stress, tau: tau, gain: gain, rows: rows, finalTemperature: rows[rows.length - 1].temperature, maxTemperature: maxTemperature, maxOvershoot: maxOvershoot, settlingTime: settlingTime(rows, config.target, 0.02 * Math.max(1, config.target - config.ambient)), saturationFraction: saturatedCount / rows.length, saturatedCount: saturatedCount, minSafetyMargin: config.safetyLimit - maxTemperature }; }
  function run(input) { var config = normalize(input); var nominal = simulate(config, false); var stress = simulate(config, true); var robustnessDeviation = 0; stress.rows.forEach(function (row, index) { robustnessDeviation = Math.max(robustnessDeviation, Math.abs(row.temperature - nominal.rows[index].temperature)); }); return { config: config, nominal: nominal, stress: stress, robustnessDeviation: robustnessDeviation, safe: stress.maxTemperature <= config.safetyLimit }; }
  function element(doc, tag, attrs, children) { var node = doc.createElement(tag); Object.keys(attrs || {}).forEach(function (key) { var value = attrs[key]; if (value === undefined || value === null || value === false) return; if (key === "className") node.setAttribute("class", String(value)); else if (key === "text") node.textContent = String(value); else if (value === true) node.setAttribute(key, ""); else node.setAttribute(key, String(value)); }); (Array.isArray(children) ? children : children === undefined ? [] : [children]).forEach(function (child) { if (child !== undefined && child !== null && child !== false) node.appendChild(child.nodeType ? child : doc.createTextNode(String(child))); }); return node; }
  function svgElement(doc, tag, attrs, children) { var node = doc.createElementNS(SVG_NS, tag); Object.keys(attrs || {}).forEach(function (key) { node.setAttribute(key, String(attrs[key])); }); (Array.isArray(children) ? children : children === undefined ? [] : [children]).forEach(function (child) { if (child !== undefined && child !== null && child !== false) node.appendChild(child.nodeType ? child : doc.createTextNode(String(child))); }); return node; }
  function clear(node) { while (node.firstChild) node.removeChild(node.firstChild); }
  function installStyles(doc) { if (doc.getElementById && doc.getElementById(STYLE_ID)) return; var style = doc.createElement("style"); style.id = STYLE_ID; style.textContent = CSS; (doc.head || doc.documentElement).appendChild(style); }
  function announce(api, root, message) { if (api && typeof api.announce === "function") api.announce(root, message); }
  function metric(doc, label, value) { return element(doc, "div", { className: "clg-metric" }, [element(doc, "span", { text: label }), element(doc, "strong", { text: value })]); }
  function linePath(rows, key, mapX, mapY) { return rows.map(function (row, index) { return (index ? "L" : "M") + mapX(row.time).toFixed(2) + " " + mapY(row[key]).toFixed(2); }).join(" "); }
  function draw(doc, svg, result) { clear(svg); var width = 720; var height = 420; var left = 54; var right = 18; var top = 28; var middle = 218; var bottom = 340; var maxTemp = Math.max(result.stress.maxTemperature, result.config.safetyLimit, result.config.target) + 3; var minTemp = Math.min(result.config.ambient, result.nominal.rows[0].temperature) - 2; var x = function (time) { return left + time / result.config.duration * (width - left - right); }; var yTemp = function (value) { return top + (middle - top) * (maxTemp - value) / (maxTemp - minTemp); }; var yInput = function (value) { return middle + 28 + (bottom - (middle + 28)) * (result.config.uMax - value) / Math.max(result.config.uMax, 0.1); }; svg.appendChild(svgElement(doc, "title", {}, "热回路标称/压力轨迹、输入饱和与安全边界")); svg.appendChild(svgElement(doc, "desc", {}, "上图显示标称与失配扰动温度，下图显示压力轨迹的饱和执行器输入；红虚线为安全上限。")); for (var grid = 0; grid <= 3; grid += 1) { var gy = top + grid / 3 * (middle - top); svg.appendChild(svgElement(doc, "line", { x1: left, y1: gy, x2: width - right, y2: gy, class: "clg-grid" })); var uy = middle + 28 + grid / 2 * (bottom - (middle + 28)); svg.appendChild(svgElement(doc, "line", { x1: left, y1: uy, x2: width - right, y2: uy, class: "clg-grid" })); } svg.appendChild(svgElement(doc, "line", { x1: left, y1: yTemp(result.config.target), x2: width - right, y2: yTemp(result.config.target), class: "clg-target" })); svg.appendChild(svgElement(doc, "line", { x1: left, y1: yTemp(result.config.safetyLimit), x2: width - right, y2: yTemp(result.config.safetyLimit), class: "clg-safe" })); svg.appendChild(svgElement(doc, "line", { x1: left, y1: middle + 28, x2: width - right, y2: middle + 28, class: "clg-axis" })); svg.appendChild(svgElement(doc, "path", { d: linePath(result.nominal.rows, "temperature", x, yTemp), class: "clg-nominal" })); svg.appendChild(svgElement(doc, "path", { d: linePath(result.stress.rows, "temperature", x, yTemp), class: "clg-stress" })); svg.appendChild(svgElement(doc, "path", { d: linePath(result.stress.rows, "action", x, yInput), class: "clg-input" })); svg.appendChild(svgElement(doc, "text", { x: left + 4, y: top + 13, class: "clg-label" }, "温度 T (°C)：标称蓝虚线 / 压力橙实线")); svg.appendChild(svgElement(doc, "text", { x: left + 4, y: middle + 20, class: "clg-label" }, "压力输入 u：绿；红虚线为安全上限")); svg.appendChild(svgElement(doc, "text", { x: width - right, y: height - 20, "text-anchor": "end", class: "clg-small" }, "时间 t (s)；u 为无量纲执行器指令")); }
  function renderTable(doc, host, result) { var body = element(doc, "tbody"); var stride = Math.max(1, Math.floor(result.stress.rows.length / 16)); result.stress.rows.forEach(function (row, index) { if (index % stride !== 0 && index !== result.stress.rows.length - 1) return; var nominal = result.nominal.rows[index]; body.appendChild(element(doc, "tr", {}, [element(doc, "th", { text: String(index) }), element(doc, "td", { text: format(row.time, 1) + " s" }), element(doc, "td", { text: format(nominal.temperature, 2) + " °C" }), element(doc, "td", { text: format(row.temperature, 2) + " °C" }), element(doc, "td", { text: format(row.error, 2) + " °C" }), element(doc, "td", { text: format(row.rawAction, 3) }), element(doc, "td", { text: format(row.action, 3) }), element(doc, "td", { text: row.saturated ? "是" : "否" }), element(doc, "td", { text: format(result.config.safetyLimit - row.temperature, 2) + " °C" })])); }); clear(host); host.appendChild(element(doc, "table", {}, [element(doc, "caption", { text: "压力热回路综合证据 ledger" }), element(doc, "thead", {}, [element(doc, "tr", {}, ["k", "time", "T nominal", "T stress", "error", "u raw", "u sat", "saturated", "safety margin"].map(function (label) { return element(doc, "th", { text: label }); }))]), body])); }
  function questionSpecs() { return [
    { key: "nominal", prompt: "标称 settling time 能否推出失配设备安全？", expected: "no", choices: [{ value: "no", label: "不能，需压力/安全证据" }, { value: "yes", label: "可以直接推出" }, { value: "always", label: "只要 PI 有积分就行" }] },
    { key: "gain", prompt: "提高 Kp 后四类证据会怎样？", expected: "tradeoff", choices: [{ value: "tradeoff", label: "性能与饱和/安全需一起复核" }, { value: "all", label: "必然全部变好" }, { value: "none", label: "完全没有影响" }] },
    { key: "ledger", prompt: "四栏 ledger 能否单独构成普适证书？", expected: "no", choices: [{ value: "no", label: "不能，它是本实验的证据组织" }, { value: "yes", label: "能认证任意闭环" }, { value: "score", label: "可压成一个风险分数" }] }
  ]; }
  function mount(root, api) {
    if (!root || !root.ownerDocument) return; var doc = root.ownerDocument; installStyles(doc); var state = { config: normalize(DEFAULTS), predictions: {}, revealed: false, feedback: "" }; var refs = { questions: [], controls: {} }; var shell = element(doc, "div"); shell.appendChild(element(doc, "h3", { text: "收官实验：性能、鲁棒性、饱和与安全证据 ledger" })); shell.appendChild(element(doc, "p", { className: "clg-note", text: "这是小型热回路 synthesis exercise。温度/裕量用 °C，时间用 s，u 是无量纲执行器指令；四栏证据不合并成认证或普适稳定结论。" }));
    var predictionHost = element(doc, "div"); questionSpecs().forEach(function (spec) { var fieldset = element(doc, "fieldset"); fieldset.appendChild(element(doc, "legend", { text: spec.prompt })); var grid = element(doc, "div", { className: "clg-choices" }); var question = { spec: spec, buttons: [] }; spec.choices.forEach(function (choice) { var button = element(doc, "button", { type: "button", text: choice.label, "aria-pressed": "false" }); button.addEventListener("click", function () { state.predictions[spec.key] = choice.value; state.feedback = ""; render(); }); question.buttons.push({ choice: choice, node: button }); grid.appendChild(button); }); fieldset.appendChild(grid); predictionHost.appendChild(fieldset); refs.questions.push(question); });
    var actions = element(doc, "div", { className: "clg-actions" }); var reveal = element(doc, "button", { type: "button", className: "clg-primary", text: "提交预测并揭晓" }); var reset = element(doc, "button", { type: "button", text: "重置" }); actions.appendChild(reveal); actions.appendChild(reset); var feedback = element(doc, "p", { className: "clg-feedback", "aria-live": "polite" }); var resultShell = element(doc, "div", { hidden: true }); var controls = element(doc, "div", { className: "clg-controls" });
    function range(key, label, min, max, step, digits, unit) { var input = element(doc, "input", { type: "range", min: min, max: max, step: step, value: state.config[key], "aria-label": label }); var output = element(doc, "output", { text: format(state.config[key], digits) + unit }); refs.controls[key] = { input: input, output: output, digits: digits, unit: unit }; input.addEventListener("input", function () { var next = {}; Object.keys(state.config).forEach(function (name) { next[name] = state.config[name]; }); next[key] = Number(input.value); state.config = normalize(next); state.feedback = ""; render(); }); return element(doc, "div", { className: "clg-control" }, [element(doc, "label", {}, [label + " = ", output]), input]); }
    controls.appendChild(range("target", "目标温度", 30, 90, 1, 0, " °C")); controls.appendChild(range("tauNom", "标称时间常数", 2, 40, 1, 0, " s")); controls.appendChild(range("gainNom", "标称热增益", 0.5, 8, 0.1, 1, " °C/s")); controls.appendChild(range("tauRatio", "真实 τ/标称 τ", 0.5, 2, 0.05, 2, "")); controls.appendChild(range("gainRatio", "真实增益/标称", 0.5, 1.5, 0.05, 2, "")); controls.appendChild(range("kp", "比例增益 Kp", 0.005, 0.2, 0.005, 3, "")); controls.appendChild(range("ki", "积分增益 Ki", 0, 0.08, 0.002, 3, "")); controls.appendChild(range("uMax", "执行器上限", 0.2, 1.5, 0.05, 2, "")); controls.appendChild(range("disturbance", "压力扰动", 0, 2, 0.05, 2, " °C/s")); controls.appendChild(range("safetyLimit", "安全上限", 35, 110, 1, 0, " °C"));
    var svg = svgElement(doc, "svg", { viewBox: "0 0 720 420", role: "img", "aria-label": "热回路标称压力轨迹和饱和输入" }); var metrics = element(doc, "div", { className: "clg-metrics" }); var table = element(doc, "div", { className: "clg-table" }); var evidence = element(doc, "div", { className: "clg-evidence" }); resultShell.appendChild(element(doc, "div", { className: "clg-layout" }, [controls, element(doc, "div", { className: "clg-stage" }, [element(doc, "div", { className: "clg-stage-frame" }, [svg]), metrics, table, evidence])])); shell.appendChild(predictionHost); shell.appendChild(actions); shell.appendChild(feedback); shell.appendChild(resultShell); clear(root); root.appendChild(shell);
    reveal.addEventListener("click", function () { var specs = questionSpecs(); if (!specs.every(function (spec) { return state.predictions[spec.key] !== undefined; })) { state.feedback = "请先完成三项预测；四栏证据图和 ledger 会在揭晓后显示。"; render(); return; } var correct = specs.filter(function (spec) { return state.predictions[spec.key] === spec.expected; }).length; state.revealed = true; state.feedback = "已揭晓：" + correct + "/" + specs.length + " 命中。请把每栏失败当作下一项验证任务。"; render(); announce(api, root, state.feedback); }); reset.addEventListener("click", function () { state = { config: normalize(DEFAULTS), predictions: {}, revealed: false, feedback: "" }; render(); announce(api, root, "收官综合预测和证据 ledger 已重置。"); });
    function render() { var result = run(state.config); Object.keys(refs.controls).forEach(function (key) { var ref = refs.controls[key]; ref.input.value = result.config[key]; ref.output.textContent = format(result.config[key], ref.digits) + ref.unit; }); refs.questions.forEach(function (question) { question.buttons.forEach(function (item) { var selected = state.predictions[question.spec.key] === item.choice.value; item.node.setAttribute("aria-pressed", selected ? "true" : "false"); item.node.className = state.revealed && item.choice.value === question.spec.expected ? "clg-correct" : state.revealed && selected ? "clg-wrong" : ""; item.node.textContent = state.revealed && item.choice.value === question.spec.expected ? "✓ " + item.choice.label : item.choice.label; }); }); feedback.textContent = state.feedback; feedback.className = "clg-feedback" + (state.feedback.indexOf("请先") === 0 ? " clg-wrong" : ""); resultShell.hidden = !state.revealed; if (!state.revealed) return; draw(doc, svg, result); clear(metrics); metrics.appendChild(metric(doc, "performance", (Number.isFinite(result.stress.settlingTime) ? format(result.stress.settlingTime, 1) + " s settling" : "未进入误差带") + "; final " + format(result.stress.finalTemperature - result.config.target, 2) + " °C")); metrics.appendChild(metric(doc, "robustness", format(result.robustnessDeviation, 2) + " °C max 偏离")); metrics.appendChild(metric(doc, "saturation", format(result.stress.saturationFraction * 100, 1) + "% samples")); metrics.appendChild(metric(doc, "safety", format(result.stress.minSafetyMargin, 2) + " °C margin")); metrics.appendChild(metric(doc, "压力峰值", format(result.stress.maxTemperature, 2) + " °C")); metrics.appendChild(metric(doc, "安全判断", result.safe ? "当前压力通过" : "当前压力越界")); renderTable(doc, table, result); clear(evidence); evidence.appendChild(element(doc, "p", { text: "综合返回：performance 读取误差/settling，robustness 读取标称与压力轨迹偏离，saturation 读取压力输入撞限比例，safety 读取 T_safe−T_max。当前只对有限时长、固定步长、指定失配/扰动的热 toy model 负责；任何一栏失败都不能由其他栏抵消，也不能写成普适证书。" })); }
    render();
  }
  function selfTest() { var checks = 0; function check(condition, message) { checks += 1; assert(condition, message); } var base = run(DEFAULTS); var repeat = run(DEFAULTS); check(JSON.stringify(base) === JSON.stringify(repeat), "deterministic capstone ledger"); check(base.stress.rows.length === Math.round(DEFAULTS.duration / DEFAULTS.dt) + 1, "fixed Euler ledger length"); check(nearly(base.nominal.rows[0].temperature, DEFAULTS.ambient, 1e-12), "ambient initial condition"); check(base.robustnessDeviation >= 0, "robustness deviation boundary"); check(base.stress.saturationFraction >= 0 && base.stress.saturationFraction <= 1, "saturation fraction bounds"); check(base.stress.maxTemperature <= DEFAULTS.safetyLimit, "default stress safety margin"); var saturated = run({ target: 50, ambient: 25, tauNom: 12, gainNom: 4, tauRatio: 1.2, gainRatio: 0.85, kp: 0.2, ki: 0.08, uMax: 0.2, disturbance: 1, disturbanceStart: 12, disturbanceEnd: 18, safetyLimit: 62, duration: 30, dt: 0.2 }); check(saturated.stress.saturationFraction > 0, "actuator saturation boundary"); var unsafe = run({ target: 50, ambient: 25, tauNom: 12, gainNom: 4, tauRatio: 0.8, gainRatio: 1.5, kp: 0.2, ki: 0.08, uMax: 1.5, disturbance: 2, disturbanceStart: 12, disturbanceEnd: 18, safetyLimit: 50.5, duration: 30, dt: 0.2 }); check(unsafe.stress.maxTemperature > unsafe.config.safetyLimit, "safety boundary can fail"); var rejected = false; try { normalize({ dt: 1 }); } catch (error) { rejected = true; } check(rejected, "invalid time step rejected"); return { checks: checks }; }
  var exported = { DEFAULTS: DEFAULTS, normalize: normalize, disturbanceAt: disturbanceAt, simulate: simulate, run: run, mount: mount, selfTest: selfTest };
  if (typeof module === "object" && module.exports) module.exports = exported;
  if (typeof window !== "undefined" && window.CourseLearning && typeof window.CourseLearning.register === "function") window.CourseLearning.register(LAB_ID, exported.mount);
  if (typeof module === "object" && module.exports && typeof require === "function" && require.main === module) { try { var report = exported.selfTest(); console.log(LAB_ID + " self-test: PASS (" + report.checks + " checks)"); } catch (error) { console.error(LAB_ID + " self-test: FAIL\n" + error.stack); process.exitCode = 1; } }
})();
