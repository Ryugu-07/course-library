(function () {
  "use strict";

  var LAB_ID = "auto-learning-gate";
  var SVG_NS = "http://www.w3.org/2000/svg";
  var STYLE_ID = LAB_ID + "-styles";
  var DEFAULTS = { x0: 1.25, trainRadius: 1, sigmaLimit: 0.25, learnedGain: 1.1, safeGain: 0.65, residualScale: 0.08, uMax: 1.2, steps: 12 };
  var A = 0.92;
  var B = 0.18;
  var CSS = [
    '[data-learning-lab="auto-learning-gate"]{--lg-blue:var(--cl-blue,#315f9d);--lg-orange:var(--cl-gold,#9b6a12);--lg-green:var(--cl-green,#39734d);--lg-red:var(--cl-red,#b64335);display:block;max-width:100%;color:var(--fg,currentColor);line-height:1.55;overflow-wrap:anywhere}',
    '[data-learning-lab="auto-learning-gate"] *{box-sizing:border-box}[data-learning-lab="auto-learning-gate"] [hidden]{display:none!important}[data-learning-lab="auto-learning-gate"] h3{margin:0;font-size:1.16rem;letter-spacing:0}[data-learning-lab="auto-learning-gate"] p{margin:8px 0}',
    '[data-learning-lab="auto-learning-gate"] .lg-note,[data-learning-lab="auto-learning-gate"] .lg-feedback{font-size:13px;color:var(--fg-soft,currentColor);line-height:1.7}[data-learning-lab="auto-learning-gate"] fieldset{min-width:0;margin:10px 0;padding:9px 10px;border:1px solid var(--border,#cbd5e1);border-radius:6px}[data-learning-lab="auto-learning-gate"] legend{max-width:100%;padding:0 4px;font-size:13px;font-weight:750;line-height:1.5}',
    '[data-learning-lab="auto-learning-gate"] .lg-choices{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:7px}[data-learning-lab="auto-learning-gate"] button,[data-learning-lab="auto-learning-gate"] input{font:inherit;letter-spacing:0}[data-learning-lab="auto-learning-gate"] button{min-width:0;min-height:44px;padding:8px 11px;border:1px solid var(--border,#cbd5e1);border-radius:6px;background:var(--bg,transparent);color:var(--fg,currentColor);line-height:1.35;cursor:pointer;overflow-wrap:anywhere}[data-learning-lab="auto-learning-gate"] button:hover{border-color:var(--accent,#315f9d)}[data-learning-lab="auto-learning-gate"] button[aria-pressed="true"],[data-learning-lab="auto-learning-gate"] .lg-primary{border-color:var(--accent,#315f9d);background:var(--accent,#315f9d);color:var(--bg,#fff);font-weight:750}[data-learning-lab="auto-learning-gate"] button:focus-visible,[data-learning-lab="auto-learning-gate"] input:focus-visible{outline:3px solid var(--cl-focus,#1769aa);outline-offset:2px}',
    '[data-learning-lab="auto-learning-gate"] .lg-actions{display:flex;flex-wrap:wrap;gap:8px;margin:11px 0}[data-learning-lab="auto-learning-gate"] .lg-actions>*{flex:1 1 170px}[data-learning-lab="auto-learning-gate"] .lg-feedback{min-height:2em;font-weight:700}[data-learning-lab="auto-learning-gate"] .lg-correct{color:var(--lg-green)}[data-learning-lab="auto-learning-gate"] .lg-wrong{color:var(--lg-red)}',
    '[data-learning-lab="auto-learning-gate"] .lg-layout{display:grid;grid-template-columns:minmax(220px,.72fr) minmax(0,1.28fr);gap:16px;align-items:start}[data-learning-lab="auto-learning-gate"] .lg-controls{display:grid;gap:10px;padding:12px;border:1px solid var(--border,#cbd5e1);border-radius:7px}[data-learning-lab="auto-learning-gate"] .lg-control{display:grid;gap:5px;min-width:0}[data-learning-lab="auto-learning-gate"] .lg-control label{font-size:13px;font-weight:700;color:var(--fg-soft,currentColor)}[data-learning-lab="auto-learning-gate"] output{color:var(--accent,#315f9d);font-variant-numeric:tabular-nums}',
    '[data-learning-lab="auto-learning-gate"] input[type="range"]{width:100%;min-height:44px;margin:0;accent-color:var(--accent,#315f9d)}[data-learning-lab="auto-learning-gate"] .lg-stage-frame{padding:8px;border:1px solid var(--border,#cbd5e1);border-radius:7px;overflow:hidden}[data-learning-lab="auto-learning-gate"] svg{display:block;width:100%;height:auto;max-width:100%}[data-learning-lab="auto-learning-gate"] svg text{fill:currentColor;font-family:inherit;letter-spacing:0}',
    '[data-learning-lab="auto-learning-gate"] .lg-grid{stroke:var(--border,#cbd5e1);stroke-width:1;stroke-opacity:.7}[data-learning-lab="auto-learning-gate"] .lg-axis{stroke:currentColor;stroke-width:1.1;stroke-opacity:.75}[data-learning-lab="auto-learning-gate"] .lg-gated{stroke:var(--lg-blue);stroke-width:2.8;fill:none}[data-learning-lab="auto-learning-gate"] .lg-ungated{stroke:var(--lg-orange);stroke-width:2.3;stroke-dasharray:6 4;fill:none}[data-learning-lab="auto-learning-gate"] .lg-boundary{stroke:var(--lg-red);stroke-width:1.5;stroke-dasharray:5 4}[data-learning-lab="auto-learning-gate"] .lg-label{font-size:11px}[data-learning-lab="auto-learning-gate"] .lg-small{font-size:10px;fill:var(--fg-soft,currentColor)!important}',
    '[data-learning-lab="auto-learning-gate"] .lg-metrics{display:grid;grid-template-columns:repeat(auto-fit,minmax(128px,1fr));gap:8px;margin:12px 0}[data-learning-lab="auto-learning-gate"] .lg-metric{min-width:0;padding:9px;border-top:2px solid var(--border,#cbd5e1)}[data-learning-lab="auto-learning-gate"] .lg-metric:nth-child(3n+1){border-color:var(--lg-blue)}[data-learning-lab="auto-learning-gate"] .lg-metric:nth-child(3n+2){border-color:var(--lg-orange)}[data-learning-lab="auto-learning-gate"] .lg-metric:nth-child(3n){border-color:var(--lg-green)}[data-learning-lab="auto-learning-gate"] .lg-metric span{display:block;font-size:11.5px;color:var(--fg-soft,currentColor)}[data-learning-lab="auto-learning-gate"] .lg-metric strong{display:block;margin-top:3px;font-size:15px;font-variant-numeric:tabular-nums;overflow-wrap:anywhere}',
    '[data-learning-lab="auto-learning-gate"] .lg-table{max-width:100%;overflow-x:auto}[data-learning-lab="auto-learning-gate"] table{width:100%;min-width:980px;border-collapse:collapse;font-size:11.5px;font-variant-numeric:tabular-nums}[data-learning-lab="auto-learning-gate"] th,[data-learning-lab="auto-learning-gate"] td{padding:7px 6px;border-bottom:1px solid var(--border,#cbd5e1);text-align:left;vertical-align:top}[data-learning-lab="auto-learning-gate"] th{font-size:11px;color:var(--fg-soft,currentColor)}[data-learning-lab="auto-learning-gate"] .lg-evidence{margin-top:11px;padding:10px 12px;border-left:3px solid var(--lg-green);font-size:13px;line-height:1.7}',
    '@media(max-width:900px){[data-learning-lab="auto-learning-gate"] .lg-layout{grid-template-columns:minmax(0,1fr)}}@media(max-width:620px){[data-learning-lab="auto-learning-gate"] .lg-choices{grid-template-columns:minmax(0,1fr)}}@media(max-width:420px){[data-learning-lab="auto-learning-gate"] .lg-stage-frame{padding:4px}[data-learning-lab="auto-learning-gate"] table{font-size:10.8px}}@media(prefers-reduced-motion:reduce){[data-learning-lab="auto-learning-gate"] *{animation:none!important;transition:none!important}}'
  ].join("");
  function assert(condition, message) { if (!condition) throw new Error(message); }
  function finite(value, label) { var number = Number(value); if (!Number.isFinite(number)) throw new RangeError(label + " must be finite"); return number; }
  function nearly(left, right, tolerance) { var scale = Math.max(1, Math.abs(left), Math.abs(right)); return Math.abs(left - right) <= (tolerance || 1e-8) * scale; }
  function clamp(value, lower, upper) { return Math.min(upper, Math.max(lower, value)); }
  function sign(value) { return value < 0 ? -1 : value > 0 ? 1 : 0; }
  function format(value, digits) { if (!Number.isFinite(value)) return "—"; var places = digits === undefined ? 3 : digits; if (Math.abs(value) > 0 && (Math.abs(value) < 0.001 || Math.abs(value) >= 10000)) return value.toExponential(Math.min(places, 4)); return value.toFixed(places).replace(/0+$/, "").replace(/\.$/, ""); }
  function normalize(input) { var source = input || {}; var config = { x0: finite(source.x0 === undefined ? DEFAULTS.x0 : source.x0, "x0"), trainRadius: finite(source.trainRadius === undefined ? DEFAULTS.trainRadius : source.trainRadius, "trainRadius"), sigmaLimit: finite(source.sigmaLimit === undefined ? DEFAULTS.sigmaLimit : source.sigmaLimit, "sigmaLimit"), learnedGain: finite(source.learnedGain === undefined ? DEFAULTS.learnedGain : source.learnedGain, "learnedGain"), safeGain: finite(source.safeGain === undefined ? DEFAULTS.safeGain : source.safeGain, "safeGain"), residualScale: finite(source.residualScale === undefined ? DEFAULTS.residualScale : source.residualScale, "residualScale"), uMax: finite(source.uMax === undefined ? DEFAULTS.uMax : source.uMax, "uMax"), steps: Math.round(finite(source.steps === undefined ? DEFAULTS.steps : source.steps, "steps")) }; if (config.x0 < -2 || config.x0 > 2) throw new RangeError("x0 must be in [-2, 2]"); if (config.trainRadius < 0.5 || config.trainRadius > 1.5) throw new RangeError("trainRadius must be in [0.5, 1.5]"); if (config.sigmaLimit < 0.05 || config.sigmaLimit > 1) throw new RangeError("sigmaLimit must be in [0.05, 1]"); if (config.learnedGain < 0.1 || config.learnedGain > 2) throw new RangeError("learnedGain must be in [0.1, 2]"); if (config.safeGain < 0.1 || config.safeGain > 2) throw new RangeError("safeGain must be in [0.1, 2]"); if (config.residualScale < 0.01 || config.residualScale > 0.2) throw new RangeError("residualScale must be in [0.01, 0.2]"); if (config.uMax < 0.3 || config.uMax > 2) throw new RangeError("uMax must be in [0.3, 2]"); if (config.steps < 6 || config.steps > 20) throw new RangeError("steps must be in [6, 20]"); return config; }
  function trueResidual(x, config) { return config.residualScale * x * x * sign(x); }
  function learnedResidual(x, config) { return Math.abs(x) <= config.trainRadius ? 0.95 * trueResidual(x, config) : -1.8 * config.residualScale * x * x * sign(x); }
  function uncertainty(x, config) { return 0.03 + 0.22 * Math.max(0, Math.abs(x) - config.trainRadius) + 0.01 * Math.abs(x); }
  function safeAction(x, config) { return clamp(-config.safeGain * x, -config.uMax, config.uMax); }
  function optimizerAction(x, config) { var best = 0; var bestScore = Infinity; for (var index = 0; index <= 16; index += 1) { var action = -config.uMax + 2 * config.uMax * index / 16; var predicted = A * x + B * action + learnedResidual(x, config); var score = predicted * predicted + 0.04 * action * action; if (score < bestScore) { bestScore = score; best = action; } } return clamp(config.learnedGain * best, -config.uMax, config.uMax); }
  function simulate(config, gated) { var x = config.x0; var rows = []; var gateCount = 0; var maxError = 0; for (var index = 0; index < config.steps; index += 1) { var residualHat = learnedResidual(x, config); var residualTrue = trueResidual(x, config); var sigma = uncertainty(x, config); var inRegion = Math.abs(x) <= config.trainRadius && sigma <= config.sigmaLimit; var learned = optimizerAction(x, config); var safe = safeAction(x, config); var useLearned = gated ? inRegion : true; var action = useLearned ? learned : safe; var predictedNext = A * x + B * action + residualHat; var actualNext = A * x + B * action + residualTrue; var error = actualNext - predictedNext; if (!useLearned) gateCount += 1; maxError = Math.max(maxError, Math.abs(error)); rows.push({ index: index, x: x, residualHat: residualHat, residualTrue: residualTrue, sigma: sigma, inRegion: inRegion, learnedAction: learned, safeAction: safe, action: action, useLearned: useLearned, predictedNext: predictedNext, actualNext: actualNext, modelError: error }); x = actualNext; } return { rows: rows, gateCount: gateCount, maxError: maxError, finalX: x }; }
  function analyze(input) { var config = normalize(input); var gated = simulate(config, true); var ungated = simulate(config, false); return { config: config, gated: gated, ungated: ungated, initialSigma: uncertainty(config.x0, config), initialInRegion: Math.abs(config.x0) <= config.trainRadius && uncertainty(config.x0, config) <= config.sigmaLimit }; }
  function element(doc, tag, attrs, children) { var node = doc.createElement(tag); Object.keys(attrs || {}).forEach(function (key) { var value = attrs[key]; if (value === undefined || value === null || value === false) return; if (key === "className") node.setAttribute("class", String(value)); else if (key === "text") node.textContent = String(value); else if (value === true) node.setAttribute(key, ""); else node.setAttribute(key, String(value)); }); (Array.isArray(children) ? children : children === undefined ? [] : [children]).forEach(function (child) { if (child !== undefined && child !== null && child !== false) node.appendChild(child.nodeType ? child : doc.createTextNode(String(child))); }); return node; }
  function svgElement(doc, tag, attrs, children) { var node = doc.createElementNS(SVG_NS, tag); Object.keys(attrs || {}).forEach(function (key) { node.setAttribute(key, String(attrs[key])); }); (Array.isArray(children) ? children : children === undefined ? [] : [children]).forEach(function (child) { if (child !== undefined && child !== null && child !== false) node.appendChild(child.nodeType ? child : doc.createTextNode(String(child))); }); return node; }
  function clear(node) { while (node.firstChild) node.removeChild(node.firstChild); }
  function installStyles(doc) { if (doc.getElementById && doc.getElementById(STYLE_ID)) return; var style = doc.createElement("style"); style.id = STYLE_ID; style.textContent = CSS; (doc.head || doc.documentElement).appendChild(style); }
  function announce(api, root, message) { if (api && typeof api.announce === "function") api.announce(root, message); }
  function metric(doc, label, value) { return element(doc, "div", { className: "lg-metric" }, [element(doc, "span", { text: label }), element(doc, "strong", { text: value })]); }
  function linePath(rows, key, mapX, mapY) { return rows.map(function (row, index) { return (index ? "L" : "M") + mapX(index).toFixed(2) + " " + mapY(row[key]).toFixed(2); }).join(" "); }
  function draw(doc, svg, result) { clear(svg); var width = 720; var height = 420; var left = 54; var right = 18; var top = 30; var bottom = 312; var all = result.gated.rows.concat(result.ungated.rows).map(function (row) { return row.x; }); all.push(result.config.trainRadius, -result.config.trainRadius); var min = Math.min.apply(null, all); var max = Math.max.apply(null, all); var pad = Math.max(0.15, (max - min) * 0.12); min -= pad; max += pad; var x = function (index) { return left + index / Math.max(result.config.steps - 1, 1) * (width - left - right); }; var y = function (value) { return top + (max - value) / (max - min) * (bottom - top); }; svg.appendChild(svgElement(doc, "title", {}, "学习残差的训练区、门控轨迹和 OOD 对照")); svg.appendChild(svgElement(doc, "desc", {}, "蓝线是带门控轨迹，橙色虚线是始终使用学习残差的对照，红虚线为训练半径边界。")); for (var grid = 0; grid <= 4; grid += 1) { var gy = top + grid / 4 * (bottom - top); svg.appendChild(svgElement(doc, "line", { x1: left, y1: gy, x2: width - right, y2: gy, class: "lg-grid" })); } svg.appendChild(svgElement(doc, "line", { x1: left, y1: y(0), x2: width - right, y2: y(0), class: "lg-axis" })); svg.appendChild(svgElement(doc, "line", { x1: left, y1: y(result.config.trainRadius), x2: width - right, y2: y(result.config.trainRadius), class: "lg-boundary" })); svg.appendChild(svgElement(doc, "line", { x1: left, y1: y(-result.config.trainRadius), x2: width - right, y2: y(-result.config.trainRadius), class: "lg-boundary" })); svg.appendChild(svgElement(doc, "path", { d: linePath(result.gated.rows, "actualNext", x, y), class: "lg-gated" })); svg.appendChild(svgElement(doc, "path", { d: linePath(result.ungated.rows, "actualNext", x, y), class: "lg-ungated" })); svg.appendChild(svgElement(doc, "text", { x: left + 4, y: top + 13, class: "lg-label" }, "状态 x：蓝 gated / 橙 ungated")); svg.appendChild(svgElement(doc, "text", { x: left + 4, y: y(result.config.trainRadius) - 6, class: "lg-small" }, "训练边界 +" + format(result.config.trainRadius, 2))); svg.appendChild(svgElement(doc, "text", { x: left + 4, y: y(-result.config.trainRadius) + 14, class: "lg-small" }, "训练边界 -" + format(result.config.trainRadius, 2))); svg.appendChild(svgElement(doc, "text", { x: width - right, y: height - 30, "text-anchor": "end", class: "lg-small" }, "步 k；状态/动作数值为模型量，非概率")); }
  function renderTable(doc, host, result) { var body = element(doc, "tbody"); result.gated.rows.forEach(function (row) { body.appendChild(element(doc, "tr", {}, [element(doc, "th", { text: String(row.index) }), element(doc, "td", { text: format(row.x, 3) }), element(doc, "td", { text: format(row.residualHat, 3) }), element(doc, "td", { text: format(row.residualTrue, 3) }), element(doc, "td", { text: format(row.sigma, 3) }), element(doc, "td", { text: row.inRegion ? "in" : "OOD" }), element(doc, "td", { text: row.useLearned ? "learned" : "fallback" }), element(doc, "td", { text: format(row.modelError, 3) })])); }); clear(host); host.appendChild(element(doc, "table", {}, [element(doc, "caption", { text: "gated 学习残差 ledger（状态与残差为模型单位）" }), element(doc, "thead", {}, [element(doc, "tr", {}, ["k", "x", "r_hat", "r_true", "sigma proxy", "区域", "动作", "真实−预测"].map(function (label) { return element(doc, "th", { text: label }); }))]), body])); }
  function questionSpecs() { return [
    { key: "uncertainty", prompt: "超出训练半径后，sigma 应怎样处理？", expected: "grow", choices: [{ value: "grow", label: "增大并可触发门控" }, { value: "same", label: "沿用训练区数值" }, { value: "zero", label: "直接设为 0" }] },
    { key: "exploit", prompt: "模型优化器会利用残差外推错误吗？", expected: "yes", choices: [{ value: "yes", label: "可能，形成 model exploit" }, { value: "no", label: "不会，优化自动修正" }, { value: "unknown", label: "只由奖励总分决定" }] },
    { key: "guarantee", prompt: "门控后的短仿真能否宣布全局稳定？", expected: "no", choices: [{ value: "no", label: "不能，需独立边界证据" }, { value: "yes", label: "可以直接宣布" }, { value: "prob", label: "等于失效概率" }] }
  ]; }
  function mount(root, api) {
    if (!root || !root.ownerDocument) return; var doc = root.ownerDocument; installStyles(doc); var state = { config: normalize(DEFAULTS), predictions: {}, revealed: false, feedback: "" }; var refs = { questions: [], controls: {} }; var shell = element(doc, "div"); shell.appendChild(element(doc, "h3", { text: "学习残差实验：训练区、OOD 不确定性与安全门控" })); shell.appendChild(element(doc, "p", { className: "lg-note", text: "a=0.92、b=0.18 是本页的确定性离散对象。sigma 是随 OOD 距离增长的 proxy，不是校准概率；状态 x 和动作 u 也不是物理单位。" }));
    var predictionHost = element(doc, "div"); questionSpecs().forEach(function (spec) { var fieldset = element(doc, "fieldset"); fieldset.appendChild(element(doc, "legend", { text: spec.prompt })); var grid = element(doc, "div", { className: "lg-choices" }); var question = { spec: spec, buttons: [] }; spec.choices.forEach(function (choice) { var button = element(doc, "button", { type: "button", text: choice.label, "aria-pressed": "false" }); button.addEventListener("click", function () { state.predictions[spec.key] = choice.value; state.feedback = ""; render(); }); question.buttons.push({ choice: choice, node: button }); grid.appendChild(button); }); fieldset.appendChild(grid); predictionHost.appendChild(fieldset); refs.questions.push(question); });
    var actions = element(doc, "div", { className: "lg-actions" }); var reveal = element(doc, "button", { type: "button", className: "lg-primary", text: "提交预测并揭晓" }); var reset = element(doc, "button", { type: "button", text: "重置" }); actions.appendChild(reveal); actions.appendChild(reset); var feedback = element(doc, "p", { className: "lg-feedback", "aria-live": "polite" }); var resultShell = element(doc, "div", { hidden: true }); var controls = element(doc, "div", { className: "lg-controls" });
    function range(key, label, min, max, step, digits, unit) { var input = element(doc, "input", { type: "range", min: min, max: max, step: step, value: state.config[key], "aria-label": label }); var output = element(doc, "output", { text: format(state.config[key], digits) + unit }); refs.controls[key] = { input: input, output: output, digits: digits, unit: unit }; input.addEventListener("input", function () { var next = {}; Object.keys(state.config).forEach(function (name) { next[name] = state.config[name]; }); next[key] = Number(input.value); state.config = normalize(next); state.feedback = ""; render(); }); return element(doc, "div", { className: "lg-control" }, [element(doc, "label", {}, [label + " = ", output]), input]); }
    controls.appendChild(range("x0", "初始状态 x0", -2, 2, 0.05, 2, "")); controls.appendChild(range("trainRadius", "训练半径", 0.5, 1.5, 0.05, 2, "")); controls.appendChild(range("sigmaLimit", "sigma 阈值", 0.05, 1, 0.01, 2, "")); controls.appendChild(range("learnedGain", "学习动作增益", 0.1, 2, 0.05, 2, "")); controls.appendChild(range("safeGain", "fallback 增益", 0.1, 2, 0.05, 2, "")); controls.appendChild(range("residualScale", "真实残差尺度", 0.01, 0.2, 0.01, 2, "")); controls.appendChild(range("uMax", "动作上限", 0.3, 2, 0.05, 2, "")); controls.appendChild(range("steps", "仿真步数", 6, 20, 1, 0, " 步"));
    var svg = svgElement(doc, "svg", { viewBox: "0 0 720 420", role: "img", "aria-label": "学习残差训练区域和门控状态轨迹" }); var metrics = element(doc, "div", { className: "lg-metrics" }); var table = element(doc, "div", { className: "lg-table" }); var evidence = element(doc, "div", { className: "lg-evidence" }); resultShell.appendChild(element(doc, "div", { className: "lg-layout" }, [controls, element(doc, "div", { className: "lg-stage" }, [element(doc, "div", { className: "lg-stage-frame" }, [svg]), metrics, table, evidence])])); shell.appendChild(predictionHost); shell.appendChild(actions); shell.appendChild(feedback); shell.appendChild(resultShell); clear(root); root.appendChild(shell);
    reveal.addEventListener("click", function () { var specs = questionSpecs(); if (!specs.every(function (spec) { return state.predictions[spec.key] !== undefined; })) { state.feedback = "请先完成三项预测；门控轨迹、OOD 对照和残差 ledger 会在揭晓后显示。"; render(); return; } var correct = specs.filter(function (spec) { return state.predictions[spec.key] === spec.expected; }).length; state.revealed = true; state.feedback = "已揭晓：" + correct + "/" + specs.length + " 命中。门控是运行时结构，不是全局稳定证明。"; render(); announce(api, root, state.feedback); }); reset.addEventListener("click", function () { state = { config: normalize(DEFAULTS), predictions: {}, revealed: false, feedback: "" }; render(); announce(api, root, "学习残差预测和门控 ledger 已重置。"); });
    function render() { var result = analyze(state.config); Object.keys(refs.controls).forEach(function (key) { var ref = refs.controls[key]; ref.input.value = result.config[key]; ref.output.textContent = format(result.config[key], ref.digits) + ref.unit; }); refs.questions.forEach(function (question) { question.buttons.forEach(function (item) { var selected = state.predictions[question.spec.key] === item.choice.value; item.node.setAttribute("aria-pressed", selected ? "true" : "false"); item.node.className = state.revealed && item.choice.value === question.spec.expected ? "lg-correct" : state.revealed && selected ? "lg-wrong" : ""; item.node.textContent = state.revealed && item.choice.value === question.spec.expected ? "✓ " + item.choice.label : item.choice.label; }); }); feedback.textContent = state.feedback; feedback.className = "lg-feedback" + (state.feedback.indexOf("请先") === 0 ? " lg-wrong" : ""); resultShell.hidden = !state.revealed; if (!state.revealed) return; draw(doc, svg, result); clear(metrics); metrics.appendChild(metric(doc, "初始 sigma", format(result.initialSigma, 3) + " proxy")); metrics.appendChild(metric(doc, "初始区域", result.initialInRegion ? "in-distribution" : "OOD")); metrics.appendChild(metric(doc, "gated fallback", String(result.gated.gateCount) + " 步")); metrics.appendChild(metric(doc, "gated max 模型误差", format(result.gated.maxError, 3) + " x-unit")); metrics.appendChild(metric(doc, "ungated max 误差", format(result.ungated.maxError, 3) + " x-unit")); metrics.appendChild(metric(doc, "动作是否限幅", result.gated.rows.some(function (row) { return Math.abs(row.action) >= result.config.uMax - 1e-9; }) ? "是" : "否")); renderTable(doc, table, result); clear(evidence); evidence.appendChild(element(doc, "p", { text: "边界返回：gated 轨迹在 |x|≤训练半径且 sigma≤阈值时使用学习动作，否则使用 fallback；ungated 对照始终使用学习残差。当前 r_hat 与 r_true 的差值是模型单位误差，不能解读成概率，也不能由这段仿真推出部署系统的全局稳定性。" })); }
    render();
  }
  function selfTest() { var checks = 0; function check(condition, message) { checks += 1; assert(condition, message); } var base = analyze(DEFAULTS); var repeat = analyze(DEFAULTS); check(JSON.stringify(base) === JSON.stringify(repeat), "deterministic gated simulation"); check(nearly(trueResidual(1, DEFAULTS), 0.08, 1e-12), "true residual formula"); check(!base.initialInRegion, "default starts outside training region"); check(base.gated.gateCount > 0, "OOD invokes fallback"); check(base.gated.rows[0].useLearned === false, "initial gate boundary"); check(base.ungated.rows[0].useLearned === true, "ungated comparison uses learner"); check(base.ungated.rows[0].modelError !== 0, "OOD model exploitation error"); var inside = analyze({ x0: 0.5, trainRadius: 1, sigmaLimit: 0.25, learnedGain: 1.1, safeGain: 0.65, residualScale: 0.08, uMax: 1.2, steps: 12 }); var lowerGain = analyze({ x0: 0.5, trainRadius: 1, sigmaLimit: 0.25, learnedGain: 0.5, safeGain: 0.65, residualScale: 0.08, uMax: 1.2, steps: 12 }); var higherGain = analyze({ x0: 0.5, trainRadius: 1, sigmaLimit: 0.25, learnedGain: 1.5, safeGain: 0.65, residualScale: 0.08, uMax: 1.2, steps: 12 }); check(inside.initialInRegion, "in-distribution boundary"); check(inside.gated.rows[0].useLearned, "learner allowed inside region"); check(Math.abs(inside.gated.rows[0].action) <= inside.config.uMax + 1e-12, "action saturation boundary"); check(!nearly(lowerGain.gated.rows[0].action, higherGain.gated.rows[0].action, 1e-12), "learnedGain changes first learned action"); check(!nearly(lowerGain.gated.finalX, higherGain.gated.finalX, 1e-12), "learnedGain changes trajectory"); var rejected = false; try { normalize({ sigmaLimit: 2 }); } catch (error) { rejected = true; } check(rejected, "invalid uncertainty threshold rejected"); return { checks: checks }; }
  var exported = { DEFAULTS: DEFAULTS, normalize: normalize, trueResidual: trueResidual, learnedResidual: learnedResidual, uncertainty: uncertainty, analyze: analyze, mount: mount, selfTest: selfTest };
  if (typeof module === "object" && module.exports) module.exports = exported;
  if (typeof window !== "undefined" && window.CourseLearning && typeof window.CourseLearning.register === "function") window.CourseLearning.register(LAB_ID, exported.mount);
  if (typeof module === "object" && module.exports && typeof require === "function" && require.main === module) { try { var report = exported.selfTest(); console.log(LAB_ID + " self-test: PASS (" + report.checks + " checks)"); } catch (error) { console.error(LAB_ID + " self-test: FAIL\n" + error.stack); process.exitCode = 1; } }
})();
