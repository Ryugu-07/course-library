(function (root, factory) {
  "use strict";
  var exported = factory(root);
  if (typeof module === "object" && module.exports) module.exports = exported;
  if (root && root.CourseLearning && typeof root.CourseLearning.register === "function") root.CourseLearning.register("micro-accelerator", exported.mount);
  if (typeof module === "object" && module.exports && typeof require === "function" && require.main === module) {
    try { var report = exported.selfTest(); console.log("micro-accelerator self-test: PASS (" + report.checks + " checks)"); }
    catch (error) { console.error("micro-accelerator self-test: FAIL\n" + error.stack); process.exitCode = 1; }
  }
})(typeof window !== "undefined" ? window : typeof globalThis !== "undefined" ? globalThis : null, function (host) {
  "use strict";

  var LAB_ID = "micro-accelerator";
  var SVG_NS = "http://www.w3.org/2000/svg";
  var STYLE_ID = "micro-accelerator-styles";
  var PERFORMANCE_UNIT = "GOPS";
  var FLOWS = { cache: { label: "普通缓存", byteFactor: 1 }, weight: { label: "权重驻留", byteFactor: 0.5 }, output: { label: "输出驻留", byteFactor: 0.625 }, row: { label: "行驻留", byteFactor: 0.45 } };
  var DEFAULTS = { peak: 100, bandwidth: 100, intensity: 0.5, flow: "cache" };
  var STYLE_TEXT = [
    '[data-learning-lab="micro-accelerator"]{--ma-blue:var(--cl-blue,#315f9d);--ma-red:var(--cl-red,#b64335);--ma-green:var(--cl-green,#39734d);--ma-gold:var(--cl-gold,#9b6a12);display:block;min-width:0;color:var(--fg,currentColor);line-height:1.55;overflow-wrap:anywhere}',
    '[data-learning-lab="micro-accelerator"] *{box-sizing:border-box}[data-learning-lab="micro-accelerator"] h3,[data-learning-lab="micro-accelerator"] h4{margin:0 0 8px;letter-spacing:0}[data-learning-lab="micro-accelerator"] p{margin:8px 0}',
    '[data-learning-lab="micro-accelerator"] .ma-layout{display:grid;grid-template-columns:minmax(220px,.72fr) minmax(0,1.28fr);gap:16px;align-items:start}[data-learning-lab="micro-accelerator"] .ma-controls{display:grid;gap:11px;padding:12px;border:1px solid var(--border);border-radius:7px;background:var(--bg,transparent)}[data-learning-lab="micro-accelerator"] .ma-control{display:grid;gap:5px}[data-learning-lab="micro-accelerator"] label{color:var(--fg-soft);font-size:13px;font-weight:700}[data-learning-lab="micro-accelerator"] output{color:var(--accent);font-variant-numeric:tabular-nums}',
    '[data-learning-lab="micro-accelerator"] select,[data-learning-lab="micro-accelerator"] input,[data-learning-lab="micro-accelerator"] button{font:inherit;letter-spacing:0}[data-learning-lab="micro-accelerator"] select{width:100%;min-height:44px;padding:7px 10px;border:1px solid var(--border);border-radius:6px;background:var(--bg);color:var(--fg)}[data-learning-lab="micro-accelerator"] input[type="range"]{width:100%;min-height:44px;accent-color:var(--accent)}[data-learning-lab="micro-accelerator"] button{min-height:44px;padding:8px 12px;border:1px solid var(--border);border-radius:6px;background:var(--bg);color:var(--fg);cursor:pointer}[data-learning-lab="micro-accelerator"] button:hover{border-color:var(--accent)}[data-learning-lab="micro-accelerator"] button:focus-visible,[data-learning-lab="micro-accelerator"] select:focus-visible,[data-learning-lab="micro-accelerator"] input:focus-visible{outline:3px solid var(--cl-focus,#1769aa);outline-offset:2px}',
    '[data-learning-lab="micro-accelerator"] .ma-stage{min-width:0;padding:8px;border:1px solid var(--border);border-radius:7px;background:var(--bg,transparent);overflow:hidden}[data-learning-lab="micro-accelerator"] svg{display:block;width:100%;height:auto;color:var(--fg,currentColor)}[data-learning-lab="micro-accelerator"] svg text{fill:currentColor;font-family:inherit;letter-spacing:0}[data-learning-lab="micro-accelerator"] .ma-grid{stroke:var(--border);stroke-width:1;stroke-opacity:.7}[data-learning-lab="micro-accelerator"] .ma-slope{fill:none;stroke:var(--ma-blue);stroke-width:3}[data-learning-lab="micro-accelerator"] .ma-roof{stroke:var(--ma-gold);stroke-width:3}[data-learning-lab="micro-accelerator"] .ma-point{fill:var(--ma-red);stroke:var(--bg);stroke-width:2}',
    '[data-learning-lab="micro-accelerator"] .ma-predict{display:grid;gap:7px;margin:12px 0;padding:11px 12px;border-left:3px solid var(--cl-gold);background:var(--block-bg,var(--bg))}[data-learning-lab="micro-accelerator"] .ma-feedback{min-height:1.6em;color:var(--fg-soft);font-size:13px}[data-learning-lab="micro-accelerator"] .ma-ok{color:var(--ma-green);font-weight:700}[data-learning-lab="micro-accelerator"] .ma-no{color:var(--ma-red);font-weight:700}[data-learning-lab="micro-accelerator"] .ma-note{color:var(--fg-soft);font-size:13px;line-height:1.7}',
    '[data-learning-lab="micro-accelerator"] .ma-metrics{display:grid;grid-template-columns:repeat(auto-fit,minmax(125px,1fr));gap:8px;margin:12px 0}[data-learning-lab="micro-accelerator"] .ma-metric{min-width:0;padding:9px;border-top:2px solid var(--border);background:var(--bg)}[data-learning-lab="micro-accelerator"] .ma-metric span{display:block;color:var(--fg-soft);font-size:11.5px}[data-learning-lab="micro-accelerator"] .ma-metric strong{display:block;margin-top:3px;font-size:15px;font-variant-numeric:tabular-nums;overflow-wrap:anywhere}',
    '@media(max-width:820px){[data-learning-lab="micro-accelerator"] .ma-layout{grid-template-columns:minmax(0,1fr)}}@media(prefers-reduced-motion:reduce){[data-learning-lab="micro-accelerator"] *{transition:none!important;animation:none!important}}'
  ].join("");

  function assert(condition, message) { if (!condition) throw new Error(message); }
  function near(a, b, tolerance) { return Math.abs(a - b) <= (tolerance || 1e-9) * Math.max(1, Math.abs(a), Math.abs(b)); }
  function format(value, digits) {
    var places = digits === undefined ? 2 : Number(digits);
    var text = Number(value).toFixed(places);
    return places === 0 ? text : text.replace(/0+$/, "").replace(/\.$/, "");
  }
  function element(doc, tag, attrs, children) { var node = doc.createElement(tag); Object.keys(attrs || {}).forEach(function (key) { var value = attrs[key]; if (value === undefined || value === null || value === false) return; if (key === "className") node.setAttribute("class", String(value)); else if (key === "htmlFor") node.setAttribute("for", String(value)); else if (key === "text") node.textContent = String(value); else if (value === true) node.setAttribute(key, ""); else node.setAttribute(key, String(value)); }); (Array.isArray(children) ? children : children === undefined ? [] : [children]).forEach(function (child) { if (child !== undefined && child !== null && child !== false) node.appendChild(child.nodeType ? child : doc.createTextNode(String(child))); }); return node; }
  function svgElement(doc, tag, attrs, children) { var node = doc.createElementNS(SVG_NS, tag); Object.keys(attrs || {}).forEach(function (key) { if (attrs[key] !== undefined && attrs[key] !== null) node.setAttribute(key, String(attrs[key])); }); (Array.isArray(children) ? children : children === undefined ? [] : [children]).forEach(function (child) { if (child !== undefined && child !== null) node.appendChild(child.nodeType ? child : doc.createTextNode(String(child))); }); return node; }
  function clear(node) { while (node.firstChild) node.removeChild(node.firstChild); }
  function installStyles(doc) { if (!doc || !doc.createElement || (doc.getElementById && doc.getElementById(STYLE_ID))) return; var style = doc.createElement("style"); style.id = STYLE_ID; style.textContent = STYLE_TEXT; (doc.head || doc.documentElement).appendChild(style); }
  function announce(api, rootNode, message) { if (api && typeof api.announce === "function") api.announce(rootNode, message); }
  function normalize(input) { var source = input || {}; var peak = Number(source.peak === undefined ? DEFAULTS.peak : source.peak); var bandwidth = Number(source.bandwidth === undefined ? DEFAULTS.bandwidth : source.bandwidth); var intensity = Number(source.intensity === undefined ? DEFAULTS.intensity : source.intensity); var flow = source.flow === undefined ? DEFAULTS.flow : String(source.flow); if (peak < 20 || peak > 300 || bandwidth < 20 || bandwidth > 300 || intensity < 0.1 || intensity > 4 || !FLOWS[flow]) throw new RangeError("invalid roofline controls"); return { peak: peak, bandwidth: bandwidth, intensity: intensity, flow: flow }; }
  function calculate(input) { var config = normalize(input); var flow = FLOWS[config.flow]; var effectiveIntensity = config.intensity / flow.byteFactor; var ridge = config.peak / config.bandwidth; var bandwidthBound = config.bandwidth * effectiveIntensity; var attainable = Math.min(config.peak, bandwidthBound); var bound = bandwidthBound < config.peak ? "bandwidth" : "compute"; var endToEnd = 1 / (0.1 + 0.9 / 20); return { config: config, unit: PERFORMANCE_UNIT, effectiveIntensity: effectiveIntensity, ridge: ridge, bandwidthBound: bandwidthBound, attainable: attainable, bound: bound, endToEndCeiling: endToEnd }; }
  function mount(rootNode, api) {
    var doc = rootNode.ownerDocument || document; installStyles(doc); clear(rootNode); var title = element(doc, "h3", { text: "加速器实验：Roofline 与数据流" }); var note = element(doc, "p", { className: "ma-note", text: "先预测：默认算子在带宽斜坡上，还是在计算屋顶上？" });
    var predict = element(doc, "div", { className: "ma-predict" }, [element(doc, "label", { htmlFor: LAB_ID + "-prediction", text: "默认算子的瓶颈：" }), element(doc, "select", { id: LAB_ID + "-prediction", "aria-label": "Roofline 瓶颈预测" }, [element(doc, "option", { value: "bandwidth", text: "带宽" }), element(doc, "option", { value: "compute", text: "计算峰值" })]), element(doc, "button", { type: "button", text: "检验预测" }), element(doc, "div", { className: "ma-feedback", "aria-live": "polite" })]);
    var controls = element(doc, "div", { className: "ma-controls" });
    function addRange(label, id, min, max, step, value, suffix, digits) { var output = element(doc, "output", { for: id, text: format(value, digits) + suffix }); var input = element(doc, "input", { id: id, type: "range", min: min, max: max, step: step, value: value }); input.addEventListener("input", function () { output.textContent = format(Number(input.value), digits) + suffix; render(); }); controls.appendChild(element(doc, "div", { className: "ma-control" }, [element(doc, "label", { htmlFor: id, text: label }), output, input])); return input; }
    var peak = addRange("峰值算力", LAB_ID + "-peak", 20, 200, 10, DEFAULTS.peak, " " + PERFORMANCE_UNIT, 0); var bandwidth = addRange("外部带宽", LAB_ID + "-bandwidth", 20, 200, 10, DEFAULTS.bandwidth, " GB/s", 0); var intensity = addRange("原始计算强度", LAB_ID + "-intensity", 0.1, 4, 0.1, DEFAULTS.intensity, " op/byte", 1);
    var flow = element(doc, "select", { id: LAB_ID + "-flow" }); Object.keys(FLOWS).forEach(function (key) { flow.appendChild(element(doc, "option", { value: key, text: FLOWS[key].label })); }); flow.value = DEFAULTS.flow; flow.addEventListener("change", render); controls.appendChild(element(doc, "div", { className: "ma-control" }, [element(doc, "label", { htmlFor: flow.id, text: "数据流" }), flow]));
    var stage = element(doc, "div", { className: "ma-stage" }); var result = element(doc, "div"); rootNode.appendChild(title); rootNode.appendChild(note); rootNode.appendChild(predict); rootNode.appendChild(element(doc, "div", { className: "ma-layout" }, [controls, element(doc, "div", {}, [stage, result])]));
    function renderStage(data) { var chart = svgElement(doc, "svg", { viewBox: "0 0 720 315", role: "img", "aria-label": "Roofline 图，显示带宽斜坡、计算屋顶和当前算子" }); chart.appendChild(svgElement(doc, "title", {}, "Roofline 性能上限")); chart.appendChild(svgElement(doc, "desc", {}, "蓝线为带宽上限，金线为计算屋顶，红点为当前数据流和计算强度。")); var left = 58, top = 20, width = 625, height = 235, xMax = 4, yMax = Math.max(data.config.peak * 1.15, 60); function x(value) { return left + Math.min(value, xMax) / xMax * width; } function y(value) { return top + height * (1 - Math.min(value, yMax) / yMax); } [0, .25, .5, .75, 1].forEach(function (tick) { var yy = y(yMax * tick); chart.appendChild(svgElement(doc, "line", { x1: left, y1: yy, x2: left + width, y2: yy, class: "ma-grid" })); chart.appendChild(svgElement(doc, "text", { x: left - 8, y: yy + 4, "text-anchor": "end", "font-size": 11 }, format(yMax * tick, 0))); }); [0, 1, 2, 3, 4].forEach(function (tick) { chart.appendChild(svgElement(doc, "text", { x: x(tick), y: top + height + 20, "text-anchor": "middle", "font-size": 11 }, tick + " op/B")); }); chart.appendChild(svgElement(doc, "path", { d: "M" + x(0) + " " + y(0) + " L" + x(xMax) + " " + y(data.config.bandwidth * xMax), class: "ma-slope" })); chart.appendChild(svgElement(doc, "line", { x1: left, y1: y(data.config.peak), x2: left + width, y2: y(data.config.peak), class: "ma-roof" })); chart.appendChild(svgElement(doc, "circle", { cx: x(data.effectiveIntensity), cy: y(data.attainable), r: 7, class: "ma-point" })); chart.appendChild(svgElement(doc, "text", { x: x(data.effectiveIntensity) + 10, y: y(data.attainable) - 7, "font-size": 12 }, format(data.attainable, 1) + " " + data.unit)); chart.appendChild(svgElement(doc, "text", { x: left, y: 288, "font-size": 11 }, "蓝：B*I   金：Ppeak   红：当前工作点；ridge=" + format(data.ridge, 2) + " op/B")); stage.replaceChildren(chart); }
    function render() { var data = calculate({ peak: Number(peak.value), bandwidth: Number(bandwidth.value), intensity: Number(intensity.value), flow: flow.value }); renderStage(data); result.replaceChildren(element(doc, "div", { className: "ma-metrics" }, [element(doc, "div", { className: "ma-metric" }, [element(doc, "span", { text: "有效计算强度" }), element(doc, "strong", { text: format(data.effectiveIntensity, 2) + " op/B" })]), element(doc, "div", { className: "ma-metric" }, [element(doc, "span", { text: "ridge point" }), element(doc, "strong", { text: format(data.ridge, 2) + " op/B" })]), element(doc, "div", { className: "ma-metric" }, [element(doc, "span", { text: "可达上限" }), element(doc, "strong", { text: format(data.attainable, 1) + " " + data.unit })]), element(doc, "div", { className: "ma-metric" }, [element(doc, "span", { text: "当前瓶颈" }), element(doc, "strong", { text: data.bound === "bandwidth" ? "带宽" : "计算" })]), element(doc, "div", { className: "ma-metric" }, [element(doc, "span", { text: "Amdahl ceiling" }), element(doc, "strong", { text: format(data.endToEndCeiling, 1) + " x" })]) ])); }
    predict.querySelector("button").addEventListener("click", function () { var answer = predict.querySelector("select").value; var feedback = predict.querySelector(".ma-feedback"); var ok = answer === "bandwidth"; feedback.className = "ma-feedback " + (ok ? "ma-ok" : "ma-no"); feedback.textContent = ok ? "正确：100 GB/s * 0.5 op/B = 50 GOPS，低于 100 GOPS 峰值。" : "再算 B*I=50 GOPS，与 100 GOPS 取最小值。"; announce(api, rootNode, feedback.textContent); }); render();
  }
  function selfTest() { var checks = 0; function check(condition, message) { checks += 1; assert(condition, message); } var base = calculate(DEFAULTS); check(base.unit === PERFORMANCE_UNIT, "performance unit"); check(near(base.effectiveIntensity, 0.5), "cache intensity"); check(near(base.attainable, 50), "bandwidth bound"); check(base.bound === "bandwidth", "bound classification"); var weight = calculate({ peak: 100, bandwidth: 100, intensity: 0.5, flow: "weight" }); check(near(weight.effectiveIntensity, 1), "weight reuse doubles intensity"); check(near(weight.attainable, 100), "weight stationary reaches roof"); check(near(base.ridge, 1), "ridge point"); check(near(base.endToEndCeiling, 1 / (0.1 + 0.9 / 20)), "Amdahl ceiling"); return { checks: checks }; }
  return { calculate: calculate, mount: mount, selfTest: selfTest, FLOWS: FLOWS, PERFORMANCE_UNIT: PERFORMANCE_UNIT };
});
